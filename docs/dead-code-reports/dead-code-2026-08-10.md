# Dead Code Report — 2026-08-10

## Summary
| Category | Count |
|---|---|
| Dead exports (never imported) | 0 |
| Test-only exports | 0 |
| Possibly used (dynamic import) | 0 |
| Unused local functions | 0 |

No dead code found. `git log --since=2026-07-31 -- src/js/` returns a single
docs-only commit (f67a632 — QA report recovery), meaning **no production JS has
changed since the 2026-07-31 sweep**. This report confirms the clean result
holds and documents the full analysis for audit purposes.

## Dead exports

None. All 36 named exports across the six leaf ES modules (`logger.js`,
`pure-fns.js` barrel, `pure-fns-format.js`, `pure-fns-validate.js`,
`pure-fns-tasks.js`, `pure-fns-export.js`) are consumed — either re-exported
through the `pure-fns.js` barrel and injected into `script.js`'s top-level
`import` block by `readPureFnsExports()` (in `build-config.js`), or, for
`wlLog`, called 145+ times across production files plus once inline in
`work-log.html`.

No `<file>:default` exports exist anywhere in `src/js/`.

| File | Symbol | Type |
|---|---|---|
| — | — | — |

## Test-only exports

None. Every symbol referenced by `test/unit.mjs` or `smoke-tests.cjs` also
has at least one production reference in the non-leaf concatenated bundle or
in `work-log.html`.

| File | Symbol | Used in |
|---|---|---|

## Possibly used (dynamic imports)

None. No `import(...)` call appears in `src/js/*.js`, `build.js`,
`build-portable.js`, `build-config.js`, `deploy-portable.js`,
`smoke-tests.cjs`, `test/unit.mjs`, or `vite.config.js`.

## Unused local functions

None. Every top-level `function` / `const` / `let` / `class` in all 45
concatenated (non-ESM) files and all 6 leaf modules has a whole-scope
reference count ≥ 2.

| File | Function | Lines |
|---|---|---|

## Intentional no-ops and guarded functions (not dead — documented by design)

These are called in production but have empty or always-early-returning bodies.
They are **not** candidates for removal without a deliberate architecture decision.

| File | Function | Notes |
|---|---|---|
| `src/js/03-timer.js` | `updateTimerBarColor()` | Stub kept after timer-bar colour removal; call sites need no change. |
| `src/js/04-render.js` | `renderChart()` | Guards on `#chart` (removed in June 2026 refactor — see CLAUDE.md). Called every 15 min via `setInterval`; always returns immediately. |
| `src/js/08-pomodoro.js` | `checkNewDay()` | Documented empty stub; preserved so the call-site in the 15-min clock tick needs no change. |
| `src/js/09-clock-weather.js` | `updateHeaderTracking()` | No-op since tracked-total moved to Hero Card (June 2026 refactor). |
| `src/js/09-clock-weather.js` | `renderFlagDay()` | Stub kept after flag-day display was removed; called from `tickClock()`. |
| `src/js/19-monthlylog.js` | `initMonthlyLog()` | Documented no-op; preserves the call-site in `07-lifecycle.js`. |

## Methodology notes — adapted for the concatenation model

Only **six** files are true ES modules with `import`/`export` syntax:
`logger.js`, `pure-fns.js` (barrel), `pure-fns-format.js`,
`pure-fns-validate.js`, `pure-fns-tasks.js`, `pure-fns-export.js`. The
remaining **45** `src/js/*.js` files (`00-config.js` … `25-rollingsummary.js`,
excluding `00-config.local.example.js`, which contains no declarations and is
excluded from the build) have no `import`/`export` syntax. `build.js`
concatenates them alphabetically into a single script (`script.js`), and they
call each other's top-level declarations through a shared global scope.
`build-portable.js` does the same for the portable build, additionally
stripping ESM syntax from the six leaf modules and inlining them.

Because cross-file "import site" grepping is meaningless for the 45 concatenated
script files, this report uses a whole-word reference-count signal: a top-level
`function`/`const`/`let`/`class` is a **dead symbol** when its whole-word text
appears ≤ 1 time (its own declaration line only) across the full analysis scope.
Every symbol that reached the scope of investigation was confirmed with a
targeted grep showing ≥ 1 call site outside the declaration line.

- Analysis date: 2026-08-10
- Scope: all 51 files in `src/js/*.js` (52 total, excluding
  `00-config.local.example.js`), `work-log.html`, `build.js`,
  `build-config.js`, `build-portable.js`, `deploy-portable.js`,
  `smoke-tests.cjs`, `test/unit.mjs`, `scripts/config-snapshot.cjs`.
- Top-level symbols analysed: ~477 (same count as 2026-07-31; no source
  changes since that sweep).
- Confirmed the reference-counting approach avoids substring false-matches
  (e.g. `fmtDur` vs `fmtDurLong`).
- Confirmed `work-log.html` has zero inline event-handler attributes that
  could create invisible call sites missed by grepping `.js` files alone.
- The `window.__wl` test-harness object (registered in `13-calendar.js` when
  `?test=1`) exposes ~80 production functions to `smoke-tests.cjs`. All were
  confirmed as also called from production paths, so none are test-only.
- `14-jira.js` wraps its entire body in a self-invoking `initJiraImporter()`
  IIFE; all inner helpers are local to that closure and not globals — verified
  all are called within the IIFE.
