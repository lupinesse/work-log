# Dead Code Report — 2026-06-05

## Summary

| Category | Count |
|---|---|
| Dead exports (never imported) | 0 |
| Test-only exports | 3 |
| Possibly used (dynamic import) | 0 |
| Unused local functions (≥5 lines) | 0 |

## Dead exports

No exports were found that are completely unreferenced by all source files, build
files, and test files.  The build pipeline (`build.js`) generates a single
`import { … }` statement that pulls **every** named export from `pure-fns.js`
into `script.js`, so from the ES-module perspective every symbol is consumed by
the build.  Functional dead-ness (a symbol imported but never called) is
reported under Test-only exports below.

## Test-only exports

Exported from `pure-fns.js` but consumed only in `test/unit.mjs`, not by any
other source module or build file.  Consider whether these need to be part of the
public API, or whether tests could be restructured to access them through a
higher-level wrapper.

| File | Symbol | Type | Notes |
|---|---|---|---|
| `src/js/pure-fns.js` | `roundUp30` | function | Never called in any source file. The comment in `02-utils.js:261` lists it as "in scope" but does not call it. Only callers are `test/unit.mjs`. |
| `src/js/pure-fns.js` | `resolveRapidDate` | function | Called internally within `pure-fns.js` by `parseRapidTokens`; the export exists solely so unit tests can call it directly. No other source module references it. |
| `src/js/pure-fns.js` | `DEFAULT_WORK_LOCATION` | const | Used internally within `pure-fns.js` (inside `locationFor`); the export exists for test assertions. No other source module references it directly — callers use `locationFor()` instead. |

## Possibly used (dynamic imports)

No dynamic `import(…)` calls were found in any source or build file.

## Unused local functions

No non-exported functions with a body of ≥ 5 lines were found that are defined
but never called within their own file.

### No-op stubs (< 5 lines, called — informational only)

The following functions are intentional stubs retained to preserve call-site
compatibility after their feature was removed.  They are short (2–3 lines),
called by other code, and therefore do not qualify as unused locals.  Listed
here as a record in case future sweeps wish to remove them end-to-end.

| File | Function | Reason |
|---|---|---|
| `src/js/03-timer.js` | `updateTimerBarColor()` | Timer-bar colour moved to CSS; stub kept so `tickTimer` call-site compiles unchanged. |
| `src/js/09-clock-weather.js` | `renderFlagDay()` | Flag-day display moved to `fetchCalendarEvents`; stub kept for compatibility. |
| `src/js/09-clock-weather.js` | `updateHeaderTracking()` | Header tracked-total removed; stub kept so `tickClock` and `tickTimer` call-sites compile unchanged. |
| `src/js/08-pomodoro.js` | `checkNewDay()` | New-day banner removed; stub kept, called from `12a-changelog.js`. |

## Methodology notes

- Analysis date: 2026-06-05
- Scope: `src/js/*.js` + `build.js` + `build-portable.js` + `smoke-tests.cjs` + `test/unit.mjs`
- Only `src/js/pure-fns.js` and `src/js/logger.js` contain ES named exports; all
  other source files are concatenated into a flat namespace with no `export`
  statements.
- All `pure-fns.js` exports are pulled into `script.js` by the build script
  (`build.js`) via a generated `import { … }` statement; they are therefore
  "consumed" by the build even if never called in application code.
- Dynamic imports: none found; no modules were marked ⚠️ Possibly used.
- Default exports: neither leaf module uses `export default`; check was skipped.
- Intra-file dead code: scanned all source files for functions defined once with
  ≥ 5 lines of body and zero calls within the same file; none found.
