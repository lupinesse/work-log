# Dead Code Report — 2026-07-31

## Summary
| Category | Count |
|---|---|
| Dead exports (never imported) | 0 |
| Test-only exports | 0 |
| Possibly used (dynamic import) | 0 |
| Unused local functions | 0 |

No dead code found. The codebase remains clean, consistent with every prior
sweep since 2026-06-23. `git log --since=2026-07-10 -- src/js/` is empty, so
no source has changed since the last report either.

## Dead exports

None. All 36 named exports across the six leaf ES modules (`logger.js`,
`pure-fns.js` barrel, `pure-fns-format.js`, `pure-fns-validate.js`,
`pure-fns-tasks.js`, `pure-fns-export.js`) are consumed — either re-exported
through the `pure-fns.js` barrel and injected into `script.js`'s top `import`
line by `readPureFnsExports()` (`build-config.js`), or, for `wlLog`, called
145+ times across production files plus once inline in `work-log.html`.

No `<file>:default` exports exist anywhere in `src/js/`.

| File | Symbol | Type |
|---|---|---|
| — | — | — |

## Test-only exports

None. Every symbol referenced by `test/unit.mjs` or `smoke-tests.cjs` also
has at least one production reference.

| File | Symbol | Used in |
|---|---|---|

## Possibly used (dynamic imports)

None. `grep -rn "import(" src/js/*.js build.js build-portable.js
build-config.js deploy-portable.js smoke-tests.cjs test/unit.mjs
vite.config.js` returns zero matches anywhere in the repo.

## Unused local functions

None. Every top-level `function`/`const`/`let`/`class` in the 43 concatenated
(non-ESM) files has a whole-scope reference count ≥ 2.

| File | Function | Lines |
|---|---|---|

## Methodology notes — adapted for the concatenation model

Only **six** files are true ES modules with `import`/`export` syntax:
`logger.js`, `pure-fns.js` (barrel), `pure-fns-format.js`,
`pure-fns-validate.js`, `pure-fns-tasks.js`, `pure-fns-export.js`. The
remaining **43** `src/js/*.js` files (`00-config.js` … `25-rollingsummary.js`,
excluding `00-config.local.example.js`, which contains no declarations and is
excluded from the build) have no `import`/`export` syntax at all. `build.js`
concatenates them alphabetically into a single script (`script.js`), and they
call each other's top-level declarations through a shared global scope.
`build-portable.js` does the same for the portable build, additionally
stripping ESM syntax from the six leaf modules and inlining them.

Because cross-file "import site" grepping is meaningless for the 43 script
files, this report reuses the same whole-word reference-count signal as prior
sweeps: a top-level `function`/`const`/`let`/`class` is a **dead symbol**
when its whole-word text appears ≤ 1 time (its own declaration line only)
across the full analysis scope.

- Analysis date: 2026-07-31
- Scope: all 49 files in `src/js/*.js`, `work-log.html`, `build.js`,
  `build-config.js`, `build-portable.js`, `deploy-portable.js`,
  `smoke-tests.cjs`, `test/unit.mjs`, `scripts/config-snapshot.cjs`.
- Top-level symbols analysed: 477 (438 across the 43 concatenated files + 39
  across the 6 leaf modules, of which 36 are exported).
- Verified the reference-counting approach avoids substring false-matches
  (e.g. `fmtDur` vs `fmtDurLong`) and confirmed no cross-file duplicate
  top-level symbol names exist.
- Confirmed `work-log.html` has zero inline event-handler attributes
  (`onclick=`, etc.), so there is no hidden HTML→JS reference channel outside
  the grep scope already covered.
- Corroborates the 2026-07-10 report independently — no `src/js/` commits
  landed between the two sweeps.
