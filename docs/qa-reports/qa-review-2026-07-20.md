# QA Review — 2026-07-20

- Commit: `5f7aceb` on `claude/fervent-bardeen-pl8au0`
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated weekly QA review)

> **Environment note:** `npm run lint` (ESLint) and `npm run build` fail in this environment —
> `node_modules/` is empty (0 entries), so no devDependency other than the package manager itself
> is installed (`Cannot find package '@eslint/js'`, `Cannot find package 'sass'`). `smoke-tests.cjs`
> cannot run (Playwright not installed). Unit tests (`npm run test:unit` → **474/74** ✅) and
> CI-script tests (`npm run test:scripts` → **171/32** ✅) execute cleanly since they use only
> Node's built-in test runner — counts are unchanged from the 2026-07-06 review. Items that depend
> on lint, build, or Playwright are marked where CI is the authoritative source, consistent with
> previous reviews. Runtime is Node `22.22.2`; `.nvmrc`/`engines.node` still pin `24.15.0` — a
> residual local-environment constraint, not a repo defect. This is the fourth consecutive review
> blocked from local lint/build verification (see priority 5).

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  77   |
| ⚠️ Partial    |   6   |
| ❌ Not met    |   0   |
| ➖ N/A        |   4   |

**Total items assessed: 87** — unchanged from last review (2026-07-06 / `c2983b2`).

---

## Regressions since last review (2026-07-06 / `c2983b2`)

None found.

---

## Improvements since last review

**None.** All 8 commits since `c2983b2` were non-functional: 1 QA-report commit (`cb29cc9`,
the 2026-07-06 report itself), 2 automated dead-code sweeps (`1f85a0e`, `5f7aceb`, both reporting
"codebase clean" with no deletions), and 4 dependency-bump chores (`eslint` 10.5.0→10.6.0, `vite`
8.0.16→8.1.3, `playwright` 1.61.0→1.61.1, `lint-staged` 17.0.7→17.0.8, `@commitlint/config-conventional`
patch bump). No source file under `src/js/`, `src/css/`, or `docs/*.md` (other than dead-code/QA
reports) changed. As a result, all five priorities raised in the 2026-07-06 report are **unaddressed**
and are carried forward unchanged below.

---

## Priorities for this week

1. **Tag v1.9.1** — `v1.9.0` is still the latest tag (`git tag -l` returns nothing newer;
   tagged 2026-06-12). At least 8 more commits/PRs (#247–#259) have merged since, including two
   user-facing fixes still sitting in `CHANGELOG.md`'s `## Unreleased` section (backup retention
   for all time-series arrays; tabbed board full-width layout). This has now been flagged in the
   2026-06-29, 2026-07-06, and this review — three consecutive audits. A patch tag is overdue and
   cheap to cut.

2. **Continue reducing single-letter lambda arguments** — still **280** occurrences
   (`grep -rPo '\([a-z]\) =>' src/js/`), identical to 2026-07-06 — no further sweep has landed.
   Highest-density files: `12-misc.js` (20), `11-timeflow.js` (20), `14-jira.js` (19),
   `06a-hero.js` (19), `16-rapid.js` (15), `13-calendar.js` (14), `12a-changelog.js` (14),
   `11-timeblock.js` (14), `02-utils.js` (14).

3. **Refresh `QA.md`'s v1.9.0+ delta table** — still reads "466 tests across 73 suites" / dated
   "as of 2026-06-26" (`QA.md:82-88`). Actual counts are unchanged from last review's finding —
   **474 unit tests / 74 suites**, **171 CI-script tests / 32 suites** — so the table is now two
   reviews stale rather than one. Still explicitly dated, so not misleading, but the gap is
   widening.

4. **Split large modules** — unchanged for three consecutive reviews: `04-render.js` (646
   non-blank lines), `09-clock-weather.js` (565), `11-timeflow.js` (536), `06a-hero.js` (512) all
   exceed comfortable single-responsibility thresholds. No split attempted.

5. **Resolve local-environment dependency gap** — `node_modules/` in this review environment
   contains zero installed packages, so `npm run lint`, `npm run build`, and Playwright-based
   smoke tests cannot be verified locally, only through CI. Flagged for a fourth consecutive
   review. Worth confirming the review environment runs `npm ci` before future audits, or noting
   in this skill's instructions that CI status should be pulled directly (e.g. via `gh run list`)
   as a substitute when local install is unavailable.

---

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | 49 JS source files (`ls src/js/*.js \| wc -l`) in named-function functional style; no class inheritance — appropriate for app size. Unchanged. |
| Code grouped into themed files/modules | ✅ Met | Numbered-prefix convention maintained; no new modules or boundary changes since last review. |
| Main scripts import and run high-level functions | ✅ Met | `07-lifecycle.js` `DOMContentLoaded` handler calls only high-level init functions; no inline business logic. |
| Low-level functions each carry out one specific task | ⚠️ Partial | Largest modules unchanged: `04-render.js` (646), `12b-changelog-data.js` (631, data file, acceptable), `09-clock-weather.js` (565), `11-timeflow.js` (536), `06a-hero.js` (512). No split attempted — see priority 4. |
| Repetition minimised | ✅ Met | Shared utilities in `pure-fns-{format,export,tasks,validate}.js` and `02-utils.js`; no new duplication introduced (no source changes this period). |
| Open for extension, closed for modification | ✅ Met | `01b-migrate.js` migrations append-only; `wl_*_v1` key pattern extends storage without touching existing keys. |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `buildBackupPayload`, `buildRollingSummary`, `mergeAdjacentEntries`, `locationFor` — self-documenting names throughout. |
| Names are explicit rather than implicit | ⚠️ Partial | `grep -rPo '\([a-z]\) =>' src/js/` still **280** — identical to 2026-07-06, no movement this period. See priority 2. |
| Code logic is clear and avoids unnecessary complexity | ⚠️ Partial | Four modules above 500 non-blank lines with mixed concerns remain (see modular-code finding). Unchanged. |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ Met | Cannot run `npm run lint` locally this review (devDependencies not installed — see priority 5). Last verifiable state unchanged: `eslint-disable-next-line prefer-const` comment still present and accurate at `10a-tasks-render.js:306`. CI `ci.yml` is authoritative; all 8 commits since last review (4 Dependabot-style bumps, 2 dead-code sweeps, 1 QA report) merged via green CI. |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | `src/`, `dist/` (untracked), `test/`, `docs/`, `.github/`, `scripts/`. Build artefacts not committed. |
| Packages follow a standard structure | ✅ Met | `package.json` + `package-lock.json` committed. `.nvmrc` (`24.15.0`) matches `engines.node` (`>=24.15.0`) exactly. |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | `// Assumption:` blocks in `pure-fns.js`, `05-entries.js`, `07-lifecycle.js` unchanged; no new comments added or needed this period. |
| Comments are up to date and not misleading | ✅ Met | `ARCHITECTURE.md` overview still reads "49 source files", matching `ls src/js/*.js \| wc -l` = 49. |
| Code is not commented out to control which lines run | ✅ Met | No toggled-off code blocks in `src/js/`. |
| All functions and classes are documented | ⚠️ Partial | Cannot run `npm run docs` locally (devDependencies not installed). CI `jsdoc-check.yml` is authoritative; unchanged from previous reviews. |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project. |
| Auto-generated HTML documentation | ⚠️ Partial | `docs.yml` deploys on push to `main` via GitHub Pages. Cannot verify locally. Unchanged. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`; README documentation index links to hosted docs. |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ✅ Met | README current — no changes needed this period. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` present. |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user. |
| QA extent clearly documented | ⚠️ Partial | `QA.md` v1.9.0+ delta table still dated "as of 2026-06-26", stating "466 tests across 73 suites" while actual is **474/74**. Now two reviews stale (unchanged from 2026-07-06 finding, no refresh landed). See priority 3. |
| Assumptions documented next to code | ✅ Met | `// Assumption:` comments unchanged; `DATA.md` documents schema assumptions for all documented keys. |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT); `CITATION.cff` carries `license-expression`. |
| Citation instructions given | ✅ Met | `CITATION.cff` with `preferred-citation`; README cite section. |
| Versioned releases (semantic versioning) | ⚠️ Partial | `v1.9.0` tagged 2026-06-12; no later tag exists (`git tag -l` confirms). Now flagged for a third consecutive review — see priority 1. |
| CHANGELOG available to users | ✅ Met | `CHANGELOG.md` current; `## Unreleased` section still documents the backup-retention fix and tabbed-board width fix, unchanged since last review — consistent with no v1.9.1 tag having been cut yet. |
| Example usage documented | ✅ Met | README usage examples; JSDoc `@example` on core pure functions. |
| Design certificates confirm compliance | ✅ Met | `ARCHITECTURE.md` certificate: `v1.9.0-r2`, last reviewed 2026-06-26, status "Approved". No architecturally significant changes since (this period's 8 commits are dependency bumps, dead-code reports, and a QA report only) that would require a cert bump. |
| Software fully specified | ✅ Met | `ARCHITECTURE.md` module map present for all 49 modules; `DATA.md` field dictionary present. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Git repo; 260+ commits; remote `lupinesse/work-log`. |
| Code committed regularly, per discrete unit of work | ✅ Met | 8 commits since last review, each a single-topic squash commit (4 dependency bumps, 2 dead-code sweeps, 1 QA report, 1 CI fix already counted last review). |
| Appropriate branching strategy defined and used | ✅ Met | All recent commits went through PRs (`claude/`-prefixed or Dependabot-style branches). No direct-to-main commits found. |
| Code is open-sourced; sensitive data omitted | ✅ Met | MIT licence; no credentials in tracked files; `config.local.ps1` and `00-config.local.js` gitignored. |
| Committing standards followed | ⚠️ Partial | All recent commits follow conventional-commit convention (commitlint + Husky). Pre-commitlint history remains. Unchanged. |
| Commits tagged at significant stages | ⚠️ Partial | `v1.9.0` tagged 2026-06-12; no patch tag since despite 8+ more merged commits this period alone. Same underlying gap as "Versioned releases" above — see priority 1. |
| Continuous integration applied | ✅ Met | 16 GitHub Actions workflow files active (`.github/workflows/`); `ci.yml` runs `npm test` and `npm run test:scripts` on every push. All 8 commits since last review merged via green CI. |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | Anthropic key server-side only; local config gitignored. |
| Configuration in dedicated config file | ✅ Met | `src/js/00-config.js`; personal overrides in gitignored `00-config.local.js`. |
| Multiple config files per system/local/user | ✅ Met | Base + user override + server (`config.local.ps1`). |
| Config files versioned separately from analysis code | ✅ Met | `00-config.js` committed independently; overrides untracked. |
| Config used to generate outputs is recorded | ✅ Met | `scripts/config-snapshot.cjs`; `wlLog.config()` at startup. |
| Example config templates provided | ✅ Met | `src/js/00-config.local.example.js` and `config.local.example.ps1`, dummy values only. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ✅ Met | `.lighthouserc.json` accessibility assertion at `"error"` ≥ 0.85. CI fails on any drop. |
| Analysis data stored in an open format | ✅ Met | All data JSON (localStorage + backup export). |
| Input data stored safely and treated as read-only | ✅ Met | Backup merge only adds entries with unseen IDs — existing records never mutated. |
| Input data versioned; changes create new versions | ✅ Met | `01b-migrate.js` append-only migration log; versioned `wl_*_v1` keys. |
| All input data documented in a data register | ✅ Met | `DATA.md` documents input keys and schema. |
| Outputs are disposable — regenerated at will | ✅ Met | `npm run build` regenerates `script.js`, `styles.css`, `dist/`; none committed. |
| Non-sensitive data available so others can run the code | ✅ Met | Self-contained fixtures in `test/unit.mjs`; no personal data in repo. |
| Data quality monitored | ✅ Met | `wlLog.warn()` on `JSON.parse` failures in all `load*()` functions; per-array excluded-record count logged at export time. |
| Fields in datasets documented | ✅ Met | `DATA.md` field dictionary present and current. |
| Large or complex data stored in a database | ➖ N/A | localStorage appropriate for a single-user personal tool. |
| Data documented in information asset register | ✅ Met | `DATA.md` is the register. |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ✅ Met | All 8 commits since last review merged as PRs (#249–#253, #257–#259), consistent with the four-phase ChatGPT/Claude review pipeline (or Dependabot's own review flow for dependency PRs). |
| Pair programming used | ✅ Met | `CONTRIBUTING.md` documents AI-pair model; `CITATION.cff` credits Claude Code. |
| Users encouraged to participate in peer review | ⚠️ Partial | Templates invite contribution; single-user tool limits wider participation. Unchanged. |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit.mjs` — **474 tests across 74 suites, 0 failures**, unchanged from last review (no test-affecting source change this period). |
| Tests run regularly and after every significant change | ✅ Met | `ci.yml` `test` job runs `npm test` on every push, including all 8 dependency/docs-only commits since last review. |
| Bug fixes add new unit tests | ✅ Met | No bug fixes landed this period (0 `fix:` commits); prior fixes (#254, #255) remain covered. |
| Informal tests recorded near the code | ✅ Met | JSDoc `@example` tags on core pure functions in `pure-fns.js`. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ Met | `QA.md` sign-off tables per release (delta table stale by two reviews now — see "QA extent" finding above, not a sign-off gap). |
| Tests run automatically via CI or git hooks | ✅ Met | `ci.yml` full suite; lint-staged pre-commit via Husky. |
| Whole process tested end-to-end | ✅ Met | `smoke-tests.cjs` Playwright suite (cannot run locally this review — Playwright not installed). CI `ci.yml` is authoritative. |
| Test code is clean and readable | ✅ Met | `freshPage()` helper, fixtures, and parameterised cases in both test files, unchanged. |
| Formal user-acceptance testing conducted and recorded | ✅ Met | `QA.md` UAT process and outcomes per release. |
| Integration tests confirm multiple units | ✅ Met | `npm run test:scripts` — **171 tests across 32 suites, 0 failures**, unchanged from last review. |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | Example config files document all secrets with placeholders. |
| Required libraries documented including versions | ✅ Met | `package-lock.json` committed; 4 dependency bumps this period (`eslint` 10.5.0→10.6.0, `vite` 8.0.16→8.1.3, `playwright` 1.61.0→1.61.1, `lint-staged` 17.0.7→17.0.8) all reflected in `package.json`/`package-lock.json`. |
| Working OS environments documented | ✅ Met | README (Windows primary), `launch.sh`, `.devcontainer/devcontainer.json`. |
| Example configuration files provided | ✅ Met | `src/js/00-config.local.example.js`, `config.local.example.ps1`. |
| Code runs independently of operating system | ✅ Met | `path.join()` throughout `build.js` and related scripts. |
| Dependencies managed separately for users/devs/testers | ✅ Met | 0 runtime dependencies; devDependencies only. |
| As few dependencies as possible | ✅ Met | No new dependencies added this period — only patch/minor version bumps to existing ones. |
| Package dependencies managed with environment manager | ✅ Met | npm + lockfile + `.nvmrc` (`24.15.0`), matching `engines.node` exactly. |
| Docker containers / VM builds version controlled | ✅ Met | `.devcontainer/devcontainer.json` (javascript-node:1-24-bookworm). |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ✅ Met | `wlLog.error()`/`warn()` on parse failures, missing DOM, and API errors throughout `src/js/`. |
| Code configuration recorded when code runs | ✅ Met | `wlLog.config()` at startup (`07-lifecycle.js`). |
| Pipeline route recorded when decisions are made | ✅ Met | `wlLog.info` at branch points in `07-lifecycle.js` (e.g. auto-pause decision); per-array excluded-record logging at export time. |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | `CONTRIBUTING.md` documents solo-developer + AI-pair model. |
| Issue tracker records development tasks | ✅ Met | GitHub Issues; recent PRs reference issue/finding closures. |
| New issues guided by user needs and stories | ✅ Met | Issue template "As a user…" sections in `.github/ISSUE_TEMPLATE/`. |
| Issue templates ensure proper logging | ✅ Met | `bug_report.md`, `feature_request.md` in `.github/ISSUE_TEMPLATE/`. |
| Acceptance criteria noted and fulfilment recorded | ✅ Met | PR template acceptance checklist; issues carry Definition of Done. |
| QA standards and processes defined | ✅ Met | `CLAUDE.md` operative standard; `QA.md` process; this automated weekly QA review is the enforcement mechanism — though this period demonstrates the mechanism can flag findings without them being acted on (0 of last review's 5 priorities closed this period). |
