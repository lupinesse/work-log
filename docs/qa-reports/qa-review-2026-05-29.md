# QA Review — 2026-05-29

- Commit: `6db8b5a` on `feat/rapid-token-grammar`
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated weekly QA review)

> **Note:** This review is taken from branch `feat/rapid-token-grammar`. The branch has one uncommitted change: 122 new lines in `00-pure-fns.js` (rapid-token grammar functions). These are in-progress additions — the review reflects the working-tree state where relevant.

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  70   |
| ⚠️ Partial    |  13   |
| ❌ Not met    |   0   |
| ➖ N/A        |   4   |

**Total items assessed: 87**

---

## Regressions since last review (2026-05-27 / `2891245`)

Four items dropped from ✅ to ⚠️:

| Item | Was | Now | Root cause |
|------|-----|-----|------------|
| Comments up to date | ✅ | ⚠️ | `ARCHITECTURE.md` still stamped v1.8.4 / 2026-05-27; app is now v1.8.9 (5 releases later). New modules `10a-tasks-render.js`, `10b-tasks-events.js`, `10b-signifiers.js`, `11-timeflow.js`, `08a-pomo-dashboard.js`, `06a-hero.js`, `12a-changelog.js` are absent from the module map. |
| QA extent clearly documented | ✅ | ⚠️ | `QA.md` still cites 133 unit + 211 smoke = 344 tests (v1.8.4 baseline). Actual counts are **278 unit + 264 smoke = 542 tests** — a 58 % increase that went undocumented. |
| Design certificates confirm compliance | ✅ | ⚠️ | `ARCHITECTURE.md` design certificate has `Covers app version: v1.8.4`; 5 releases have shipped since without an update. |
| Software fully specified | ✅ | ⚠️ | `ARCHITECTURE.md` module map (and its line-count annotations) does not reflect the 7 modules added or split since v1.8.4. |

In addition, two previously-⚠️ items remain ⚠️ but have worsened in a specific respect:

- **JSDoc errors** shifted files: was 8 errors in `00-pure-fns.js` / `10-tasks.js` (both now clean); now **4 errors** in `08a-pomo-dashboard.js` (lines 12, 18 — union type inside an object literal) and `18-dailylog.js` (lines 3, 11 — `?` optional property inside `Array<…>` return type). Halved but not resolved.
- **Commits tagged**: v1.8.1–v1.8.3 were untagged in the previous report — all three now have annotated tags ✅. But **v1.8.9** is in `package.json` and `CHANGELOG.md` with no git tag. Net: still ⚠️.

---

## Improvements since last review

Five items rose in status since 2026-05-27:

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| Branching strategy defined and used | ⚠️ | ✅ | `CONTRIBUTING.md` now has a full "Branching Strategy" section + table; `commitlint.config.js` + `.husky/commit-msg` enforce Conventional Commits. Commit `2348995` (PR #61). |
| Code committed regularly per unit of work | ⚠️ | ✅ | PRs #65–#81 are all single-topic squash commits. The broad catch-all commit pattern from before commitlint enforcement is gone. |
| Pipeline route recorded when decisions made | ⚠️ | ✅ | Issue #12 closed: `wlLog.info` added at branch-decision points in `18-dailylog.js`, `19-monthlylog.js`, `21-reflection.js`, `20-migration.js`, `22-trackers.js`, `23-sprints.js` (commit `d945f44`). |
| renderMonthlyLog() single-purpose | (tracked as ⚠️ modular code) | ✅ | Split into `renderMonthlyCalendar()`, `renderMonthlySummary()`, `renderMonthlyTasks()` + 12-line orchestrator (PR #56, closes #9). |
| 10-tasks.js oversized | ⚠️ | ⚠️ (improved) | Was 1 255 lines; now 137 lines (state/persist only). Rendering split to `10a-tasks-render.js` (596 lines) and events to `10b-tasks-events.js` (599 lines). Responsibility is clearer but the two new files remain large — still ⚠️ on the modular-code item. |

---

## Priorities for this week

1. **Update `ARCHITECTURE.md` to v1.8.9** — The doc is 5 releases behind and missing 7 modules from its module map. Add entries for `06a-hero.js`, `08a-pomo-dashboard.js`, `10a-tasks-render.js`, `10b-tasks-events.js`, `10b-signifiers.js`, `11-timeflow.js`, `12a-changelog.js`. Update the design certificate (`Document version`, `Covers app version`, `Last reviewed`). This single change resolves all 4 regressions simultaneously (stale comments, stale cert, incomplete spec, and `QA.md` should be updated in the same PR with the correct test counts).

2. **Fix 4 remaining JSDoc type-expression ERRORs** — `08a-pomo-dashboard.js` lines 12 and 18: replace `Array<{ts:number,mins:number,task:string|null}>` with `Array.<Object>` (or two `@typedef` blocks). `18-dailylog.js` lines 3 and 11: replace `entryId?: string` with `entryId: string|undefined`. These stop `npm run docs` from emitting ERRORs and ensure all param types appear in the generated HTML.

3. **Tag v1.8.9** — `package.json` and `CHANGELOG.md` both declare v1.8.9 but `git tag -l` shows no tag for it. Run `git tag -a v1.8.9 -m "Release v1.8.9" <merge-sha>` on the commit that merged the v1.8.9 changes into main, then push with `git push origin v1.8.9`.

4. **Consider further splitting `10a-tasks-render.js` (596 lines) and `10b-tasks-events.js` (599 lines)** — the modular split from issue #8 improved responsibility boundaries but both files are still near the 600-line danger zone. In particular `10a-tasks-render.js` mixes checkpoint rendering, task-list rendering, and completed-section rendering — three natural boundaries. Not urgent but should be tracked as a follow-on to issue #8.

5. **Update `QA.md` with current test counts** — record 278 unit + 264 smoke = 542 total, with a v1.8.5–v1.8.9 acceptance delta section. This is a sub-item of priority #1 but listed separately because it can land independently in a single-line PR if ARCHITECTURE.md is delayed.

---

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | 35 JS source files using named-function functional style. No classes used; appropriate for app size. |
| Code grouped into themed files/modules | ✅ Met | Numbered prefix convention (`00-pure-fns.js` … `23-sprints.js`); sub-modules use letter suffixes (`10a`, `10b`, `12a`). Clear theme-per-file. |
| Main scripts import and run high-level functions | ✅ Met | `07-lifecycle.js` `DOMContentLoaded` calls `load()`, `render()`, `initRapid()`, `initMigration()`, `initSprints()` — no inline business logic. |
| Low-level functions each carry out one specific task | ⚠️ Partial | `10a-tasks-render.js` (596 lines) mixes checkpoint, task-list, and completed-section rendering. `10b-tasks-events.js` (599 lines) mixes click-handler registration for all three sub-domains. `05-entries.js` at 526 lines. All improved since last review but still large. |
| Repetition minimised | ✅ Met | `fmtDur`/`fmtDurLong`, `escHtml`, `qa()`, `getCat()`, `wlLog.*` used across all modules without duplication. |
| Open for extension, closed for modification | ✅ Met | Schema migrations in `01b-migrate.js` are append-only; new localStorage keys use versioned suffixes. Rapid token grammar extended in `00-pure-fns.js` without touching call sites. |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `parseRapidTokens`, `resolveRapidDate`, `renderMonthlyCalendar`, `trackerDayStatus` — all self-documenting. |
| Names are explicit rather than implicit | ⚠️ Partial | Single-letter loop variables (`e`, `t`, `d`, `c`, `n`) persist in `.filter`/`.map` chains throughout `10-tasks.js`, `10a-tasks-render.js`, `05-entries.js`. Inconsistent with function-level naming quality. |
| Code logic is clear and avoids unnecessary complexity | ⚠️ Partial | `10a-tasks-render.js` and `10b-tasks-events.js` at ~600 lines each are hard to navigate. `renderEntries()` in `05-entries.js` at 526 lines is the single longest render function. |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ Met | `npm run lint` passes with 0 errors; 16 pre-existing `!important` Stylelint warnings. Commitlint enforced via Husky `commit-msg` hook. |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | `src/` (source), `dist/` (build output), `test/` (tests), `docs/` (generated HTML + QA reports), `.github/` (CI + scripts). Build artefacts not committed. |
| Packages follow a standard structure | ✅ Met | `package.json` + `package-lock.json` committed; `main`, `scripts`, `devDependencies` all correct; `.nvmrc` pins Node 24. |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | `// Assumption:` blocks in 9+ locations; temporal dead zone workaround in `07-lifecycle.js`; BuJo module-level design comments explain trade-offs. |
| Comments are up to date and not misleading | ⚠️ Partial | `ARCHITECTURE.md` is stamped v1.8.4 / 2026-05-27. Seven modules added or split since then (`06a-hero.js`, `08a-pomo-dashboard.js`, `10a-tasks-render.js`, `10b-tasks-events.js`, `10b-signifiers.js`, `11-timeflow.js`, `12a-changelog.js`) are absent from the module map and line-count annotations. Regression since last review. |
| Code is not commented out to control which lines run | ✅ Met | No dead code blocks. One empty `catch {}` in `06-timer.js` carries an explanatory comment. |
| All functions and classes are documented | ⚠️ Partial | `npm run docs` emits 4 ERRORs: `08a-pomo-dashboard.js` lines 12, 18 (`string\|null` inside an object-literal type expression) and `18-dailylog.js` lines 3, 11 (`entryId?` TypeScript optional syntax). Affected functions' param types are silently dropped from HTML output. Was 8 errors last review (now 4 — the `00-pure-fns.js` and `10-tasks.js` errors were fixed). |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project; JSDoc is the equivalent. |
| Auto-generated HTML documentation | ⚠️ Partial | `npm run docs` generates pages for all 35 modules. 4 remaining ERRORs cause param-type loss in generated HTML for `08a-pomo-dashboard.js` and `18-dailylog.js`. `docs.yml` auto-deploys on push to `main`. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`. README links to hosted docs. |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ✅ Met | Has "What it does", "Setup", "Quick start", "Build & test", documentation index. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md` (full PR workflow, branching strategy, commitlint rules) and `CODE_OF_CONDUCT.md` both present. |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user — no audience for desk instructions. |
| QA extent clearly documented | ⚠️ Partial | `QA.md` still cites 133 unit + 211 smoke = 344 total (v1.8.4 baseline). Actual: **278 unit + 264 smoke = 542 tests**. The v1.8.5–v1.8.9 acceptance section is missing. Regression since last review. |
| Assumptions documented next to code | ✅ Met | `// Assumption:` inline comments throughout `00-pure-fns.js`, `05-entries.js`, `07-lifecycle.js`. `DATA.md` documents schema assumptions. |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT). `CITATION.cff` has `license-expression: MIT`. |
| Citation instructions given | ✅ Met | `CITATION.cff` with `preferred-citation`; README has "Cite this project" section. |
| Versioned releases (semantic versioning) | ⚠️ Partial | v1.8.0–v1.8.8 all have annotated git tags (v1.8.1–v1.8.3 fixed since last review). `package.json` and `CHANGELOG.md` declare **v1.8.9** but `git tag -l` shows no `v1.8.9` tag. |
| CHANGELOG available to users | ✅ Met | `CHANGELOG.md` in root, linked from README. `release.yml` extracts sections for GitHub Releases. |
| Example usage documented | ✅ Met | README usage examples; JSDoc `@example` on core pure functions. |
| Design certificates confirm compliance | ⚠️ Partial | `ARCHITECTURE.md` design certificate: `Covers app version: v1.8.4`. Five releases have shipped without an update. Regression since last review. |
| Software fully specified | ⚠️ Partial | `ARCHITECTURE.md` module map covers modules through the v1.8.4 feature set. Seven modules added or split since then are absent. `DATA.md` is up to date (all BuJo localStorage keys present). Regression since last review. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Git repo, 200+ commits, remote at GitHub (`lupinesse/worklog`). |
| Code committed regularly, per discrete unit of work | ✅ Met | PRs #65–#81 are all single-topic squash commits. Pre-commitlint catch-all pattern is gone. Improvement since last review. |
| Appropriate branching strategy defined and used | ✅ Met | `CONTRIBUTING.md` "Branching Strategy" section documents trunk-based flow, prefix table, and squash-merge-to-main rule. Husky enforces commitlint. Improvement since last review. |
| Code is open-sourced; sensitive data omitted | ✅ Met | MIT licence. Anthropic API key moved server-side in v1.8.7 (PR #40, closes #33). No credentials in tracked files. |
| Committing standards followed | ⚠️ Partial | 4 of the last 30 commits predate the commitlint hook: "Add SECURITY.md" (#65), "Log all invalid Jira CSV rows…" (#59), "Add PR workflow Step 5b…" (#58), "Add schema validators…" (#57). All subsequent commits are conformant. Gradually improving. |
| Commits tagged at significant stages | ⚠️ Partial | v1.8.0–v1.8.8 all tagged (v1.8.1–v1.8.3 now corrected since last review). `v1.8.9` documented in CHANGELOG and `package.json` but no git tag exists. |
| Continuous integration applied | ✅ Met | 11 GitHub Actions workflows: `ci.yml`, `docs.yml`, `lighthouse.yml`, `release.yml`, `weekly-qa-review.yml`, `chatgpt-pr-review.yml`, `pr-review.yml`, `auto-fix-ci.yml`, `auto-label-pr.yml`, `codeql.yml`, `delivery-metrics.yml`. |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | Anthropic key moved to server-side proxy in v1.8.7; CSP `connect-src` no longer permits direct browser calls to `api.anthropic.com`. |
| Configuration in dedicated config file | ✅ Met | `src/js/00-config.js` for all app settings; `00-config.local.js` for personal overrides (gitignored). |
| Multiple config files per system/local/user | ✅ Met | Base (`00-config.js`) + user (`00-config.local.js`) + server (`config.local.ps1`). |
| Config files versioned separately from analysis code | ✅ Met | `00-config.js` committed independently; local overrides gitignored. |
| Config used to generate outputs is recorded | ✅ Met | `scripts/config-snapshot.cjs` captures active config at run-time and appends to `timesheets/` output. |
| Example config templates provided | ✅ Met | `00-config.local.example.js` and `config.local.example.ps1` in root with dummy values only. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ✅ Met | `lighthouse.yml` enforces ≥85 % accessibility score on every push. |
| Analysis data stored in an open format | ✅ Met | All data in JSON (localStorage + backup export). |
| Input data stored safely and treated as read-only | ✅ Met | Backup restore writes to fresh localStorage keys; source file never mutated. |
| Input data versioned; changes create new versions | ✅ Met | `01b-migrate.js` — append-only schema migration log; localStorage keys use `_v1` suffix. |
| All input data documented in a data register | ✅ Met | `DATA.md` documents all localStorage keys including all 5 BuJo-feature keys (`wl_lognotes_v1`, `wl_reflection_v1`, `wl_sprints_v1`, `wl_trackers_v1`, `wl_migration_v1`). |
| Outputs are disposable — regularly deleted and regenerated | ✅ Met | `script.js`, `styles.css`, `docs/` all regenerable from source; none committed. |
| Non-sensitive data available so others can run the code | ✅ Met | No personal data in repo. `CATS` fixture in `smoke-tests.cjs` provides self-contained test data. |
| Data quality monitored | ✅ Met | `wlLog.warn()` on every `JSON.parse` failure in all `load*()` functions. |
| Fields in datasets documented | ✅ Met | `DATA.md` has a complete field dictionary for all object types including checkpoint and signifier fields. |
| Large or complex data stored in a database | ➖ N/A | Personal tool; localStorage appropriate for data volume. |
| Data documented in information asset register | ✅ Met | `DATA.md` lists all keys, purpose, and retention policy. |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ✅ Met | PRs #65–#81 squash-merged with ChatGPT + Claude review dialogue; thread-resolution records visible in GitHub PR history. |
| Pair programming used to review code and share knowledge | ✅ Met | `CONTRIBUTING.md` documents AI pair-programming workflow; `CITATION.cff` credits Claude Code as co-contributor. |
| Users encouraged to participate in peer review | ⚠️ Partial | Issue and PR templates invite contribution. Inherent limitation of a personal single-user tool. |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit.cjs` — **278 tests** across 36 `describe` blocks (up from 133 at last review). |
| Tests run regularly and after every significant change | ✅ Met | `ci.yml` runs `npm test` on every push. |
| Bug fixes add new unit tests so the same bug cannot recur | ✅ Met | Regression section 38 in `smoke-tests.cjs` labelled "Regression: three-state checkpoint toggle". |
| Informal tests recorded near the code | ✅ Met | JSDoc `@example` tags on `roundToNearest30`, `safeCssColor`, `fmtDur`, `fmtDurLong`, `parseRapidTokens`. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ Met | `QA.md` documents acceptance criteria and sign-off per release. |
| Tests run automatically via CI or git hooks | ✅ Met | `ci.yml` runs full test suite; lint-staged runs ESLint/Prettier/Stylelint pre-commit. |
| Whole process tested end-to-end | ✅ Met | `smoke-tests.cjs` — **264 smoke tests** covering start-of-day → timer → entries → tasks → carry-over → export → BuJo features. |
| Test code is clean and readable | ✅ Met | `freshPage()` helper, `CATS` fixture, `assert()` wrapper, consistent section headers. |
| Formal user-acceptance testing conducted and recorded | ✅ Met | `QA.md` documents formal UAT process and outcome per release. |
| Integration tests confirm multiple units work together | ✅ Met | Smoke test sections 7, 17, 39 exercise multiple modules together. |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | `00-config.local.example.js` and `config.local.example.ps1` document all required secrets with placeholder values. |
| Required libraries and packages documented including versions | ✅ Met | `package-lock.json` committed; exact versions pinned. |
| Working OS environments documented | ✅ Met | README documents Windows primary; `launch.sh` for Linux/Mac; `.devcontainer/devcontainer.json` for container. |
| Example configuration files provided | ✅ Met | `00-config.local.example.js`, `config.local.example.ps1` in root. |
| Code runs independently of operating system | ✅ Met | `build.js` uses `path.join()` throughout; no Windows-only separators in JS source. |
| Dependencies managed separately for users, developers, testers | ✅ Met | 0 runtime dependencies; 11 devDependencies (added `@commitlint/cli` and `@commitlint/config-conventional` since last review). |
| As few dependencies as possible | ✅ Met | 11 devDependencies, each justified: Vite, ESLint, Prettier, Stylelint, Playwright, Husky, lint-staged, JSDoc, commitlint (×2), eslint-plugin-security. |
| Package dependencies managed with environment manager | ✅ Met | npm + `package-lock.json` + `.nvmrc` (`24.15.0`). All CI workflows and devcontainer aligned to Node 24. |
| Docker containers or VM builds exist and are version controlled | ✅ Met | `.devcontainer/devcontainer.json` at `mcr.microsoft.com/devcontainers/javascript-node:1-24-bookworm`. |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ✅ Met | `wlLog.error()` / `wlLog.warn()` on all parse failures, missing DOM elements, and API errors across all modules. |
| Code configuration recorded when code runs | ✅ Met | `wlLog.config()` in `07-lifecycle.js` logs active config at startup; `scripts/config-snapshot.cjs` persists config to `timesheets/`. |
| Pipeline route recorded when decisions are made | ✅ Met | Issue #12 closed: `wlLog.info` at branch-decision points in all BuJo modules (`18-dailylog.js`, `19-monthlylog.js`, `21-reflection.js`, `20-migration.js`, `22-trackers.js`, `23-sprints.js`). Improvement since last review. |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | `CONTRIBUTING.md` defines the solo-developer + AI-pair model, decision rights, and review responsibilities. |
| Issue tracker records development tasks | ✅ Met | GitHub Issues with labels, acceptance criteria, and Projects board. PRs #55–#81 all reference closure of issues. |
| New issues guided by user needs and stories | ✅ Met | Issue template uses "As a user…" sections with definition of done. |
| Issue templates ensure proper logging | ✅ Met | Both issue templates require title, description, labels, and expected vs actual behaviour. |
| Acceptance criteria noted for issues and their fulfilment recorded | ✅ Met | PR template includes "Acceptance criteria" checklist; issues include "Definition of Done". |
| QA standards and processes defined | ✅ Met | `CLAUDE.md` is the operative QA standard. `QA.md` documents processes. This automated weekly review is the enforcement mechanism. |
