# QA Review — 2026-08-03

- Commit: `e68579f` on `claude/fervent-bardeen-56lzhb` (branched from `main`)
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated weekly QA review)

> **Environment note:** unlike the previous three reviews, `npm ci` succeeded in this
> environment (330 packages installed cleanly), so `npm run lint`, `npm run build`, and
> `npm run docs` were all run and verified **directly**, not just inferred from CI. Results:
> lint **0 errors, 0 warnings**; build succeeds (`script.js` 13,414 lines from 43 files,
> `styles.css` 8,112 lines, Vite bundle 725ms); `npm run docs` (JSDoc) **0 errors**. Unit tests
> (`npm run test:unit` → **530/81** ✅, up from 474/74) and CI-script tests
> (`npm run test:scripts` → **171/32** ✅, unchanged) pass. `smoke-tests.cjs` (Playwright) still
> cannot run locally — not the previous "Playwright not installed" issue, but a browser-revision
> mismatch: `playwright@1.61.1` (bumped from 1.61.0 in #252) expects
> `chromium_headless_shell-1228`, while this environment's pre-installed browser is
> `chromium-1194`/`chromium_headless_shell-1194`. CI (`ci.yml`) is authoritative for this suite
> and has passed on every push since the last review. Runtime is Node `22.22.2` against
> `engines.node`/`.nvmrc` pinned to `24.15.0` — an `EBADENGINE` warning on `npm ci`, not a repo
> defect. This review also unshallowed the local clone and fetched tags, which were both
> incomplete on checkout and gave false signals (e.g. `git merge-base` initially reported no
> common ancestor with `v1.9.0`) until corrected.

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  75   |
| ⚠️ Partial    |   8   |
| ❌ Not met    |   0   |
| ➖ N/A        |   4   |

**Total items assessed: 87** — net −2 ✅ vs last review (2026-07-06 / `c2983b2`). Two items
improved (local doc/lint verification unblocked) but four items regressed, driven mainly by a
newly discovered defect: two of the repo's five automated weekly QA workflows have been silently
non-functional for at least nine consecutive scheduled runs.

---

## Regressions since last review (2026-07-06 / `c2983b2`)

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| QA standards and processes defined | ✅ | ⚠️ | **`dead-code.yml`'s scheduled run has failed on all 9 runs since 2026-06-01** (`gh` run history: 2026-06-01, 06-08, 06-15, 06-22, 06-29, 07-06, 07-13, 07-20, 07-27 — every one `conclusion: failure`). The `/dead-code` skill itself succeeds each time and produces a correct report (e.g. run 30266833600 on 2026-07-27 logged "Report written to `docs/dead-code-reports/dead-code-2026-07-27.md`" and "zero dead code"), but the **"Commit the report" step then fails**: `git push` targets `main` directly and is rejected by branch protection (`remote: error: GH013 ... 2 of 2 required status checks are expected`). The report is generated in the ephemeral runner and then lost — it never lands in the repo. Confirmed: `docs/dead-code-reports/` has no file newer than `dead-code-2026-07-10.md`, itself landed via a manual PR (#259), not the automation. **Separately, `delivery-metrics.yml` has reported `success` on all 9 scheduled runs since 2026-06-01, but is a complete no-op**: `docs/delivery-metrics.md` does not exist in the repo at all. Root cause: `.gitignore` line 9 (`docs/*`) ignores everything under `docs/` except the three explicitly allow-listed subfolders (`qa-reports/`, `dead-code-reports/`, `ab-tests/`) — `docs/delivery-metrics.md` isn't one of them, so the file the workflow writes every run is invisible to `git status --porcelain`, the "no changes to commit" branch fires, and the job exits 0. Nine weeks of "Say/Do ratio" tracking — one of this project's own documented QA mechanisms — has silently produced nothing. |
| Software fully specified | ✅ | ⚠️ | `12c-gapreport.js` (127 lines, added this week in #278 for the end-of-week gap report) has **no entry anywhere in `ARCHITECTURE.md`**, unlike its sibling `12c-startup.js` which is documented as a sub-module bullet under `12a-changelog.js`. `grep -n "gapreport\|Gap Report\|GapReport" ARCHITECTURE.md` returns nothing. |
| Design certificates confirm compliance | ✅ | ⚠️ | The certificate (`Document version 1.9.0-r2`, `Last reviewed 2026-06-26`, `Status: Approved`) has not been refreshed despite 5 feature PRs (#275–#279) landing since, two of which (see below) measurably changed the documented shape of `04-render.js` and `03-timer.js` without the doc being updated. Last review's basis for ✅ — "no architecturally significant changes ... that would require a cert bump" — no longer holds given the module-map gap and stale line counts found this review. |
| Comments are up to date and not misleading | ✅ | ⚠️ | Two of `ARCHITECTURE.md`'s per-module line-count annotations, previously spot-checked as accurate, are now wrong: `04-render.js` documented at **646 lines**, actual `grep -c . src/js/04-render.js` = **762** (+116, +18%, from #276/#278/#279 touching it); `03-timer.js` documented at **494 lines**, actual = **572** (+78, +16%, from #277's long-running-timer warning logic). Five other sampled annotations (`01-state.js`, `02-utils.js`, `06a-hero.js`, `09-clock-weather.js`, `11-timeflow.js`, `12-misc.js`) still match exactly. |

---

## Improvements since last review

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| All functions and classes are documented | ⚠️ | ✅ | Verified directly this review (last three reviews could only defer to CI): `npm run docs` exits with **0 errors, 0 warnings**. Spot-checked the 7 new pure functions added this week (`isWorkdayLikelyOver`, `buildTaskNoteMap`, `buildEntryNoteMap`, `mergeNoteMaps`, `findGapReportEntries`, `mondayOfWeek`, `isLongRunningTimer`) — all carry full JSDoc with `@param`, `@returns`, and worked `@example` blocks. |
| Auto-generated HTML documentation | ⚠️ | ✅ | Same underlying cause as above — `npm run docs` (JSDoc→HTML via `jsdoc.config.json`) now verified locally rather than deferred to `docs.yml` CI. |

Also, though not a formal checklist status change: the local-environment dependency gap flagged as
Priority 5 in the last two reviews (`node_modules/` missing devDependencies) is resolved this
review — `npm ci` completed cleanly, unblocking direct verification of lint/build/docs for the
first time in three consecutive reviews.

---

## Priorities for this week

1. **Fix `dead-code.yml`'s commit step** — it pushes straight to `main`, which branch protection
   has rejected on all 9 scheduled runs since 2026-06-01 (`GH013`, "2 of 2 required status checks
   are expected"). Switch to the branch-then-PR pattern this very report uses (or open a PR the
   way `auto-fix-ci.yml` pushes to a feature branch), so weekly dead-code sweeps actually land
   again — the last one that did was #259 on 2026-07-10, committed by hand, not by the automation.

2. **Fix `delivery-metrics.yml`** — add `docs/delivery-metrics.md` (or whatever path is chosen) to
   the `.gitignore` allow-list at line 9–12, alongside `docs/qa-reports/`, `docs/dead-code-reports/`,
   and `docs/ab-tests/`. The workflow has reported `success` 9 times in a row while never once
   committing a row — the Say/Do ratio feature has been fully inert since it was introduced.

3. **Refresh `ARCHITECTURE.md`** — update the stale line counts (`04-render.js` 646→762,
   `03-timer.js` 494→572), add a module-map entry for `12c-gapreport.js`, and bump the design
   certificate (currently `v1.9.0-r2`, dated 2026-06-26) once the above are corrected — 5 PRs and
   two now-measurably-wrong doc entries is enough drift to warrant it. While in there, note that
   `04-render.js` at 762 lines is now the single largest module in the codebase (previously
   `09-clock-weather.js` at 565) and the "split large modules" backlog item should be re-ranked
   with it at the top.

4. **Tag v1.9.1** — `v1.9.0` was tagged 2026-06-12; HEAD is now **55 commits** ahead with no later
   tag. This is the third consecutive review flagging it — the gap has grown from "5+ PRs" to 55
   commits including 5 user-facing features documented in `CHANGELOG.md`'s `## Unreleased` section.

5. **Refresh `QA.md`'s v1.9.0+ delta table** — still reads "466 tests across 73 suites" / "957
   tests total", dated "as of 2026-06-26"; actual is now **530 unit tests / 81 suites** (965 →
   1,059 total with smoke + CI-script counts unchanged). While fixing it, consider auditing
   `weekly-qa-review.yml` itself — its "Commit the report" step uses the identical direct-push-to-
   `main` pattern that broke `dead-code.yml`. It is currently safe only because it has no
   `schedule:` trigger (`workflow_dispatch` only); if one is ever added without also adopting a
   branch+PR flow, it will fail the same way, silently, on the first scheduled run.

---

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | 50 JS source files (49 excluding the example config) in named-function functional style; new pure functions this week (`isWorkdayLikelyOver`, `findGapReportEntries`, etc.) follow the same convention. |
| Code grouped into themed files/modules | ✅ Met | New `12c-gapreport.js` follows the numbered-prefix convention (see Regressions for its missing doc entry, a documentation gap not a structural one). |
| Main scripts import and run high-level functions | ✅ Met | `07-lifecycle.js` `DOMContentLoaded` handler unchanged — calls only high-level init functions. |
| Low-level functions each carry out one specific task | ⚠️ Partial | `04-render.js` grew to 762 non-blank lines (was 646), now the largest module in the codebase; `03-timer.js` grew to 572 (was 494). No split attempted this period — see Priority 3. |
| Repetition minimised | ✅ Met | New note-handling logic this week explicitly avoids duplication: `buildTaskNoteMap`/`buildEntryNoteMap` are separate pure functions merged via a single `mergeNoteMaps()`, rather than inlining the merge twice. |
| Open for extension, closed for modification | ✅ Met | New `link`/`note` entry fields and the gap-report feature were added without modifying existing validation/migration code paths; `wl_*_v1` key pattern unchanged. |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `isWorkdayLikelyOver`, `findGapReportEntries`, `mondayOfWeek`, `isLongRunningTimer`, `buildEntryNoteMap` — self-documenting names for all of this week's new functions. |
| Names are explicit rather than implicit | ⚠️ Partial | `grep -rPo '\([a-z]\) =>' src/js/` now counts **285** (up marginally from 280 last review, +5 net from this week's feature work). Same top-density files as last review: `14-jira.js` (22), `12-misc.js` (20), `11-timeflow.js` (20), `06a-hero.js` (19). Still Partial — unresolved for a third review. |
| Code logic is clear and avoids unnecessary complexity | ⚠️ Partial | Same modules over 500 non-blank lines flagged last review, now larger: `04-render.js` (762, was 646), `09-clock-weather.js` (565), `11-timeflow.js` (536), `06a-hero.js` (512), `03-timer.js` (572, newly crossed 500). |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ Met | Verified directly this review (previously deferred to CI for three reviews running): `npm run lint` → **0 errors, 0 warnings** across ESLint and Stylelint. |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | `src/`, `dist/` (untracked), `test/`, `docs/`, `.github/`, `scripts/`. `npm run build` verified locally this review — regenerates `script.js`/`styles.css`/`dist/`, none committed (`git status` clean post-build). |
| Packages follow a standard structure | ✅ Met | `package.json` + `package-lock.json` committed and in sync (`npm ls --depth=0` shows no drift). `.nvmrc` (`24.15.0`) matches `engines.node` (`>=24.15.0`). |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | `CHANGELOG.md`'s Unreleased entries for all 5 new features explain rationale (e.g. the long-running-timer threshold cites the two real 12h/7h incidents that motivated it), mirrored in code comments where non-obvious. |
| Comments are up to date and not misleading | ⚠️ Partial | **Regression** — see Regressions table: `04-render.js` and `03-timer.js` line-count annotations in `ARCHITECTURE.md` no longer match reality. |
| Code is not commented out to control which lines run | ✅ Met | No toggled-off code blocks found in `src/js/`. |
| All functions and classes are documented | ✅ Met | **Improvement** — verified directly: `npm run docs` (JSDoc) exits 0 errors. All 7 new pure functions carry full JSDoc with `@param`/`@returns`/`@example`. |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project. |
| Auto-generated HTML documentation | ✅ Met | **Improvement** — verified directly this review rather than deferred to `docs.yml` CI. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`; unchanged. |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ✅ Met | Current; no drift detected against this week's features. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` present, unchanged. |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user. |
| QA extent clearly documented | ⚠️ Partial | `QA.md`'s v1.9.0+ delta table still reads "466 tests across 73 suites" dated "as of 2026-06-26"; actual is now **530 tests / 81 suites**. Gap has widened since last review (was 474/74 vs. documented 466/73; now 530/81) — see Priority 5. |
| Assumptions documented next to code | ✅ Met | `// Assumption:` comments unchanged; `DATA.md` documents the new `link`/`note` field schema and their semantics (lines 22–23). |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT); `CITATION.cff` unchanged. |
| Citation instructions given | ✅ Met | `CITATION.cff` with `preferred-citation`; unchanged. |
| Versioned releases (semantic versioning) | ⚠️ Partial | `v1.9.0` tagged 2026-06-12; HEAD is now **55 commits** ahead with no later tag — third consecutive review flagging this, gap widening. See Priority 4. |
| CHANGELOG available to users | ✅ Met | `CHANGELOG.md`'s Unreleased section documents all 5 new features (end-of-day export reminder, end-of-week gap report, long-running-timer warning, proof-link/note fields, task-notes-in-timesheet) and both carried-over fixes, each with clear rationale. |
| Example usage documented | ✅ Met | README + JSDoc `@example` tags, including all 7 new pure functions this week. |
| Design certificates confirm compliance | ⚠️ Partial | **Regression** — see Regressions table. Certificate unchanged since 2026-06-26 despite 5 feature PRs and now-confirmed doc drift. |
| Software fully specified | ⚠️ Partial | **Regression** — see Regressions table: `12c-gapreport.js` missing from the module map. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Git repo; 55+ commits since last tag; remote `lupinesse/work-log`. |
| Code committed regularly, per discrete unit of work | ✅ Met | 13 commits since last review, each a single-topic conventional-commit; the 5 feature PRs (#275–#279) all landed on 2026-08-02 but each is its own atomic, independently-reviewed PR. |
| Appropriate branching strategy defined and used | ✅ Met | All recent commits went through PRs on `claude/`-prefixed branches; no direct-to-main commits found in the diff since last review. |
| Code is open-sourced; sensitive data omitted | ✅ Met | MIT licence; `grep` for common credential patterns (`sk-ant`, `AKIA`, private-key headers) across `src/` and `.github/` found nothing; local config gitignored. |
| Committing standards followed | ⚠️ Partial | Recent commits follow conventional-commit convention; pre-commitlint history remains further back. Unchanged. |
| Commits tagged at significant stages | ⚠️ Partial | Same underlying gap as "Versioned releases" above — see Priority 4. |
| Continuous integration applied | ✅ Met | `ci.yml` ran and passed on all 13 pushes since last review, including all 5 same-day feature PRs. Note: 2 of the repository's other 15 workflow files (`dead-code.yml`, `delivery-metrics.yml`) were found this review to be silently non-functional on every scheduled run — see Regressions and "QA standards and processes defined" below; the core test/lint/build gate itself is unaffected and trustworthy. |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | Confirmed via targeted grep this review (see Version control); local config gitignored. |
| Configuration in dedicated config file | ✅ Met | `src/js/00-config.js`; unchanged. |
| Multiple config files per system/local/user | ✅ Met | Base + user override + server config; unchanged. |
| Config files versioned separately from analysis code | ✅ Met | Unchanged. |
| Config used to generate outputs is recorded | ✅ Met | `scripts/config-snapshot.cjs`; `wlLog.config()` at startup; unchanged. |
| Example config templates provided | ✅ Met | `src/js/00-config.local.example.js`, `config.local.example.ps1`; unchanged. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ✅ Met | `.lighthouserc.json` accessibility assertion unchanged; CI-enforced. |
| Analysis data stored in an open format | ✅ Met | All data JSON; new `link`/`note` fields are plain strings. |
| Input data stored safely and treated as read-only | ✅ Met | Unchanged merge/append-only pattern. |
| Input data versioned; changes create new versions | ✅ Met | `01b-migrate.js` unchanged; new fields are additive/optional, no migration needed. |
| All input data documented in a data register | ✅ Met | `DATA.md` documents the new `link`/`note` fields (lines 22–23) — added same week as the feature, not lagging. |
| Outputs are disposable — regenerated at will | ✅ Met | `npm run build` verified to regenerate cleanly this review. |
| Non-sensitive data available so others can run the code | ✅ Met | Self-contained fixtures in `test/unit.mjs`; unchanged. |
| Data quality monitored | ✅ Met | `wlLog.warn()` on parse failures unchanged; `validEntry` extended with type-checks for optional `link`/`note` (new unit tests confirm). |
| Fields in datasets documented | ✅ Met | `DATA.md` current, including this week's additions. |
| Large or complex data stored in a database | ➖ N/A | localStorage appropriate for a single-user personal tool. |
| Data documented in information asset register | ✅ Met | `DATA.md` is the register; current. |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ✅ Met | All 13 commits since last review landed via PR, consistent with the four-phase ChatGPT/Claude review pipeline (`ci.yml` green on every one). |
| Pair programming used | ✅ Met | `CONTRIBUTING.md` documents AI-pair model; unchanged. |
| Users encouraged to participate in peer review | ⚠️ Partial | Templates invite contribution; single-user tool limits wider participation. Unchanged. |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit.mjs` — **530 tests across 81 suites, 0 failures** (up from 474/74 last review). |
| Tests run regularly and after every significant change | ✅ Met | `ci.yml` `test` job ran on all 13 pushes since last review. |
| Bug fixes add new unit tests | ✅ Met | No bug fixes landed this period (all 5 PRs were `feat:`); prior pattern (#255) remains the most recent example and is unchanged. |
| Informal tests recorded near the code | ✅ Met | JSDoc `@example` tags on all new pure functions this week. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ Met | `QA.md` sign-off tables per release (delta table stale — see "QA extent" finding, not a sign-off gap). |
| Tests run automatically via CI or git hooks | ✅ Met | `ci.yml` full suite; lint-staged pre-commit via Husky; unchanged. |
| Whole process tested end-to-end | ✅ Met | `smoke-tests.cjs` cannot run locally this review (Playwright browser-revision mismatch after the 1.61.1 bump — see environment note); `ci.yml` is authoritative and has passed on every push. |
| Test code is clean and readable | ✅ Met | `freshPage()` helper and parameterised cases unchanged; new tests for this week's 7 pure functions follow the same pattern. |
| Formal user-acceptance testing conducted and recorded | ✅ Met | `QA.md` UAT process unchanged. |
| Integration tests confirm multiple units | ✅ Met | `npm run test:scripts` — **171 tests across 32 suites, 0 failures**, unchanged from last review. |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | Example config files unchanged. |
| Required libraries documented including versions | ✅ Met | `package-lock.json` verified in sync via `npm ls --depth=0`; no unpinned or drifted packages. |
| Working OS environments documented | ✅ Met | README, `launch.sh`, `.devcontainer/devcontainer.json` unchanged. |
| Example configuration files provided | ✅ Met | Unchanged. |
| Code runs independently of operating system | ✅ Met | `path.join()` usage unchanged. |
| Dependencies managed separately for users/devs/testers | ✅ Met | 0 runtime dependencies; devDependencies only, confirmed via `npm ls`. |
| As few dependencies as possible | ✅ Met | No new dependencies added since last review (only version bumps: eslint, vite, playwright, lint-staged, @commitlint/config-conventional — all already justified). |
| Package dependencies managed with environment manager | ✅ Met | npm + lockfile + `.nvmrc`; `npm ci` succeeded cleanly this review (330 packages, no resolution errors beyond the expected `EBADENGINE` warning). |
| Docker containers / VM builds version controlled | ✅ Met | `.devcontainer/devcontainer.json` unchanged. |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ✅ Met | `wlLog.error()`/`warn()` unchanged; extended to new `validEntry` link/note type-checks. |
| Code configuration recorded when code runs | ✅ Met | `wlLog.config()` at startup, unchanged. |
| Pipeline route recorded when decisions are made | ✅ Met | `wlLog.info` at branch points unchanged; no new decision branches this period that lack logging. |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | `CONTRIBUTING.md` unchanged. |
| Issue tracker records development tasks | ✅ Met | GitHub Issues; #275–#279 all reference their originating context. |
| New issues guided by user needs and stories | ✅ Met | Issue templates unchanged. |
| Issue templates ensure proper logging | ✅ Met | `bug_report.md`, `feature_request.md` unchanged. |
| Acceptance criteria noted and fulfilment recorded | ✅ Met | PR template checklist unchanged. |
| QA standards and processes defined | ⚠️ Partial | **Regression** — see Regressions table. `CLAUDE.md` and `QA.md` remain well-defined as documents, but two of the five automated weekly QA/process workflows (`dead-code.yml`, `delivery-metrics.yml`) have been silently non-functional for at least 9 consecutive scheduled runs, undermining the claim that these processes are actually operating as designed. This weekly QA review itself (`weekly-qa-review.yml` / this session) remains the one enforcement mechanism confirmed to work, precisely because it uses a branch+PR flow rather than a direct push to `main`. |

---

*Generated by an automated weekly QA review session. Read-only — no source files were modified.*
