# QA Review — 2026-06-08

- Commit: `cfa6b41` on `claude/fervent-bardeen-yvnlp`
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated weekly QA review)

> **Environment note:** Playwright browser binaries could not be downloaded in this execution environment
> (network restriction blocks the Chromium CDN). Smoke tests (`smoke-tests.cjs`) could not be run directly.
> The DOM selectors in `smoke-tests.cjs` have been inspected and appear consistent with the post-kanban
> and post-rolling-summary DOM structure. CI is the authoritative source for smoke-test status.

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  72   |
| ⚠️ Partial    |  11   |
| ❌ Not met    |   0   |
| ➖ N/A        |   4   |

**Total items assessed: 87**

---

## Regressions since last review (2026-06-02 / `e273b3a`)

One item dropped in status:

| Item | Was | Now | Root cause |
|------|-----|-----|------------|
| Code follows a standard style | ✅ | ⚠️ | ESLint warning count rose from 142 → 156. Net 14 new warnings in three new categories: (1) 23 `prefer-const` warnings — PRs #177 (kanban) and #179 (location toggle) introduced module-level `let` declarations (`editingPlanId`, `wipWarnDismissed`, `viewDate`, `selectedTag`, etc.) that are never reassigned in the declaring module; (2) 1 `security/detect-non-literal-regexp` in `.github/scripts/impact-check.mjs:37`; (3) 1 `no-unused-disable-directive` in `src/js/19-monthlylog.js:3` — the `eslint-disable-next-line no-var` comment is one line too early and applies to a comment line, not the `var` declaration on the line after. `npm run lint` exits 0 (0 errors). Stylelint: **0 problems** (improved from 16 `!important` warnings at last review). |

---

## Improvements since last review

Three items rose in status:

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| All functions and classes are documented | ⚠️ | ✅ | PR #170 (`fix(docs): remove invalid JSDoc @type descriptions; fix entryId? type syntax`) resolved all 2 ERRORs (`entryId?: string` TypeScript optional syntax in `18-dailylog.js` lines 3, 19) and all 10 WARNINGs (inline descriptions on `@type` tags in `06a-hero.js` and `16-rapid.js`). `npm run docs` now exits clean: **0 ERRORs, 0 WARNINGs**. |
| Auto-generated HTML documentation | ⚠️ | ✅ | Same fix (PR #170). JSDoc generates HTML for all 38 source files with no parse errors; no params silently dropped. `docs.yml` auto-deploys on push to `main`. |
| Whole process tested end-to-end | ❌ | ⚠️ | The ❌ at the last review was caused by uncommitted changes to `04-render.js`, `11-timeflow.js`, `19-monthlylog.js`, and `work-log.html` that mismatched `smoke-tests.cjs` selectors. Those changes have since been committed (PRs #177–#184). `smoke-tests.cjs` now uses the correct kanban selectors (`#planList`, `#progressList`, `#doneList`) and Today's Flow selectors (`#tfLogPane`, `#tfFlowPane`). Cannot confirm end-to-end in this environment (Playwright browsers unavailable); marking ⚠️ pending CI confirmation. |

Additional positive signals (no status change):
- **Unit tests grew**: 429 tests across 62 suites, 0 failures (`npm run test:unit`). Up from 397/57 at last review. New suites cover kanban DnD, rolling summary, location helpers, and calendar recurrence logic.
- **Stylelint**: 0 problems (was 16 `!important` warnings at last review).
- **CHANGELOG** (`## Unreleased`): fully documents all features from PRs #177–#187 (kanban, responsive layout, rolling summary, location toggle, calendar recurrence fix).

---

## Priorities for this week

1. **Fix 23 `prefer-const` warnings (low effort, high compliance signal)** — Change `let` to `const` for module-level declarations that are never reassigned in their declaring module. Affected variables: `WEATHER_LAT`, `WEATHER_LON`, `WEATHER_NAME`, `JIRA_BASE`, `AUTO_PAUSE_ON_TAB_SWITCH` in `src/js/00-config.js`; `qpHidden`, `viewDate`, `selectedTag`, `trackers`, `timerInterval`, `chartMode`, `blocks`, `planDragId` in `src/js/01-state.js`; `editingPlanId`, `_pendingCommentId`, `splitInputId`, `_pendingCommentText`, `_expandedHistoryId`, `_cpOpenIds`, `_cpEditId`, `_cpEditIdx`, `doneHistoryOpen`, `wipWarnDismissed` in `src/js/10b-tasks-events.js`. Note: if any of these are exported and reassigned by another module at runtime, they must stay `let` and be exempted with an `eslint-disable` comment explaining why.

2. **Document `25-rollingsummary.js` in `ARCHITECTURE.md`** — Module added by PR #184 but absent from the ARCHITECTURE.md module map. Add an entry describing responsibility (Rolling Summary tab in Today's Flow), key export (`renderRollingSummary()`), and its dependency on `buildRollingSummary()` in `pure-fns.js`. File is 178 lines — no split needed.

3. **Update stale line count and suite count in `ARCHITECTURE.md`** — Three annotations are stale: (a) `pure-fns.js` documented as "824 lines", actual is **939 lines** — the split-recommendation note also says "824 lines"; (b) `test/unit.mjs` documented as "57 suites", actual is **62 suites** as of this review. Update both.

4. **Update QA.md test counts** — The v1.8.9 acceptance section records "397 tests across 57 suites" (unit) and "679 total". Actual: **429 unit tests across 62 suites**. Update the unit count and the total (smoke count unverifiable in this environment; note CI as source of truth).

5. **Verify smoke tests green on CI** — The previous ❌ has been upgraded to ⚠️ based on code inspection, but needs a confirmed CI run. Check the `ci.yml` run on `main` after the latest kanban and rolling-summary merges to confirm `npm test` exits 0.

---

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | 38 JS source files using named-function functional style. No classes; appropriate for app size. |
| Code grouped into themed files/modules | ✅ Met | Numbered prefix convention (`01-state.js` … `25-rollingsummary.js`); sub-modules use letter suffixes; `pure-fns.js` and `logger.js` as leaf ESM modules. |
| Main scripts import and run high-level functions | ✅ Met | `07-lifecycle.js` `DOMContentLoaded` calls only high-level functions — no inline business logic. |
| Low-level functions each carry out one specific task | ⚠️ Partial | `11-timeblock.js` remains at **1 050 lines** (annotated as 1 050 — no change since last review). `pure-fns.js` grew from 824 → **939 lines** (PRs #179, #184 added location and rolling-summary helpers). `10b-tasks-events.js` grew from 600 → **727 lines** (PR #177 kanban DnD). `10a-tasks-render.js` grew from 597 → **682 lines**. |
| Repetition minimised | ✅ Met | `fmtDur`, `escHtml`, `qa()`, `wlLog.*`, `safeCssColor`, `buildRollingSummary` used across modules without duplication. `pure-fns.js` is the canonical shared-helpers module. |
| Open for extension, closed for modification | ✅ Met | Schema migrations in `01b-migrate.js` are append-only. Kanban column additions did not modify existing task status logic. |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `buildRollingSummary`, `locationFor`, `nextLocation`, `bindBoardColumnDnD`, `moveTaskToColumn` — new additions are self-documenting. |
| Names are explicit rather than implicit | ⚠️ Partial | Single-letter loop variables (`e`, `t`, `d`, `c`) persist in `.filter`/`.map` chains in `10a-tasks-render.js` and `05-entries.js`. Inconsistent with function-level naming quality. Unchanged since last review. |
| Code logic is clear and avoids unnecessary complexity | ⚠️ Partial | `11-timeblock.js` at 1 050 lines remains the most complex module. `pure-fns.js` at 939 lines is now the second-largest. `10b-tasks-events.js` at 727 lines (new kanban event handling). |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ⚠️ Partial | `npm run lint`: **0 errors, 156 warnings** — up from 142. New warning categories: 23 `prefer-const` (module-level `let` in `00-config.js`, `01-state.js`, `10b-tasks-events.js`), 1 `security/detect-non-literal-regexp` (`.github/scripts/impact-check.mjs:37`), 1 unused `eslint-disable` directive (`19-monthlylog.js:3`). Stylelint: **0 problems** (improved; was 16 `!important` warnings). |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | `src/` (source), `dist/` (build output), `test/` (tests), `docs/` (generated HTML + QA reports), `.github/` (CI + scripts). Build artefacts not committed. |
| Packages follow a standard structure | ✅ Met | `package.json` + `package-lock.json` committed; `main`, `scripts`, `devDependencies` correct; `.nvmrc` pins Node 24. |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | `// Assumption:` blocks in 9+ locations; temporal dead zone workaround noted in `07-lifecycle.js` and `19-monthlylog.js`. |
| Comments are up to date and not misleading | ⚠️ Partial | `ARCHITECTURE.md` has two stale annotations: `pure-fns.js` listed as "824 lines" (actual: 939); `test/unit.mjs` listed as "57 suites" (actual: 62). `25-rollingsummary.js` is absent from the module map entirely. `ARCHITECTURE.md` split recommendation still references "824 lines" — underestimates urgency. |
| Code is not commented out to control which lines run | ✅ Met | No dead code blocks found. |
| All functions and classes are documented | ✅ Met | `npm run docs` exits clean: **0 ERRORs, 0 WARNINGs**. PR #170 fixed all JSDoc issues. Improvement since last review. |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project; JSDoc is the equivalent. |
| Auto-generated HTML documentation | ✅ Met | `npm run docs` generates pages for all 38 source files with 0 errors. `docs.yml` auto-deploys on push to `main`. Improvement since last review. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`. README links to hosted docs. |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ✅ Met | Has "What it does", "Setup", "Quick start", "Build & test", documentation index. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` both present and current. |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user. |
| QA extent clearly documented | ⚠️ Partial | `QA.md` v1.8.9 acceptance section records "397 tests across 57 suites". Actual unit count: **429 across 62 suites**. Total "679 tests" figure is also stale. Smoke count unverifiable in this environment. |
| Assumptions documented next to code | ✅ Met | `// Assumption:` inline comments throughout `pure-fns.js`, `05-entries.js`, `07-lifecycle.js`. `DATA.md` documents schema assumptions. |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT). `CITATION.cff` has `license-expression: MIT`. |
| Citation instructions given | ✅ Met | `CITATION.cff` with `preferred-citation`; README "Cite this project" section. |
| Versioned releases (semantic versioning) | ✅ Met | All releases v1.0.0–v1.8.9 have annotated git tags. Significant unreleased work (kanban, responsive, rolling summary) accumulating in `## Unreleased` since v1.8.9. |
| CHANGELOG available to users | ✅ Met | `CHANGELOG.md` `## Unreleased` section fully documents PRs #177–#187. `release.yml` extracts sections for GitHub Releases. |
| Example usage documented | ✅ Met | README usage examples; JSDoc `@example` on core pure functions. |
| Design certificates confirm compliance | ✅ Met | `ARCHITECTURE.md`: `Covers app version: v1.8.9 + unreleased (main, 2026-06-03)`, `Last reviewed: 2026-06-03`, `Status: Approved`. |
| Software fully specified | ⚠️ Partial | `ARCHITECTURE.md` module map covers 37 source files but is missing `25-rollingsummary.js` (added in PR #184). `pure-fns.js` line count is stale (824 vs 939). `test/unit.mjs` suite count is stale (57 vs 62). `DATA.md` is up to date. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Git repo, 200+ commits, remote at GitHub (`lupinesse/work-log`). |
| Code committed regularly, per discrete unit of work | ✅ Met | PRs #177–#187 are single-topic squash commits with conventional-commit messages. |
| Appropriate branching strategy defined and used | ✅ Met | `CONTRIBUTING.md` "Branching Strategy" documents trunk-based flow, prefix table, squash-merge rule. |
| Code is open-sourced; sensitive data omitted | ✅ Met | MIT licence. No credentials in tracked files. `config.local.ps1` gitignored. |
| Committing standards followed | ⚠️ Partial | 4 pre-commitlint commits remain in history (pre-PR #57). All commits from PR #57 onward are conformant. Gradually self-resolving as history ages. |
| Commits tagged at significant stages | ✅ Met | All releases v1.0.0–v1.8.9 tagged. Substantial unreleased work since v1.8.9 — a new release tag is warranted when the kanban/responsive/rolling-summary bundle is considered stable. |
| Continuous integration applied | ✅ Met | 11 GitHub Actions workflows active. PR #181 replaced API-based CI scripts with static analysis to reduce cost. |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | Anthropic key server-side only; `config.local.ps1` gitignored. |
| Configuration in dedicated config file | ✅ Met | `src/js/00-config.js` for all app settings; `00-config.local.js` for personal overrides (gitignored). |
| Multiple config files per system/local/user | ✅ Met | Base (`00-config.js`) + user (`00-config.local.js`) + server (`config.local.ps1`). |
| Config files versioned separately from analysis code | ✅ Met | `00-config.js` committed independently; local overrides gitignored. |
| Config used to generate outputs is recorded | ✅ Met | `scripts/config-snapshot.cjs` captures active config at run-time. |
| Example config templates provided | ✅ Met | `00-config.local.example.js` and `config.local.example.ps1` with dummy values. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ✅ Met | `lighthouse.yml` enforces ≥85 % accessibility score on every push. |
| Analysis data stored in an open format | ✅ Met | All data in JSON (localStorage + backup export). |
| Input data stored safely and treated as read-only | ✅ Met | Backup restore writes to fresh localStorage keys; source never mutated. |
| Input data versioned; changes create new versions | ✅ Met | `01b-migrate.js` — append-only schema migration log. |
| All input data documented in a data register | ✅ Met | `DATA.md` documents all localStorage keys with origin and retention policy. |
| Outputs are disposable — regularly deleted and regenerated | ✅ Met | `script.js`, `styles.css`, `docs/` all regenerable from source; none committed. |
| Non-sensitive data available so others can run the code | ✅ Met | No personal data in repo. Self-contained test fixtures in `smoke-tests.cjs`. |
| Data quality monitored | ✅ Met | `wlLog.warn()` on every `JSON.parse` failure in all `load*()` functions. |
| Fields in datasets documented | ✅ Met | `DATA.md` has a complete field dictionary for all object types. |
| Large or complex data stored in a database | ➖ N/A | Personal tool; localStorage is appropriate for data volume. |
| Data documented in information asset register | ✅ Met | `DATA.md` lists all keys, purpose, and retention policy. |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ✅ Met | PRs #177–#187 squash-merged with ChatGPT + Claude review dialogue; thread-resolution records visible in GitHub PR history. |
| Pair programming used to review code and share knowledge | ✅ Met | `CONTRIBUTING.md` documents AI pair-programming workflow; `CITATION.cff` credits Claude Code as co-contributor. |
| Users encouraged to participate in peer review | ⚠️ Partial | Issue and PR templates invite contribution. Inherent limitation of a single-user personal tool. |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit.mjs` — **429 tests across 62 suites**, 0 failures (`npm run test:unit`). Up from 397/57 at last review. New suites cover kanban DnD (`moveTaskToColumn`), rolling summary (`buildRollingSummary`), location helpers (`locationFor`, `nextLocation`), and calendar recurrence. |
| Tests run regularly and after every significant change | ✅ Met | `ci.yml` runs `npm test` on every push. |
| Bug fixes add new unit tests so the same bug cannot recur | ✅ Met | PR #183 (calendar recurrence) and PR #180 (session times follow day in view) both include regression unit tests. |
| Informal tests recorded near the code | ✅ Met | JSDoc `@example` tags on core pure functions in `pure-fns.js`. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ Met | `QA.md` documents acceptance criteria and sign-off per release. |
| Tests run automatically via CI or git hooks | ✅ Met | `ci.yml` runs full test suite; lint-staged runs pre-commit. |
| Whole process tested end-to-end | ⚠️ Partial | Cannot confirm in this environment: Playwright browsers not downloadable (CDN blocked). Code inspection shows `smoke-tests.cjs` selectors have been updated for the new kanban DOM (`#planList`, `#progressList`, `#doneList`, `#boardCols`) and Today's Flow tabs (`#tfLogPane`, `#tfFlowPane`). The ❌ from the previous review (crash caused by uncommitted changes) should be resolved — selectors match the committed code. Requires CI confirmation. |
| Test code is clean and readable | ✅ Met | `freshPage()` helper, fixture objects, `assert()` wrapper, consistent section headers in both `unit.mjs` and `smoke-tests.cjs`. |
| Formal user-acceptance testing conducted and recorded | ✅ Met | `QA.md` documents formal UAT process and outcome per release. |
| Integration tests confirm multiple units work together | ⚠️ Partial | Smoke tests unverifiable in this environment (same constraint as above). Unit tests (429) confirm individual module behaviour. |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | `00-config.local.example.js` and `config.local.example.ps1` document all required secrets. |
| Required libraries and packages documented including versions | ✅ Met | `package-lock.json` committed; exact versions pinned. `devDependencies`: `@commitlint/cli` 21.0.2, `vite` 8.0.16, `eslint` 10.4.1, `lint-staged` 17.0.7 (unchanged since last review). |
| Working OS environments documented | ✅ Met | README documents Windows primary; `launch.sh` for Linux/Mac; `.devcontainer/devcontainer.json` for container. |
| Example configuration files provided | ✅ Met | `00-config.local.example.js`, `config.local.example.ps1` in root. |
| Code runs independently of operating system | ✅ Met | `build.js` uses `path.join()` throughout. |
| Dependencies managed separately for users, developers, testers | ✅ Met | 0 runtime dependencies; 11 devDependencies. |
| As few dependencies as possible | ✅ Met | 11 devDependencies, each justified. No new dependencies added since last review. |
| Package dependencies managed with environment manager | ✅ Met | npm + `package-lock.json` + `.nvmrc` (`24.15.0`). |
| Docker containers or VM builds exist and are version controlled | ✅ Met | `.devcontainer/devcontainer.json` at `mcr.microsoft.com/devcontainers/javascript-node:1-24-bookworm`. |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ✅ Met | `wlLog.error()` / `wlLog.warn()` on all parse failures, missing DOM elements, and API errors. |
| Code configuration recorded when code runs | ✅ Met | `wlLog.config()` in `07-lifecycle.js` logs active config at startup. |
| Pipeline route recorded when decisions are made | ✅ Met | `wlLog.info` at branch-decision points in `18-dailylog.js`, `19-monthlylog.js`, `21-reflection.js`, `20-migration.js`, `22-trackers.js`, `23-sprints.js`. |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | `CONTRIBUTING.md` defines the solo-developer + AI-pair model, decision rights, and review responsibilities. |
| Issue tracker records development tasks | ✅ Met | GitHub Issues with labels, acceptance criteria, and Projects board. PRs #177–#187 reference issue closure. |
| New issues guided by user needs and stories | ✅ Met | Issue template uses "As a user…" sections with definition of done. |
| Issue templates ensure proper logging | ✅ Met | Both issue templates require title, description, labels, and expected vs actual behaviour. |
| Acceptance criteria noted and fulfilment recorded | ✅ Met | PR template includes "Acceptance criteria" checklist; issues include "Definition of Done". |
| QA standards and processes defined | ✅ Met | `CLAUDE.md` is the operative QA standard. This automated review is the enforcement mechanism. |
