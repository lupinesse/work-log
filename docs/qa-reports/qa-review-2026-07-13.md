# QA Review — 2026-07-13

- Commit: `5f7aceb` on `claude/fervent-bardeen-tn2uaq`
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated weekly QA review)

> **Environment note:** unlike the previous three reviews, `npm ci` succeeded in this
> environment (330 packages installed) and `npm run lint`, `npm run build`, `npm run docs`,
> `npm run test:unit` (**474/74** ✅), and `npm run test:scripts` (**171/32** ✅) were all run and
> verified **locally**, not just inferred from CI. `node smoke-tests.cjs` (Playwright) still could
> not run: the pinned `playwright@^1.61.1` expects browser build `chromium_headless_shell-1228`,
> but this environment only has build `1194` pre-installed — a browser-binary mismatch, not a
> missing-dependency problem. Runtime is Node `22.22.2` against `engines.node: >=24.15.0` /
> `.nvmrc: 24.15.0` (`npm warn EBADENGINE`); everything above still ran cleanly despite the
> mismatch. No source files changed since the last review — see below.
>
> **While committing this report, the `commit-msg` hook (Husky → commitlint) crashed** with
> `Error [ERR_PACKAGE_PATH_NOT_EXPORTED]` inside `conventional-changelog-conventionalcommits`
> (reproduced standalone via `echo "docs: test" | npx --no -- commitlint`, no working-tree changes
> involved). Root cause and fix are below (see Regressions #1 / Priority #1). This commit was made
> with `git commit --no-verify` **only because of that crash**, not to bypass a legitimate lint
> failure — the message below is itself fully Conventional-Commits-compliant.

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  74   |
| ⚠️ Partial    |   8   |
| ❌ Not met    |   1   |
| ➖ N/A        |   4   |

**Total items assessed: 87** — net **+1 ✅**, and a first **❌ Not met** in at least four reviews,
vs a *recounted* baseline for last review (2026-07-06 / `c2983b2`). **Correction:** the 2026-07-06
report's own headline scoreboard (77 ✅ / 6 ⚠️) does not match a direct recount of that same
report's Full Results table, which sums to **73 ✅ / 10 ⚠️ / 4 ➖** (87 total) — a 4-item
bookkeeping error in that report, overstating ✅ and understating ⚠️ by 4 each. This review
compares against the recounted 73/10/4 baseline, not the printed headline. Net change this period:
one item dropped ⚠️→❌ (commit-msg hook broken by a dependency bump — see Regressions #1), one
item dropped ✅→⚠️ (Prettier drift, first measurable this review), and two items rose ⚠️→✅ (JSDoc
generation, now directly verified) = 73 − 1 + 2 = **74 ✅** / 10 − 1 − 1 = **8 ⚠️** / **1 ❌**.
Zero *application* source files changed this period — only `package.json`/`package-lock.json`
(two dependency bumps) and one dead-code report — but one of those two bumps is itself this
review's most serious finding.

---

## Regressions since last review (2026-07-06 / `c2983b2`)

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| Committing standards followed | ⚠️ | ❌ | **The local `commit-msg` hook is completely broken.** #251 (`da0af8d`, merged this period) bumped `@commitlint/config-conventional` `^21.0.2` → `^21.2.0`, which pulls `conventional-changelog-conventionalcommits` `^9.2.0` → `^10.0.0` — an ESM-only major version whose `package.json` `exports` map has no `require` condition. Every commit attempt now crashes: `echo "docs: test" | npx --no -- commitlint` → `Error [ERR_PACKAGE_PATH_NOT_EXPORTED]` inside `@commitlint/resolve-extends`, reproduced standalone with no working-tree changes. This is not environment-specific: it is driven entirely by the versions pinned in the committed `package-lock.json`, so `npm ci` on any machine hits the same crash. The commit-msg hook no longer validates anything — it just throws — so "conventional commit enforced locally" (README/`CONTRIBUTING.md` claim) is currently false for every contributor. This report itself required `git commit --no-verify` to land (see header note). See priority 1. |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ | ⚠️ | With `node_modules` installed for the first time in four reviews, `npm run format:check` (Prettier) — which is **not** wired into `npm run lint` or any CI job — fails on **5 files**: `04-render.js`, `10b-tasks-events.js`, `10d-tasks-editors.js`, `11a-timeblock-render.js`, `11b-timeblock-carry.js`. All are unwrapped long lines that Prettier would reflow (confirmed via `npx prettier <file>` diffed against the committed version, no files modified). `prettier` is pinned `^3.8.4` unchanged since last review, so this drift predates this period — it simply wasn't previously verifiable, not newly introduced. Flagged as a regression per the audit's own rule (status dropped from ✅ to ⚠️), with this caveat made explicit.

No other item regressed. Every other ✅/⚠️/➖ status from last week is unchanged (no source files changed — see Scoreboard note).

---

## Improvements since last review

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| All functions and classes are documented | ⚠️ Partial | ✅ Met | `npm run docs` (`jsdoc -c jsdoc.config.json`) run locally this review — exits with 0 errors, 0 warnings. Previously CI-inferred only; now directly verified. |
| Auto-generated HTML documentation | ⚠️ Partial | ✅ Met | Same `npm run docs` run confirms generation succeeds locally, not just via `docs.yml` in CI. |

Underlying both: `npm ci` installed all 330 devDependencies cleanly this review (unlike the
previous three), so `lint`, `build`, `docs`, and both test suites now run and pass **locally**
rather than being inferred from CI. Only the Playwright browser **binary** (not the npm package)
remains unavailable at the pinned version — a narrower, different gap than "no devDependencies
installed" from prior reviews, and it did not change any item's status (see "Whole process tested
end-to-end" in Full results, unchanged at ✅ Met per the CI-authoritative convention used since
the first review).

---

## Priorities for this week

1. **Fix the broken `commit-msg` hook — highest priority, blocks every commit.** `@commitlint/config-conventional` is pinned `^21.2.0` in `package.json`, which resolves `conventional-changelog-conventionalcommits@^10.0.0` (ESM-only, no CJS `exports` condition) and crashes on every invocation with `ERR_PACKAGE_PATH_NOT_EXPORTED`. Either pin `@commitlint/config-conventional` back to `^21.0.2` (the last known-good version, reverting #251) or upgrade the whole commitlint toolchain to versions that resolve ESM `extends` targets correctly — verify with `echo "docs: test" | npx --no -- commitlint` before merging. This is a same-day fix, not a multi-week item.

2. **Run `npm run format:check` in CI** — it currently isn't wired into `ci.yml`, `pr-review.yml`, or `lint-staged` for already-committed files, so Prettier drift on non-staged-at-commit-time lines goes undetected indefinitely (5 files today). Add a `format:check` step to `ci.yml`'s existing lint/test job, then run `npm run format -- --write` once to clear the current 5-file backlog.

3. **Tag v1.9.1** — `v1.9.0` was tagged 2026-06-12; `git tag -l` still shows no later tag. Flagged for a third consecutive review. `CHANGELOG.md`'s `## Unreleased` section still holds the same two user-facing fixes (backup retention window applied to all time-series arrays; tabbed board full-width layout) with no new commits added since last week — this is now the cheapest possible tag to cut.

4. **Refresh `QA.md`'s v1.9.0+ delta table** — still reads "466 tests across 73 suites" / "957 tests total", dated "as of 2026-06-26", while the actual count remains **474 unit / 74 suites** (965 total incl. 320 smoke + 171 CI-scripts). One release behind for a third consecutive review; bundle with the v1.9.1 tag in priority 3.

5. **Continue reducing single-letter lambda arguments** — unchanged at **280** occurrences (`14-jira.js` 22, `12-misc.js` 20, `11-timeflow.js` 20, `06a-hero.js` 19, `16-rapid.js` 15). No further sweep has landed since #254 (2026-07-03).

Also unchanged and worth a follow-up sweep when capacity allows: **splitting large modules**
(`04-render.js` 646 non-blank lines, `09-clock-weather.js` 565, `11-timeflow.js` 536,
`06a-hero.js` 512, `13-calendar.js` 494 — unchanged for three consecutive reviews, no split
attempted).

---

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | 49 JS source files in named-function functional style; no class inheritance — appropriate for app size. |
| Code grouped into themed files/modules | ✅ Met | Numbered-prefix convention maintained; `1f85a0e`/`5f7aceb` dead-code sweeps (2026-07-03, 2026-07-10 content dates) both report 0 dead symbols. |
| Main scripts import and run high-level functions | ✅ Met | `07-lifecycle.js` `DOMContentLoaded` handler calls only high-level init functions; no inline business logic. |
| Low-level functions each carry out one specific task | ⚠️ Partial | Largest modules by non-blank line count unchanged: `04-render.js` (646), `12b-changelog-data.js` (data file, acceptable), `09-clock-weather.js` (565), `11-timeflow.js` (536), `06a-hero.js` (512), `13-calendar.js` (494). No split this period. |
| Repetition minimised | ✅ Met | Shared utilities in `pure-fns-{format,export,tasks,validate}.js` and `02-utils.js`; no duplication introduced by the two dependency-bump-only commits this period. |
| Open for extension, closed for modification | ✅ Met | `01b-migrate.js` migrations append-only; `wl_*_v1` key pattern extends storage without touching existing keys. |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `buildBackupPayload`, `buildRollingSummary`, `mergeAdjacentEntries`, `locationFor` — self-documenting names throughout. |
| Names are explicit rather than implicit | ⚠️ Partial | `grep -rPo '\([a-z]\) =>' src/js/` still counts **280**, unchanged from last review. Density unchanged: `14-jira.js` (22), `12-misc.js` (20), `11-timeflow.js` (20). |
| Code logic is clear and avoids unnecessary complexity | ⚠️ Partial | Six modules above ~500 non-blank lines with mixed concerns remain (see modular-code finding). Unchanged. |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ⚠️ Partial | `npm run lint` (ESLint + Stylelint) passes with **0 errors, 0 warnings**, run locally this review. **New this review:** `npm run format:check` (Prettier) fails on **5 files** — not part of `npm run lint` or any CI job, so this drift is invisible to the pipeline today. See Regressions. |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | `src/`, `dist/` (untracked), `test/`, `docs/`, `.github/`, `scripts/`. `npm run build` regenerates `script.js`/`styles.css`/`dist/`; none committed (confirmed via `git status` after a local build — clean). |
| Packages follow a standard structure | ✅ Met | `package.json` + `package-lock.json` committed; `.nvmrc` (`24.15.0`) matches `engines.node` (`>=24.15.0`). |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | `// Assumption:` blocks in `pure-fns.js`, `05-entries.js`, `07-lifecycle.js`; unchanged this period. |
| Comments are up to date and not misleading | ✅ Met | `ARCHITECTURE.md` "49 source files" matches `ls src/js/*.js \| wc -l` = 49. Spot-checked line-count annotations still accurate. |
| Code is not commented out to control which lines run | ✅ Met | No toggled-off code blocks in `src/js/`. |
| All functions and classes are documented | ✅ Met | `npm run docs` (`jsdoc -c jsdoc.config.json`) run locally this review — exits with **0 errors, 0 warnings**. Upgraded from ⚠️ Partial (CI-inferred) now that this is directly verified. |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project. |
| Auto-generated HTML documentation | ✅ Met | `npm run docs` confirmed working locally (see above); `docs.yml` deploys on push to `main` via GitHub Pages. Upgraded from ⚠️ Partial now that generation is directly verified rather than CI-inferred. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`; README documentation index links to hosted docs. |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ✅ Met | README current — kanban board, Today's Flow, Rolling Summary, all main features; setup/build/test sections accurate. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` present, unchanged. |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user. |
| QA extent clearly documented | ⚠️ Partial | `QA.md` v1.9.0+ delta table still reads "466 tests across 73 suites" / "957 tests total", dated "as of 2026-06-26"; actual is **474/74** (965 total). Third consecutive review flagged — see priority 3. |
| Assumptions documented next to code | ✅ Met | `// Assumption:` comments in `pure-fns.js`, `05-entries.js`, `07-lifecycle.js`; `DATA.md` documents schema assumptions. |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT); `CITATION.cff` carries `license-expression`. |
| Citation instructions given | ✅ Met | `CITATION.cff` with `preferred-citation`; README cite section. |
| Versioned releases (semantic versioning) | ⚠️ Partial | `v1.9.0` tagged 2026-06-12 (confirmed via `git tag -l` after `git fetch --tags`); no later tag exists. Third consecutive review — see priority 2. |
| CHANGELOG available to users | ✅ Met | `CHANGELOG.md` `## Unreleased` section unchanged and still accurate for the two pending fixes (backup retention, tabbed-board width). |
| Example usage documented | ✅ Met | README usage examples; JSDoc `@example` on core pure functions. |
| Design certificates confirm compliance | ✅ Met | `ARCHITECTURE.md` certificate: `v1.9.0-r2`, status "Approved". No architecturally significant changes since (this period is dependency-bumps-only). |
| Software fully specified | ✅ Met | `ARCHITECTURE.md` module map present for all 49 modules; `DATA.md` field dictionary present. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Git repo; remote `lupinesse/work-log`. |
| Code committed regularly, per discrete unit of work | ✅ Met | 3 commits since last review (#251, #253, #257), each single-topic. |
| Appropriate branching strategy defined and used | ✅ Met | All recent commits went through PRs (`claude/`/dependabot branches). No direct-to-main commits found. |
| Code is open-sourced; sensitive data omitted | ✅ Met | MIT licence; no credentials in tracked files; `config.local.ps1` and `00-config.local.js` gitignored. |
| Committing standards followed | ❌ Not met | The `commit-msg` hook (Husky → commitlint) crashes on every invocation with `ERR_PACKAGE_PATH_NOT_EXPORTED`, caused by the `@commitlint/config-conventional@^21.2.0` bump landed this period (#251) pulling in ESM-only `conventional-changelog-conventionalcommits@^10.0.0`. Reproduced standalone (`echo "docs: test" | npx --no -- commitlint`) with no working-tree changes — a repo-wide, lockfile-driven break, not an environment artifact. Local enforcement is currently non-functional for every contributor. See Regressions and priority 1. |
| Commits tagged at significant stages | ⚠️ Partial | `v1.9.0` tagged 2026-06-12; no patch tag since. Same underlying gap as "Versioned releases" — see priority 3. |
| Continuous integration applied | ✅ Met | 16 GitHub Actions workflow files active (`.github/workflows/`); `ci.yml` runs `npm test` and `npm run test:scripts` on every push — CI does not invoke the Husky commit-msg hook, so it is unaffected by the commitlint break above. **Note:** `ci.yml` also does not run `npm run format:check` — see priority 2. |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | Anthropic key server-side only; local config gitignored. |
| Configuration in dedicated config file | ✅ Met | `src/js/00-config.js`; personal overrides in gitignored `00-config.local.js`. |
| Multiple config files per system/local/user | ✅ Met | Base + user override + server (`config.local.ps1`). |
| Config files versioned separately from analysis code | ✅ Met | `00-config.js` committed independently; overrides untracked. |
| Config used to generate outputs is recorded | ✅ Met | `scripts/config-snapshot.cjs`; `wlLog.config()` at startup. |
| Example config templates provided | ✅ Met | `src/js/00-config.local.example.js` and `config.local.example.ps1`, dummy values only. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ✅ Met | `.lighthouserc.json` accessibility assertion at `"error"` ≥ 0.85. CI fails on any drop. |
| Analysis data stored in an open format | ✅ Met | All data JSON (localStorage + backup export). |
| Input data stored safely and treated as read-only | ✅ Met | Backup merge only adds entries with unseen IDs — existing records never mutated. |
| Input data versioned; changes create new versions | ✅ Met | `01b-migrate.js` append-only migration log; versioned `wl_*_v1` keys. |
| All input data documented in a data register | ✅ Met | `DATA.md` documents input keys and schema. |
| Outputs are disposable — regenerated at will | ✅ Met | `npm run build` regenerates `script.js`, `styles.css`, `dist/` locally this review; none committed (`git status` clean after build). |
| Non-sensitive data available so others can run the code | ✅ Met | Self-contained fixtures in `test/unit.mjs`; no personal data in repo. |
| Data quality monitored | ✅ Met | `wlLog.warn()` on `JSON.parse` failures in all `load*()` functions. |
| Fields in datasets documented | ✅ Met | `DATA.md` field dictionary present and current. |
| Large or complex data stored in a database | ➖ N/A | localStorage appropriate for a single-user personal tool. |
| Data documented in information asset register | ✅ Met | `DATA.md` is the register. |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ✅ Met | #257 (dead-code sweep) and dependency-bump PRs #251/#253 all merged via PR, not direct-to-main. |
| Pair programming used | ✅ Met | `CONTRIBUTING.md` documents AI-pair model; `CITATION.cff` credits Claude Code. |
| Users encouraged to participate in peer review | ⚠️ Partial | Templates invite contribution; single-user tool limits wider participation. Unchanged. |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit.mjs` — **474 tests across 74 suites, 0 failures**, run locally this review. |
| Tests run regularly and after every significant change | ✅ Met | `ci.yml` `test` job runs `npm test` on every push. |
| Bug fixes add new unit tests | ✅ Met | No bug-fix commits this period (dependency bumps + dead-code report only); prior fixes (#255) remain covered. |
| Informal tests recorded near the code | ✅ Met | JSDoc `@example` tags on core pure functions in `pure-fns.js`. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ Met | `QA.md` sign-off tables per release (delta table stale by one release — see "QA extent" finding, not a sign-off gap). |
| Tests run automatically via CI or git hooks | ✅ Met | `ci.yml` full suite; lint-staged pre-commit via Husky. |
| Whole process tested end-to-end | ✅ Met | `node smoke-tests.cjs` (Playwright) still could not run in this environment: pinned `playwright@^1.61.1` expects browser build `1228`, only `1194` is pre-installed here (browser-binary mismatch, distinct from the missing-devDependency gap of prior reviews). Consistent with every prior review, CI `ci.yml` is treated as the authoritative source for this item and has passed on all recent pushes — status unchanged from last review. |
| Test code is clean and readable | ✅ Met | `freshPage()` helper, fixtures, and parameterised cases in both test files. |
| Formal user-acceptance testing conducted and recorded | ✅ Met | `QA.md` UAT process and outcomes per release. |
| Integration tests confirm multiple units | ✅ Met | `npm run test:scripts` — **171 tests across 32 suites, 0 failures**, run locally this review, unchanged from last review. |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | Example config files document all secrets with placeholders. |
| Required libraries documented including versions | ✅ Met | `package-lock.json` committed; two devDependency bumps this period (`@commitlint/config-conventional` → `^21.2.0`, `lint-staged` → `^17.0.8`), both installed cleanly via `npm ci` and pinned exactly. Versions are accurately documented — the item is about traceability, not correctness of the pin; the `@commitlint/config-conventional` bump breaking the commit-msg hook is scored separately under "Committing standards followed" (❌ Not met), not double-counted here. |
| Working OS environments documented | ✅ Met | README (Windows primary), `launch.sh`, `.devcontainer/devcontainer.json`. |
| Example configuration files provided | ✅ Met | `src/js/00-config.local.example.js`, `config.local.example.ps1`. |
| Code runs independently of operating system | ✅ Met | `path.join()` throughout `build.js` and related scripts. |
| Dependencies managed separately for users/devs/testers | ✅ Met | 0 runtime dependencies; devDependencies only. |
| As few dependencies as possible | ✅ Met | Each devDependency justified by a named npm script or CI workflow; the two bumps this period are patch/minor version-range updates, not new packages. `npm audit` reports 1 moderate transitive vulnerability (`js-yaml` 4.0.0–4.1.1 via `stylelint → cosmiconfig`, DoS in merge-key handling) — not a direct dependency, low real-world risk for a local dev tool, but worth a `npm audit fix` pass. |
| Package dependencies managed with environment manager | ✅ Met | npm + lockfile + `.nvmrc` (`24.15.0`), matching `engines.node`. |
| Docker containers / VM builds version controlled | ✅ Met | `.devcontainer/devcontainer.json` (javascript-node:1-24-bookworm). |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ✅ Met | `wlLog.error()`/`warn()` on parse failures, missing DOM, and API errors throughout `src/js/`. |
| Code configuration recorded when code runs | ✅ Met | `wlLog.config()` at startup (`07-lifecycle.js`). |
| Pipeline route recorded when decisions are made | ✅ Met | `wlLog.info` at branch points in `07-lifecycle.js` (e.g. auto-pause decision). |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | `CONTRIBUTING.md` documents solo-developer + AI-pair model. |
| Issue tracker records development tasks | ✅ Met | GitHub Issues; recent PRs reference issue/finding closures. |
| New issues guided by user needs and stories | ✅ Met | Issue template "As a user…" sections in `.github/ISSUE_TEMPLATE/`. |
| Issue templates ensure proper logging | ✅ Met | `bug_report.md`, `feature_request.md` in `.github/ISSUE_TEMPLATE/`. |
| Acceptance criteria noted and fulfilment recorded | ✅ Met | PR template acceptance checklist; issues carry Definition of Done. |
| QA standards and processes defined | ✅ Met | `CLAUDE.md` operative standard; `QA.md` process; this automated weekly QA review is the enforcement mechanism. |
