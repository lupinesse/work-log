# QA Review — 2026-06-26

- Commit: `5c3c4e7` on `main`
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated QA review)

> **Environment note:** `jsdoc` and `playwright` are available locally. `npm run lint`
> (0 errors, 1 warning), `npm test` (466 unit + 320 smoke + 30 CI-scripts = 816 via
> `npm test`; 171 across all 8 CI-script files via `npm run test:scripts` = 957 total),
> and `npm run build` all executed successfully. Items marked ⚠️ where CI is the only
> authoritative source are noted in evidence.

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  74   |
| ⚠️ Partial    |   9   |
| ❌ Not met    |   0   |
| ➖ N/A        |   4   |

**Total items assessed: 87** — net −1 ✅ vs last review (2026-06-22), driven by one new regression.

---

## Regressions since last review (2026-06-22 / `6f0cbcf`)

| Item | Was | Now | Root cause |
|------|-----|-----|------------|
| Comments are up to date and not misleading | ✅ | ⚠️ | `ARCHITECTURE.md` line-count annotations drifted with subsequent PRs (#232–#245). Overview still reads "39 source files across 30+ numbered modules"; actual count is **50 files**. Module annotations: `04-render.js` shows 690 (actual 646), `09-clock-weather.js` shows 608 (actual 565), `11-timeflow.js` shows 606 (actual 536), `06a-hero.js` shows 596 (actual 512). Fixed in #220+#223 but not maintained since. |

---

## Improvements since last review

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ (135 warnings) | ✅ (1 warning) | #242 cleared all 146 ESLint warnings (config relaxations + dead-code removals). Current: 0 errors, 1 warning (`prefer-const` on `_emojiPickerPid` in `10a-tasks-render.js:306`). |
| Dead top-level symbols removed | ✅ | ✅ (cleaner) | #243 removed 4 confirmed-dead symbols: `DAILY_GOAL_MS`, `DEFAULT_IDS`, `planDragId`, `getHandoffNote`. Dead-code report committed `docs/dead-code-reports/dead-code-2026-06-23.md`; 482 symbols analysed, 0 remaining dead. |
| Dead CSS cleared | ✅ | ✅ (cleaner) | #244 removed TIME LOG chrome + old Log-view CSS; #245 removed ~20 more dead classes. Report in `docs/dead-code-reports/dead-css-2026-06-23.md`. |
| Server-helper tests hit real code | ⚠️ (wrappers) | ✅ | #239–#241 extracted `Get-YearAnchor`, `Test-NewComRef`, `Test-DebugQuery` so Pester tests exercise real logic rather than thin wrappers. |
| Names explicit rather than implicit | ⚠️ (431 instances) | ⚠️ (232 instances) | Single-letter lambda arguments down ~46% (431 → 232) across `src/js/`. Still ⚠️ but meaningfully improved. |
| Unit tests | 461 / 72 suites | 466 / 73 suites | 5 new unit tests added across the PRs since last review. |
| Smoke tests | ~317 | 320 passed | 3 additional Playwright scenarios (tabbed board full-width regression in #238). |

---

## Priorities for this week

1. **Update `QA.md` for current counts** — still shows v1.8.9 figures (429 unit + 282 smoke = 711 total). Actual: **466 unit / 73 suites** + **320 smoke / 43 sections** + **171 CI-scripts / 32 suites** = **957 tests total**. Add a v1.9.x acceptance delta row. Closes the regression carried from the 2026-06-22 review.

2. **Refresh `ARCHITECTURE.md` line counts and overview** — the overview section reads "39 source files" but there are 50. Module-level annotations are stale for at least four modules (see Regressions). A pass updating the counts (and noting #243's symbol removals) would restore the ✅ for "Comments up to date".

3. **Fix the one remaining ESLint warning** — `prefer-const` on `_emojiPickerPid` in `src/js/10a-tasks-render.js:306`. Add `eslint-disable-next-line prefer-const` with a comment explaining the variable is reassigned in `11-timeblock.js` (cross-file global in the concat build model — changing to `const` would break the build). Eliminates the last warning and makes `npm run lint` fully clean.

4. **Continue reducing single-letter lambda args** — 232 instances remain across `src/js/`. High-priority files: `04-render.js`, `06a-hero.js`, `10b-tasks-events.js`. Even a focused pass over the three largest files would cut the count materially.

5. **Split or further reduce large modules** — `04-render.js` (646 lines) and `09-clock-weather.js` (565 lines) remain above the comfortable threshold. Even a thematic `04a-` sub-module for the section-rendering helpers would help; `09-clock-weather.js` could split weather/rain fetch from the clock/header update loop.

---

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | 50 JS source files in named-function functional style; no class inheritance — appropriate for app size. |
| Code grouped into themed files/modules | ✅ Met | Numbered-prefix convention maintained. Dead-code sweep (#243) and CSS sweeps (#244, #245) kept module concerns tidy. |
| Main scripts import and run high-level functions | ✅ Met | `07-lifecycle.js` `DOMContentLoaded` handler calls only high-level init functions; no inline business logic. |
| Low-level functions each carry out one specific task | ⚠️ Partial | Largest modules by actual line count: `04-render.js` (646), `12b-changelog-data.js` (631 — data only, acceptable), `09-clock-weather.js` (565), `11-timeflow.js` (536), `06a-hero.js` (512), `03-timer.js` (494). Improved from last review's 686/633/608 top-3 but two remain above 500 with mixed concerns. |
| Repetition minimised | ✅ Met | Shared utilities in four `pure-fns-*.js` leaf modules and `02-utils.js`; `wlLog` and `escHtml` used without duplication. |
| Open for extension, closed for modification | ✅ Met | `01b-migrate.js` migrations append-only; `wl_*_v1` key pattern extends storage without touching existing keys. |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `buildRollingSummary`, `mergeAdjacentEntries`, `locationFor`, `parseRapidTokens`, `Get-YearAnchor` — self-documenting names throughout. |
| Names are explicit rather than implicit | ⚠️ Partial | Single-letter lambda arguments (`(e) =>`, `(t) =>`, `(d) =>`) reduced from 431 to **232** instances across `src/js/` (~46% improvement). Still prevalent in `04-render.js`, `06a-hero.js`, `10b-tasks-events.js`. One residual `.catch((e) =>` in `12-misc.js`. |
| Code logic is clear and avoids unnecessary complexity | ⚠️ Partial | Two modules above 500 lines with mixed concerns remain (see modular-code finding). `06a-hero.js` (512) state machine logic is still complex. |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ Met | `npm run lint` exits with **0 errors, 1 warning** (`prefer-const` on `_emojiPickerPid` at `10a-tasks-render.js:306`). #242 cleared all 146 prior warnings. Stylelint: 0 issues. |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | `src/`, `dist/` (untracked), `test/`, `docs/`, `.github/`, `scripts/`. Build artefacts not committed; `npm run build` regenerates cleanly. |
| Packages follow a standard structure | ✅ Met | `package.json` + `package-lock.json` committed. `.nvmrc` pins `24.15.0`. Minor: `engines.node` still says `>=22.12.0` while `.nvmrc` specifies `24` — harmless mismatch, unchanged from previous reviews. |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | `// Assumption:` blocks in `pure-fns.js`, `05-entries.js`, `07-lifecycle.js`; `24-location.js` rationale comment intact. |
| Comments are up to date and not misleading | ⚠️ Partial | `ARCHITECTURE.md` line-count annotations are stale for at least four modules (see Regressions). Overview section reads "39 source files" but actual count is 50. Was ✅ after #220+#223 (2026-06-13) but drifted across the 18 subsequent PRs. |
| Code is not commented out to control which lines run | ✅ Met | No toggled-off code blocks found in `src/js/`. |
| All functions and classes are documented | ⚠️ Partial | `jsdoc` command unavailable locally in this run; rely on CI `jsdoc-check.yml` job. Last confirmed: #213 fixed the 8 TypeScript-optional-syntax ERRORs; new split modules (`pure-fns-export.js` etc.) have `@file` JSDoc. Cannot independently confirm 0 JSDoc errors at `HEAD`. |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project. |
| Auto-generated HTML documentation | ⚠️ Partial | `docs.yml` deploys on push to `main` via GitHub Pages. Local `npm run docs` not verified in this run. CI is the source of truth; all PRs since last review passed `docs.yml`. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`; README documentation index links to hosted docs. |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ✅ Met | README current — covers kanban board, Today's Flow, Rolling Summary, all main features; setup/build/test sections accurate. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` present. |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user. |
| QA extent clearly documented | ⚠️ Partial | `QA.md` still shows v1.8.9 figures (429 unit + 282 smoke = 711 total). Actual today: **466 unit / 73 suites** + **320 smoke / 43 sections** + **171 CI-scripts / 32 suites** = **957 total tests**. Not updated since the last review raised this finding. **Regression carried from 2026-06-22.** |
| Assumptions documented next to code | ✅ Met | `// Assumption:` comments in `pure-fns.js`, `05-entries.js`, `07-lifecycle.js`; `DATA.md` documents schema assumptions for every key. |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT); `CITATION.cff` carries `license-expression`. |
| Citation instructions given | ✅ Met | `CITATION.cff` with `preferred-citation`; README cite section. |
| Versioned releases (semantic versioning) | ✅ Met | `v1.9.0` tagged and released. Unreleased section in `CHANGELOG.md` has one entry for the #238 tabbed-board fix (pending next release). |
| CHANGELOG available to users | ✅ Met | `CHANGELOG.md` current; Unreleased section has one entry (tabbed board full-width fix from #238). |
| Example usage documented | ✅ Met | README usage examples; JSDoc `@example` on core pure functions. |
| Design certificates confirm compliance | ✅ Met | `ARCHITECTURE.md` certificate: `v1.9.0-r1`, last reviewed 2026-06-13, status "Approved". Line counts in body are stale (see Code documentation finding) but certificate block itself is unchanged. |
| Software fully specified | ✅ Met | `ARCHITECTURE.md` covers all modules; new sub-modules documented. Overview file count is stale (39 vs 50), but individual module sections are present. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Git repo; 245+ commits; remote `lupinesse/work-log`. |
| Code committed regularly, per discrete unit of work | ✅ Met | 18 commits since last review (#227–#245), all single-topic squash commits. |
| Appropriate branching strategy defined and used | ✅ Met | All 18 commits since last review went through PRs. No direct-to-main commits. |
| Code is open-sourced; sensitive data omitted | ✅ Met | MIT licence; no credentials in tracked files; `config.local.ps1` and `00-config.local.js` gitignored. |
| Committing standards followed | ⚠️ Partial | All recent commits follow conventional-commit convention enforced by commitlint + Husky. Pre-commitlint history remains but all new commits (#227–#245) are conformant. (Unchanged.) |
| Commits tagged at significant stages | ✅ Met | `v1.9.0` tagged. Unreleased work accumulated since 2026-06-12; a `v1.9.1` or patch tag would be appropriate once accumulated fixes are released. |
| Continuous integration applied | ✅ Met | 17 GitHub Actions workflows active; `ci.yml` has two jobs: `test` (`npm test`) and `scripts-test` (`npm run test:scripts`). Both run on every push. |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | Anthropic key server-side only; local config gitignored. Dead symbol `DAILY_GOAL_MS` removed (#243) had no credential implications. |
| Configuration in dedicated config file | ✅ Met | `src/js/00-config.js`; personal overrides in gitignored `00-config.local.js`. |
| Multiple config files per system/local/user | ✅ Met | Base + user override + server (`config.local.ps1`). |
| Config files versioned separately from analysis code | ✅ Met | `00-config.js` committed independently; overrides untracked. |
| Config used to generate outputs is recorded | ✅ Met | `scripts/config-snapshot.cjs`; `wlLog.config()` at startup. |
| Example config templates provided | ✅ Met | `src/js/00-config.local.example.js` and `config.local.example.ps1`, dummy values only. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ✅ Met | `.lighthouserc.json` accessibility assertion at `"error"` ≥ 0.85. CI fails on any drop. |
| Analysis data stored in an open format | ✅ Met | All data JSON (localStorage + backup export). |
| Input data stored safely and treated as read-only | ✅ Met | Backup merge only adds entries with unseen IDs — existing records never mutated. |
| Input data versioned; changes create new versions | ✅ Met | `01b-migrate.js` append-only migration log; versioned `wl_*_v1` keys. |
| All input data documented in a data register | ✅ Met | `DATA.md` documents all 32 keys. |
| Outputs are disposable — regenerated at will | ✅ Met | `npm run build` regenerates `script.js`, `styles.css`, `dist/`; none committed. |
| Non-sensitive data available so others can run the code | ✅ Met | Self-contained fixtures in `test/unit.mjs`; no personal data in repo. |
| Data quality monitored | ✅ Met | `wlLog.warn()` on `JSON.parse` failures in all `load*()` functions. |
| Fields in datasets documented | ✅ Met | `DATA.md` field dictionary complete for all 32 keys. |
| Large or complex data stored in a database | ➖ N/A | localStorage appropriate for a single-user personal tool. |
| Data documented in information asset register | ✅ Met | `DATA.md` is the register; complete. |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ✅ Met | PRs #227–#245 merged through ChatGPT + Claude four-phase dialogue with merge-gate. |
| Pair programming used | ✅ Met | `CONTRIBUTING.md` documents AI-pair model; `CITATION.cff` credits Claude Code. |
| Users encouraged to participate in peer review | ⚠️ Partial | Templates invite contribution; single-user tool limits wider participation. (Unchanged.) |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit.mjs` — **466 tests across 73 suites, 0 failures** (up from 461/72 at last review). |
| Tests run regularly and after every significant change | ✅ Met | `ci.yml` `test` job runs `npm test` on every push. |
| Bug fixes add new unit tests | ✅ Met | #238 tabbed-board regression shipped with Playwright smoke test; #232 carry-guard fix has guard verified. |
| Informal tests recorded near the code | ✅ Met | JSDoc `@example` tags on core pure functions. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ Met | `QA.md` sign-off tables per release. (Counts stale — see "QA extent" finding.) |
| Tests run automatically via CI or git hooks | ✅ Met | `ci.yml` full suite; lint-staged pre-commit via Husky. |
| Whole process tested end-to-end | ✅ Met | `smoke-tests.cjs` — **320 passed, 0 failed** (up from ~317 at last review). |
| Test code is clean and readable | ✅ Met | `freshPage()` helper, fixtures, and parameterised cases in both test files. |
| Formal user-acceptance testing conducted and recorded | ✅ Met | `QA.md` UAT process and outcomes per release. |
| Integration tests confirm multiple units | ✅ Met | `npm run test:scripts` runs all 8 CI-script test files — **171 tests across 32 suites, 0 failures**. Server-helper tests now exercise real code after #239–#241 refactors. Note: `npm test` only invokes `ci-scripts.test.mjs` (30/5); the remaining 141 tests run via the separate `scripts-test` CI job (`npm run test:scripts`). |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | Example config files document all secrets with placeholders. |
| Required libraries documented including versions | ✅ Met | `package-lock.json` committed; exact version pins. Bumped since last review: `eslint-plugin-security` 4.0.0→4.0.1 (#227), `prettier` 3.8.3→3.8.4 (#230), `sass` 1.100.0→1.101.0 (#228), `eslint` 10.4.1→10.5.0 (#229), `playwright` 1.60.0→1.61.0 (#235), `actions/checkout` v6→v7 (#234). |
| Working OS environments documented | ✅ Met | README (Windows primary), `launch.sh`, `.devcontainer/devcontainer.json`. |
| Example configuration files provided | ✅ Met | `src/js/00-config.local.example.js`, `config.local.example.ps1`. |
| Code runs independently of operating system | ✅ Met | `path.join()` throughout `build.js` and related scripts. |
| Dependencies managed separately for users/devs/testers | ✅ Met | 0 runtime dependencies; 15 devDependencies. |
| As few dependencies as possible | ✅ Met | 15 devDependencies; each justified by a named npm script or CI workflow. |
| Package dependencies managed with environment manager | ✅ Met | npm + lockfile + `.nvmrc` (24.15.0). Minor mismatch: `engines.node` says `>=22.12.0`. (Unchanged from previous reviews.) |
| Docker containers / VM builds version controlled | ✅ Met | `.devcontainer/devcontainer.json` (javascript-node:1-24-bookworm). |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ✅ Met | `wlLog.error()`/`warn()` on parse failures, missing DOM, API errors. `[OutputType([bool])]` declared on shared PowerShell helpers (#240). |
| Code configuration recorded when code runs | ✅ Met | `wlLog.config()` at startup (`07-lifecycle.js`). |
| Pipeline route recorded when decisions are made | ✅ Met | `wlLog.info` at branch points (e.g. auto-pause decision in `07-lifecycle.js`). |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | `CONTRIBUTING.md` documents solo-developer + AI-pair model. |
| Issue tracker records development tasks | ✅ Met | GitHub Issues; PRs #227–#245 reference issue closures. |
| New issues guided by user needs and stories | ✅ Met | Issue template "As a user…" sections in `.github/ISSUE_TEMPLATE/`. |
| Issue templates ensure proper logging | ✅ Met | `bug_report.md`, `feature_request.md` in `.github/ISSUE_TEMPLATE/`. |
| Acceptance criteria noted and fulfilment recorded | ✅ Met | PR template acceptance checklist; issues carry Definition of Done. |
| QA standards and processes defined | ✅ Met | `CLAUDE.md` operative standard; `QA.md` process; this automated QA review is the enforcement mechanism. |
