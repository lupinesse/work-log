# QA Review — 2026-06-12

- Commit: `5381010` on `main`
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated weekly QA review)

> **Note:** The audit began at `db50c30` (#205). During the audit a local commit
> `5381010 test(logger): add unit tests for wlLog` (dated 2026-06-12, +115 lines in
> `test/unit.mjs`) appeared on `main` from outside this session. It is unpushed and did
> not go through the PR workflow — see the branching-strategy finding below.
> Also noted: commit `4cd5ecf` (#196) references a "2026-06-08 QA review", but no
> 2026-06-08 report exists in `docs/qa-reports/` — that review's evidence was not committed.

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  67   |
| ⚠️ Partial    |  16   |
| ❌ Not met    |   0   |
| ➖ N/A        |   4   |

**Total items assessed: 87**

---

## Regressions since last review (2026-06-02 / `e273b3a`)

Seven items dropped in status (three share one root cause):

| Item | Was | Now | Root cause |
|------|-----|-----|------------|
| Design certificates confirm compliance | ✅ | ⚠️ | `ARCHITECTURE.md` certificate reads `v1.8.9 + unreleased (main, 2026-06-03)` — nine days and ~20 merged PRs stale. It predates the tabbed board (#201), Rolling Summary (#184), location toggle, task notes (#194), and backup merge (#192/#203). |
| Appropriate branching strategy used | ✅ | ⚠️ | Local commit `5381010` (2026-06-12) was made directly on `main`, bypassing the mandatory PR workflow in `CLAUDE.md`. It is unpushed, so it can still be moved to a branch and PR'd. |
| Commits tagged at significant stages | ✅ | ⚠️ | Last tag is `v1.8.9` (2026-05-28). Since then ~35 PRs merged, including major features (kanban board #177/#201, Rolling Summary #184, backup merge #192, 21-day backup window #203). The `Unreleased` section of `CHANGELOG.md` now spans two weeks of work with no release cut. |
| Published outputs meet accessibility regulations | ✅ | ⚠️ | **Scoring correction, not a code change:** `.lighthouserc.json` sets all assertions to `"warn"` — the build never fails on an accessibility drop. The 2026-06-02 review's claim that lighthouse "enforces ≥85 %" was over-optimistic; it only warns. |
| All input data documented in a data register | ✅ | ⚠️ | `wl_location_v1` (per-day Remote/Office map, written by `src/js/24-location.js`) is absent from `DATA.md`. 31 other keys are documented. |
| Fields documented in a data dictionary | ✅ | ⚠️ | Same root cause — the location map's field shape (`{ 'YYYY-MM-DD': 'remote'\|'office' }`) is undocumented. |
| Data documented in information asset register | ✅ | ⚠️ | Same root cause — `DATA.md` is the asset register and omits the location store. |

Also worsened within an unchanged ⚠️ status: **JSDoc ERRORs rose from 2 to 8.** The old
`18-dailylog.js` errors and the 10 `@type` warnings were fixed (#196), but the same
TypeScript-optional-syntax mistake (`note?: string` inside `@param`) was reintroduced in
`10a-tasks-render.js` (lines 51, 57, 62, 69), `11-timeflow.js` (lines 326, 333), and
`pure-fns.js` (lines 940, 955). `npm run docs` exits 1; the affected params are dropped
from the generated HTML. The `jsdoc-check.yml` workflow checks JSDoc *presence*, not
parse validity, so these passed CI.

## Improvements since last review

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| Whole process tested end-to-end | ❌ | ✅ | `smoke-tests.cjs`: **317 passed, 0 failed**. The "Daily Log" crash from 2026-06-02 was fixed when the Today's Flow branch landed. `npm test` exits 0. |
| Integration tests confirm multiple units | ⚠️ | ✅ | Smoke suite runs to completion across all 43+ sections; CI-script tests (30) also pass. |
| QA extent clearly documented | ⚠️ | ✅ | `QA.md` updated 2026-06-08 with current figures (429 unit / 62 suites + 282 smoke = 711 total) and a v1.8.5–v1.8.9 acceptance delta section. Actual counts today: 437 unit / 63 suites + 317 smoke. |

Also improved within unchanged statuses: ESLint warnings 142 → 135 (0 errors);
`ARCHITECTURE.md` fixed the `10-tasks.js` 929→142 annotation and the stale
`00-pure-fns.js` references flagged last time.

## Priorities for this week

1. **Fix the 8 JSDoc parse ERRORs** — replace TypeScript optional syntax in `@param`
   tags with JSDoc-valid forms (e.g. `{{ id: string, note: (string|undefined) }}` or a
   shared `@typedef`). Locations: `10a-tasks-render.js:51,57,62,69`,
   `11-timeflow.js:326,333`, `pure-fns.js:940,955`. This is the third review in a row
   with this error class — add a CI job (or extend `jsdoc-check.yml`) that fails when
   `npm run docs` exits non-zero, so it cannot recur silently.

2. **Move commit `5381010` onto a branch and open a PR** — it is unpushed, so the fix is
   cheap: `git branch test/logger-unit-tests 5381010`, reset local `main` to
   `origin/main`, and PR the branch through the normal review pipeline.

3. **Cut a release (v1.9.0)** — two weeks of user-facing features are sitting in
   `CHANGELOG.md § Unreleased`. Tag and release so the changelog, version number, and
   `QA.md` acceptance records line up with what users are running. While there, merge the
   duplicate `### Added`/`### Fixed`/`### Changed` headings in the Unreleased section
   (Keep-a-Changelog expects one heading per category).

4. **Document the location store** — add `wl_location_v1` (origin, retention, field
   shape) to `DATA.md`. This single gap accounts for three ⚠️ items. At the same time
   add a `24-location.js` module section to `ARCHITECTURE.md`.

5. **Refresh `ARCHITECTURE.md`** — re-date the design certificate after reviewing the
   post-2026-06-03 changes, and correct stale line counts (`04-render.js` 413→650,
   `03-timer.js` 237→494, `09-clock-weather.js` 404→565, `12a-changelog.js` 279→899,
   `13-calendar.js` 193→496, `11-timeblock.js` 1 050→988). Consider adding sections for
   `pure-fns.js`, `logger.js`, `00-config.js`, and `01b-migrate.js`, which are referenced
   only in passing.

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | 36 JS source files in named-function functional style; no classes — appropriate for app size. |
| Code grouped into themed files/modules | ✅ Met | Numbered prefix convention (`01-state.js` … `25-rollingsummary.js`); `pure-fns.js` and `logger.js` as leaf ESM modules. |
| Main scripts import and run high-level functions | ✅ Met | `07-lifecycle.js` `DOMContentLoaded` calls only high-level init functions. |
| Low-level functions each carry out one specific task | ⚠️ Partial | Module-size creep: `11-timeblock.js` 988, `pure-fns.js` 908, `12a-changelog.js` 899, `10b-tasks-events.js` **883 (was 600 at last review — kanban DnD/tabs growth)**, `10a-tasks-render.js` 666. |
| Repetition minimised | ✅ Met | `fmtDur`, `escHtml`, `qa()`, `wlLog.*`, `safeCssColor` shared via `pure-fns.js`/`02-utils.js` without duplication. |
| Open for extension, closed for modification | ✅ Met | `01b-migrate.js` migrations are append-only; new stores use versioned `wl_*_v1` keys (e.g. `wl_location_v1`). |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `buildRollingSummary`, `mergeBackupEntries`, `locationFor`, `nextLocation` — self-documenting. |
| Names are explicit rather than implicit | ⚠️ Partial | Single-letter variables (`e`, `t`, `d`) persist in catch blocks and `.filter`/`.map` chains (visible throughout the ESLint warning list, e.g. `13-calendar.js`, `10a-tasks-render.js`). |
| Code logic is clear and avoids unnecessary complexity | ⚠️ Partial | Five modules in the 650–990-line range (see modular-code finding); `10b-tasks-events.js` grew 47 % since last review. |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ Met | `npm run lint`: **0 errors, 135 warnings** (was 142). All warnings are known classes: `no-unused-vars` from build concatenation + 9 `security/detect-unsafe-regex` in test/build scripts. |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | `src/`, `dist/`, `test/`, `docs/`, `.github/`, `scripts/`. Build artefacts (`script.js`, `styles.css`, `dist/`) untracked; `npm run build` regenerates cleanly. |
| Packages follow a standard structure | ✅ Met | `package.json` + lockfile committed; `.nvmrc` pins `24.15.0`. Minor: `engines` says `>=22.12.0` while `.nvmrc` says 24 — harmless but worth aligning. |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | e.g. `24-location.js` explains *why* `STORE_LOCATION` must live in `01-state.js` (concatenation order); `// Assumption:` blocks persist. |
| Comments are up to date and not misleading | ⚠️ Partial | `ARCHITECTURE.md` line annotations stale again: `04-render.js` 413 vs 650 actual, `03-timer.js` 237 vs 494, `12a-changelog.js` 279 vs 899, `13-calendar.js` 193 vs 496, `09-clock-weather.js` 404 vs 565. (The `10-tasks.js` and `00-pure-fns.js` issues from last review *were* fixed.) |
| Code is not commented out to control which lines run | ✅ Met | No toggled-off code blocks found. |
| All functions and classes are documented | ⚠️ Partial | `npm run docs` exits 1 with **8 ERRORs** (TS optional `?` in `@param`): `10a-tasks-render.js:51,57,62,69`, `11-timeflow.js:326,333`, `pure-fns.js:940,955`. The 2 old ERRORs and 10 WARNINGs from 2026-06-02 are fixed. |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project; JSDoc is the equivalent. |
| Auto-generated HTML documentation | ⚠️ Partial | 40 HTML pages generate (fresh timestamps) despite the exit-1, but the 8 erroring params are dropped from output. `docs.yml` auto-deploys on push to `main`. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`; README links to hosted docs. |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ✅ Met | Updated by #202 — now documents the kanban board and tabbed Today's Flow; setup/build/test sections current. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` present. |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user. |
| QA extent clearly documented | ✅ Met | `QA.md` updated 2026-06-08: 429 unit + 282 smoke = 711 total, with v1.8.5–v1.8.9 delta section. **Improvement since last review.** (Counts already 4 days behind — 437 + 317 today — refresh at next release.) |
| Assumptions documented next to code | ✅ Met | `// Assumption:` comments in `pure-fns.js`, `05-entries.js`, `07-lifecycle.js`; `DATA.md` documents schema assumptions. |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT); `CITATION.cff` carries `license-expression`. |
| Citation instructions given | ✅ Met | `CITATION.cff` with `preferred-citation`; README cite section. |
| Versioned releases (semantic versioning) | ✅ Met | Tags `v1.0.0`–`v1.8.9` follow semver; `release.yml` builds GitHub Releases from changelog sections. |
| CHANGELOG available to users | ✅ Met | `CHANGELOG.md` actively maintained — every PR since v1.8.9 has an Unreleased entry. Nit: Unreleased contains duplicate `### Added`/`### Fixed`/`### Changed` headings; Keep-a-Changelog expects one per category. |
| Example usage documented | ✅ Met | README usage examples; JSDoc `@example` on core pure functions. |
| Design certificates confirm compliance | ⚠️ Partial | Certificate stuck at `v1.8.9 + unreleased (main, 2026-06-03)`, ~20 PRs behind current main. **Regression since last review.** |
| Software fully specified | ⚠️ Partial | `ARCHITECTURE.md` has 32 module sections vs 36 source files: `24-location.js`, `pure-fns.js`, `logger.js`, `00-config.js`, `01b-migrate.js` lack their own sections (referenced only in passing). `25-rollingsummary.js` *is* covered. Stale line counts as noted above. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Git repo, 200+ commits, remote `lupinesse/worklog`. |
| Code committed regularly, per discrete unit of work | ✅ Met | PRs #170–#205 are single-topic squash commits. |
| Appropriate branching strategy defined and used | ⚠️ Partial | Strategy documented in `CONTRIBUTING.md`, but local commit `5381010` (2026-06-12) was made directly on `main`, bypassing the mandatory PR workflow. Unpushed — recoverable. **Regression since last review.** |
| Code is open-sourced; sensitive data omitted | ✅ Met | MIT licence; no credentials in tracked files; `config.local.ps1` and `00-config.local.js` gitignored. |
| Committing standards followed | ⚠️ Partial | 4 pre-commitlint commits remain in old history; all recent commits (incl. `5381010`) are conventional-commit conformant. |
| Commits tagged at significant stages | ⚠️ Partial | No tag since `v1.8.9` (2026-05-28) despite major shipped features (#177, #184, #192, #201, #203). A v1.9.0 release is overdue. **Regression since last review.** |
| Continuous integration applied | ✅ Met | 16 GitHub Actions workflows active (ci, codeql, lighthouse, docs, a11y-audit, scss-audit, dead-code, jsdoc-check, impact-check, pr-review, chatgpt-pr-review, auto-fix-ci, auto-label-pr, delivery-metrics, release, weekly-qa-review). |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | Anthropic key server-side (since v1.8.7); local config files gitignored. |
| Configuration in dedicated config file | ✅ Met | `src/js/00-config.js`; personal overrides in gitignored `00-config.local.js`. |
| Multiple config files per system/local/user | ✅ Met | Base + user (`00-config.local.js`) + server (`config.local.ps1`). |
| Config files versioned separately from analysis code | ✅ Met | `00-config.js` committed independently; overrides untracked. |
| Config used to generate outputs is recorded | ✅ Met | `scripts/config-snapshot.cjs`; `wlLog.config()` at startup ([07-lifecycle.js:345](../../src/js/07-lifecycle.js)). |
| Example config templates provided | ✅ Met | `src/js/00-config.local.example.js` and `config.local.example.ps1`, dummy values only. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ⚠️ Partial | `.lighthouserc.json` asserts accessibility ≥ 0.85 at `"warn"` level only — CI never fails on a drop. `a11y-audit.yml` provides additional WCAG checking. **Scoring correction from last review's ✅** — flip the assertion to `"error"` to genuinely enforce. |
| Analysis data stored in an open format | ✅ Met | All data JSON (localStorage + backup export). |
| Input data stored safely and treated as read-only | ✅ Met | Backup **merge** (#192) only adds entries with unseen IDs — never mutates existing records; restore writes fresh keys. |
| Input data versioned; changes create new versions | ✅ Met | `01b-migrate.js` append-only migration log; versioned `wl_*_v1` keys. |
| All input data documented in a data register | ⚠️ Partial | `DATA.md` documents 31 keys but omits `wl_location_v1` (added with the Remote/Office toggle). **Regression since last review.** |
| Outputs are disposable — regenerated at will | ✅ Met | `npm run build` regenerates `script.js` (13 019 lines), `styles.css`, `dist/`; none committed. |
| Non-sensitive data available so others can run the code | ✅ Met | Self-contained fixtures in `smoke-tests.cjs`; no personal data in repo. |
| Data quality monitored | ✅ Met | `wlLog.warn()` on `JSON.parse` failures in `load*()` functions (e.g. `24-location.js`). |
| Fields in datasets documented | ⚠️ Partial | `DATA.md` field dictionary complete except the `wl_location_v1` map shape. Same root cause as the register gap. |
| Large or complex data stored in a database | ➖ N/A | localStorage appropriate for a single-user tool. |
| Data documented in information asset register | ⚠️ Partial | `DATA.md` serves as the register; the location store is the one omission. |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ✅ Met | PRs #170–#205 squash-merged through the ChatGPT + Claude four-phase dialogue with merge-gate; thread resolutions in PR history. |
| Pair programming used | ✅ Met | `CONTRIBUTING.md` documents the AI pair model; `CITATION.cff` credits Claude Code. |
| Users encouraged to participate in peer review | ⚠️ Partial | Templates invite contribution; inherent single-user limitation. |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit.mjs` — **437 tests across 63 suites, 0 failures** (up from 397/57). New suites cover kanban DnD, rolling summary, location helpers, calendar recurrence, wlLog. |
| Tests run regularly and after every significant change | ✅ Met | `ci.yml` runs `npm test` on every push. |
| Bug fixes add new unit tests | ✅ Met | #196 (QA regressions) and #199 (locale separator) shipped with tests; CHANGELOG entries cite "covered by new unit and smoke tests". |
| Informal tests recorded near the code | ✅ Met | JSDoc `@example` tags on core pure functions. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ Met | `QA.md` sign-off tables per release, last updated 2026-06-08. |
| Tests run automatically via CI or git hooks | ✅ Met | `ci.yml` full suite; lint-staged pre-commit via husky. |
| Whole process tested end-to-end | ✅ Met | `smoke-tests.cjs`: **317 passed, 0 failed**. `npm test` exits 0. **Regression from 2026-06-02 resolved.** |
| Test code is clean and readable | ✅ Met | `freshPage()` helper, fixtures, parameterised cases in both suites. |
| Formal user-acceptance testing conducted and recorded | ✅ Met | `QA.md` UAT process and outcomes per release. |
| Integration tests confirm multiple units work together | ✅ Met | Smoke suite exercises full app flows; `.github/scripts/test/ci-scripts.test.mjs` (30 tests) covers CI tooling. **Improvement since last review.** |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | Example config files document all secrets with placeholders. |
| Required libraries documented including versions | ✅ Met | `package-lock.json` committed; exact pins. |
| Working OS environments documented | ✅ Met | README (Windows primary), `launch.sh`, `.devcontainer/devcontainer.json`. |
| Example configuration files provided | ✅ Met | `src/js/00-config.local.example.js`, `config.local.example.ps1`. |
| Code runs independently of operating system | ✅ Met | `path.join()` throughout `build.js`. |
| Dependencies managed separately for users/devs/testers | ✅ Met | 0 runtime dependencies; 15 devDependencies. |
| As few dependencies as possible | ✅ Met | 15 devDependencies (was 11 — additions are playwright, sass, and lint tooling, each justified by an npm script). |
| Package dependencies managed with environment manager | ✅ Met | npm + lockfile + `.nvmrc` (24.15.0). |
| Docker containers / VM builds version controlled | ✅ Met | `.devcontainer/devcontainer.json` (javascript-node:1-24-bookworm). |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ✅ Met | `wlLog.error()`/`warn()` on parse failures, missing DOM, API errors; now itself unit-tested (commit `5381010`). |
| Code configuration recorded when code runs | ✅ Met | `wlLog.config()` at startup ([07-lifecycle.js:345](../../src/js/07-lifecycle.js)). |
| Pipeline route recorded when decisions are made | ✅ Met | `wlLog.info` at branch points, e.g. auto-pause decision at [07-lifecycle.js:336](../../src/js/07-lifecycle.js). |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | `CONTRIBUTING.md` solo-developer + AI-pair model. |
| Issue tracker records development tasks | ✅ Met | GitHub Issues; PRs #170–#205 reference issue closure. |
| New issues guided by user needs and stories | ✅ Met | Issue template "As a user…" sections. |
| Issue templates ensure proper logging | ✅ Met | `bug_report.md`, `feature_request.md` in `.github/ISSUE_TEMPLATE/`. |
| Acceptance criteria noted and fulfilment recorded | ✅ Met | PR template acceptance checklist; issues carry Definition of Done. |
| QA standards and processes defined | ✅ Met | `CLAUDE.md` operative standard; this automated review is the enforcement mechanism. Process gap noted: the 2026-06-08 review's report was not committed to `docs/qa-reports/`. |
