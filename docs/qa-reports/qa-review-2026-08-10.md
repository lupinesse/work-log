# QA Review — 2026-08-10

- Commit: `f67a632` on `claude/fervent-bardeen-5lvbjs` (branched from `main`)
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated weekly QA review)

> **Environment note:** `npm ci` succeeded cleanly again this review (324 packages) once Node
> was switched from the sandbox default (`v22.22.2`) to the pinned `24.15.0` via `nvm` (matches
> `.nvmrc`/`engines.node`). Under that version, verified **directly**: `npm run lint` (ESLint +
> Stylelint) → 0 errors, 0 warnings; `npm run build` → `script.js` 13,971 lines/45 files,
> `styles.css` 8,059 lines, Vite bundle in 671ms, `git status` clean after; `npm run docs`
> (JSDoc) → 0 errors; `npm run test:unit` → **634/98 ✅** (up from 530/81); `npm run test:scripts`
> → **248/50 ✅** (up from 171/32, driven mainly by the two new self-test suites below); the new
> `npm run test:commitlint` → **passes**, confirming last week's Priority 3 fix actually works.
> `npm run test:actionlint` fails locally only because the `actionlint` binary isn't installed in
> this sandbox (`ENOENT`) — same class of environment gap as Playwright below, not a repo defect;
> `ci.yml`'s `lint` job installs a SHA-pinned `actionlint` and has passed on every push since
> introduction (#312). `smoke-tests.cjs` (Playwright) still cannot run locally: confirmed via
> `npx playwright install --dry-run chromium` that `playwright@1.61.1` now expects build
> `chromium-1228`/`chromium_headless_shell-1228`, while the sandbox only ships `-1194` — identical
> unresolved mismatch to the last two reviews. `ci.yml` is authoritative and confirmed green (via
> the GitHub Actions API) on every push to `main` since the last review. **New this review:**
> `npm audit` reports **5 high-severity advisories** (`brace-expansion`, `fast-uri`, `immutable`,
> `js-yaml`, `linkify-it`) — all transitive devDependencies (via `eslint`, `commitlint`/`stylelint`,
> `sass`, `jsdoc`), all with fixes available via `npm audit fix`, none checked in any prior QA
> review. See Dependency management.
>
> **Also new this review — a correction to last week's report's own arithmetic.** The
> 2026-08-03 report's scoreboard header states `✅ 74 / ⚠️ 9 / ❌ 0 / ➖ 4`, but its own "Full
> results" tables (counted programmatically, `grep`-verified twice) actually contain **70 ✅, 13
> ⚠️, 0 ❌, 4 ➖** — a 4-item miscount in both directions that happens to preserve the same total
> (87). This review's "Regressions since last review" section below uses the **actual** counted
> baseline (70/13/0/4), not the reported one (74/9/0/4), and this report's own scoreboard has been
> independently re-counted the same way before being written down, specifically to avoid repeating
> the error. Flagged as Priority 3 below — a QA process whose own scoreboard doesn't foot is a
> credibility problem for a process whose entire purpose is catching exactly this kind of drift.

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  70   |
| ⚠️ Partial    |  13   |
| ❌ Not met    |   0   |
| ➖ N/A        |   4   |

**Total items assessed: 87** — net **unchanged** vs last review's actual counted baseline
(70/13/0/4 on 2026-08-03; see the correction note above). One item was promoted (commitlint
enforcement, now verified fixed) and one dropped (README usage docs, found stale on direct
inspection this week) — they exactly offset. Three of last week's five priority fixes
(dead-code.yml, delivery-metrics.yml, ARCHITECTURE.md refresh) turned out to be **only partially
effective**: each fix is correct in isolation but was either not yet exercised by a real
scheduled run, or was already out of date by the time it landed, because a same-day/same-week PR
reintroduced the identical gap in a new file.

---

## Regressions since last review (2026-08-03, actual count 70/13/0/4 — see correction note)

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| README covers purpose, installation, usage | ✅ | ⚠️ | Direct inspection this review: the README's "Export & review", "Planning", and "Today's Flow" sections list none of the five `[1.9.1]` features already shipped as of last review's own commit range (`weekly report draft`, `end-of-week gap report`, `weekly plan review checklist`, `long-running-timer warning`, `proof-link/note fields` on log entries). Last week's report rated this ✅ ("no drift detected against this week's features") — that call does not hold up under a section-by-section read this week. Not new drift so much as a correction to an over-generous prior rating, but the effect is the same: a user reading the README today would not learn these features exist. |

---

## Improvements since last review

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| Committing standards followed | ⚠️ | ✅ | Verified directly: `echo "test: fix" \| npx commitlint` exits 0, and the new `npm run test:commitlint` self-test (added in #292) passes — `@commitlint/cli@21.2.1` now resolves correctly against `@commitlint/config-conventional@21.2.0`. Last week's blocking defect (`ERR_PACKAGE_PATH_NOT_EXPORTED` on every clean-install commit) is fixed and now has CI coverage (`ci.yml`'s `lint` job runs `test:commitlint`), so a future version-skew regression like this one would be caught before merge instead of surfacing on someone's next clean checkout. |

Also, not a formal status change but worth recording: the two automation workflows fixed by
last week's PR #288 (`dead-code.yml`, `delivery-metrics.yml`) now have branch+PR commit logic and
a corrected `.gitignore` allow-list respectively — both **verified correct on inspection** — but
neither fix has yet been exercised by an actual scheduled trigger (both fire at 09:15/09:30 UTC
today, after this review runs). The two dead-code reports that *have* landed since the fix
(`dead-code-2026-07-31.md` via PR #274, `dead-code-2026-08-07.md` via draft PR #316) were both
produced by manual/ad-hoc Claude sessions on non-bot branch names (`claude/cool-heisenberg-*`),
not by `dead-code.yml` itself — see "QA standards and processes defined" below and Priority 2.

---

## Priorities for this week

1. **Tag and release v1.9.1.** `CHANGELOG.md` already has a full `## [1.9.1] — 2026-08-03`
   section and `package.json` reads `"version": "1.9.1"`, but `git tag -l` shows no `v1.9.1` —
   only `v1.9.0` (2026-06-12) exists, confirmed after `git fetch --tags`. `release.yml` triggers
   solely `on: push: tags: 'v*'`, so it has never run for this version and no GitHub Release
   exists for it either. This is the **7th consecutive review** flagging release-tagging drift
   (first raised 2026-06-15); it has now progressed from "gap widening" to an outright
   inconsistency between what the docs claim shipped and what git/GitHub can verify shipped.
   Concrete next step: `git tag v1.9.1 && git push origin v1.9.1` from `main` at the commit where
   the `[1.9.1]` CHANGELOG section was finalised (`d3cba88` or later), then confirm `release.yml`
   fires and publishes.

2. **Confirm `dead-code.yml` and `delivery-metrics.yml` actually work under their real
   triggers, not just on paper.** Both were fixed in `#288` (branch+PR commit pattern;
   `.gitignore` allow-list for `docs/delivery-metrics.md`) and both look correct on inspection.
   But **every one of the 10 recorded runs of `dead-code.yml` is still a `failure`** (latest:
   2026-08-03, pre-fix — checked via the GitHub Actions API), and `delivery-metrics.yml`'s last
   recorded run (also 2026-08-03) fired **7 minutes before** the fix landed the same day, so it's
   still the old false-"success" no-op. The reports that have landed since (`dead-code-2026-07-31`,
   `dead-code-2026-08-07`) came from people/sessions running the underlying skill by hand, which
   means the actual automation is currently unverified and being silently substituted for by manual
   effort — exactly the failure mode this dashboard exists to catch. Both are scheduled to fire
   today (09:15 and 09:30 UTC); check `gh api .../actions/workflows/<id>/runs` after they do and
   confirm a real `success` with a real committed file, not just a green checkmark.

3. **Fix the QA-report scoreboard miscount, and consider a self-check.** The 2026-08-03 report's
   header claimed 74 ✅ / 9 ⚠️, but its own tables contain 70 ✅ / 13 ⚠️ (see the correction note
   above) — a discrepancy nobody caught for a week because nothing cross-checks the header against
   the table it summarises. A one-line addition to the `/qa-review` skill — count the emoji in the
   tables it just wrote and assert the total matches the header before finishing — would catch this
   class of error going forward at near-zero cost.

4. **`npm audit fix` the 5 new high-severity findings.** First time any QA review has run
   `npm audit`: `brace-expansion` (via `eslint→minimatch`), `fast-uri` (via `commitlint`/
   `stylelint→ajv`), `immutable` (via `sass`), `js-yaml` (via `stylelint→cosmiconfig`), and
   `linkify-it` (via `jsdoc→markdown-it`) all have high-severity DoS advisories with fixes
   available. All are transitive devDependencies with no runtime/production exposure, so the real
   risk is low, but there is no reason to leave a one-command fix unapplied once it's known about.

5. **Add `12d-weeklyreport.js` to `ARCHITECTURE.md`'s module map, and re-baseline the line
   counts and design cert properly this time.** The exact defect fixed last week for
   `12c-gapreport.js` (a new module missing from the module map) has recurred for a second new
   file — `12d-weeklyreport.js` (104 lines, from PR #289) — which already existed in the tree 10
   minutes *before* last week's doc-refresh commit (`859fa3a`) landed, meaning the refresh missed
   a file that was already there. Separately, `04-render.js` has grown again, from 762 lines (the
   count fixed last week) to **892** — +130 lines/+17% in the week since, and now the single
   largest module for the third review running, still unsplit. `pure-fns-export.js` (726 lines)
   and `12b-changelog-data.js` (631 lines) have also newly crossed the 500-line watch threshold.
   While re-baselining, bump the design cert (currently `1.9.0-r3`, dated 2026-08-03 — already
   inaccurate for `04-render.js` as of the same day it was published) and refresh `QA.md`'s test
   table (currently documents 582/92 unit and 171/32 CI-script tests "as of 2026-08-03"; actual is
   now 634/98 and 248/50). A lightweight CI check that fails when a `src/js/*.js` file has zero
   mentions in `ARCHITECTURE.md` would stop this specific gap from recurring a third time.

---

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | Unchanged — functional style across 52 JS source files; this week's new pure functions (`buildEntryLinkMap`, `findExportWarnings`, `buildWeeklyTicketSummary`, `formatWeeklyTicketSummaryText`, `findWeeklyPlanReviewTasks`) follow the same convention. |
| Code grouped into themed files/modules | ✅ Met | Two new modules this week, both following the numbered-prefix convention: `12d-weeklyreport.js`, `10e-weeklyplan-review.js`. |
| Main scripts import and run high-level functions | ✅ Met | Unchanged — `07-lifecycle.js`'s `DOMContentLoaded` handler calls only high-level init functions. |
| Low-level functions each carry out one specific task | ⚠️ Partial | `04-render.js` is now **892** non-blank lines (was 762 last review, was 646 two reviews ago) — +17% in one week despite being flagged as the top split candidate last review. No split attempted. See Priority 5. |
| Repetition minimised | ✅ Met | `createRestartedEntry()` (`05-entries.js`) consolidates what were three separate inline "restart timer" implementations (log ▶ button, board "▸ track", "+ track recent" chips) into one shared function this week. |
| Open for extension, closed for modification | ✅ Met | New weekly-report/gap-report/plan-review features added without modifying existing validation or migration code paths. |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `findGapReportEntries`, `buildWeeklyTicketSummary`, `createRestartedEntry`, `findPromotableTask` — self-documenting names for this week's new/refactored functions. |
| Names are explicit rather than implicit | ⚠️ Partial | `grep -rPo '\([a-z]\) =>' src/js/` now counts **294** (up from 285 last review, 280 the review before). Same top-density files as the last three reviews: `14-jira.js` (22), `12-misc.js` / `11-timeflow.js` (20 each), `06a-hero.js` (19). Unresolved for a **fourth consecutive review**. |
| Code logic is clear and avoids unnecessary complexity | ⚠️ Partial | Files over 500 non-blank lines, growing: `04-render.js` (892, was 762), `pure-fns-export.js` (726, newly over threshold — most of this week's export-feature pure functions landed here), `12b-changelog-data.js` (631, newly over threshold), `03-timer.js` (572, unchanged), `09-clock-weather.js` (565, unchanged), `11-timeflow.js` (536, unchanged), `06a-hero.js` (515, unchanged). |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ Met | Verified directly: `npm run lint` → 0 errors, 0 warnings. |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | Unchanged: `src/`, `dist/` (untracked), `test/`, `docs/`, `.github/`, `scripts/`. `npm run build` verified locally; `git status` clean afterward. |
| Packages follow a standard structure | ✅ Met | `package.json`/`package-lock.json` in sync (`npm ls --depth=0` shows no drift). `.nvmrc` (`24.15.0`) matches `engines.node`. |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | `CHANGELOG.md`'s entries for this week's features explain rationale (e.g. the weekly plan review checklist cites the specific failure mode — planned tasks quietly finished elsewhere before their week arrived). |
| Comments are up to date and not misleading | ⚠️ Partial | Mixed: `03-timer.js`'s ARCHITECTURE.md annotation (fixed last week at 572) still matches exactly. But `04-render.js`'s annotation, refreshed to 848 last week, is already wrong again — actual is **892** (+44/+5.2%, from PR #294 landing 29 minutes after the refresh commit, same day). See Priority 5. |
| Code is not commented out to control which lines run | ✅ Met | No toggled-off code blocks found in `src/js/`. |
| All functions and classes are documented | ✅ Met | `npm run docs` (JSDoc) exits 0 errors. Spot-checked this week's new pure functions (`findWeeklyPlanReviewTasks`, `buildWeeklyTicketSummary`, `buildEntryLinkMap`, `findExportWarnings`) — all carry full JSDoc with `@param`/`@returns`. |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project. |
| Auto-generated HTML documentation | ✅ Met | `npm run docs` verified locally, 0 errors. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`, unchanged. |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ⚠️ Partial | **Regression** (see table above) — five already-shipped `[1.9.1]` features are absent from README's feature sections. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md` updated this week to document the new `test:commitlint`/`test:actionlint` scripts (#305); `CODE_OF_CONDUCT.md` unchanged. |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user. |
| QA extent clearly documented | ⚠️ Partial | `QA.md`'s v1.9.0+ delta table was refreshed last week to "582 tests / 92 suites ... as of 2026-08-03" — already stale one week later: actual is now **634/98** unit and **248/50** CI-script tests (the latter nearly +50% from the two new self-test suites). Same recurring pattern as the last five reviews. |
| Assumptions documented next to code | ✅ Met | `// Assumption:` comments unchanged; no new implicit assumptions introduced this week that lack a comment. |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT), `CITATION.cff` unchanged. |
| Citation instructions given | ✅ Met | `CITATION.cff` unchanged. |
| Versioned releases (semantic versioning) | ⚠️ Partial | No tag newer than `v1.9.0` (2026-06-12), 50 commits ahead — but now sharper: `CHANGELOG.md`/`package.json` both assert `v1.9.1` shipped 2026-08-03 while git/GitHub show no such release exists. See Priority 1. |
| CHANGELOG available to users | ✅ Met | `CHANGELOG.md`'s `[1.9.1]` section and `Unreleased` section both current and detailed for this week's work. |
| Example usage documented | ✅ Met | JSDoc `@example` tags present on this week's new pure functions; README examples exist for prior features (gap flagged above is coverage of *new* features, not example quality generally). |
| Design certificates confirm compliance | ⚠️ Partial | Cert (`1.9.0-r3`, dated 2026-08-03) was refreshed last week specifically to fix stale line counts, but was already inaccurate for `04-render.js` by the same evening (see Code documentation). The refresh process itself needs to survive same-day follow-on PRs, not just catch up once. |
| Software fully specified | ⚠️ Partial | `12c-gapreport.js`'s missing module-map entry (last week's finding) is fixed. But `12d-weeklyreport.js` (104 lines, PR #289) has **zero mentions** in `ARCHITECTURE.md` (`grep -n "weeklyreport" ARCHITECTURE.md` → empty), and this file already existed before last week's refresh commit ran. Same defect, different file, still open. See Priority 5. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Unchanged. |
| Code committed regularly, per discrete unit of work | ✅ Met | 33 commits since last review, each single-topic. |
| Appropriate branching strategy defined and used | ✅ Met | All commits landed via PR on `claude/`- or `feat/`/`fix/`/`chore/`-prefixed branches; no direct-to-main commits found. |
| Code is open-sourced; sensitive data omitted | ✅ Met | Targeted grep for credential patterns (`sk-ant`, `AKIA`, private-key headers) across `src/` and `.github/` this review found nothing. |
| Committing standards followed | ✅ Met | **Improvement** (see table above) — commit-msg hook verified working; `test:commitlint` passes and is wired into CI. |
| Commits tagged at significant stages | ⚠️ Partial | Same underlying gap as "Versioned releases" — see Priority 1. |
| Continuous integration applied | ✅ Met | `ci.yml` confirmed green (via GitHub Actions API) on every push to `main` since last review, including all this week's dependency bumps and feature PRs. The two non-functional-until-verified scheduled workflows (`dead-code.yml`, `delivery-metrics.yml`) are tracked separately under "QA standards and processes defined" — the core test/lint/build gate is unaffected. |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | Confirmed via targeted grep this review; local config gitignored. |
| Configuration in dedicated config file | ✅ Met | `src/js/00-config.js`, unchanged. |
| Multiple config files per system/local/user | ✅ Met | Unchanged. |
| Config files versioned separately from analysis code | ✅ Met | Unchanged. |
| Config used to generate outputs is recorded | ✅ Met | `scripts/config-snapshot.cjs`; `wlLog.config()` at startup, unchanged. |
| Example config templates provided | ✅ Met | `src/js/00-config.local.example.js`, `config.local.example.ps1`, unchanged. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ✅ Met | `.lighthouserc.json` accessibility assertion unchanged, CI-enforced. |
| Analysis data stored in an open format | ✅ Met | All data JSON; no new field types introduced this week. |
| Input data stored safely and treated as read-only | ✅ Met | Unchanged merge/append-only pattern. |
| Input data versioned; changes create new versions | ✅ Met | `01b-migrate.js` unchanged; no new fields required migration this week. |
| All input data documented in a data register | ✅ Met | `DATA.md` unchanged; no new persisted fields were added this week (weekly report/gap report/plan-review are computed views over existing fields, not new stored data). |
| Outputs are disposable — regenerated at will | ✅ Met | `npm run build` verified to regenerate cleanly. |
| Non-sensitive data available so others can run the code | ✅ Met | Self-contained fixtures in `test/unit.mjs`, unchanged. |
| Data quality monitored | ✅ Met | `wlLog.warn()` on parse failures unchanged. |
| Fields in datasets documented | ✅ Met | `DATA.md` current — no new fields this week. |
| Large or complex data stored in a database | ➖ N/A | localStorage appropriate for a single-user personal tool. |
| Data documented in information asset register | ✅ Met | `DATA.md` is the register, current. |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ✅ Met | All 33 commits since last review landed via PR, consistent with the four-phase ChatGPT/Claude review pipeline. |
| Pair programming used | ✅ Met | `CONTRIBUTING.md` documents the AI-pair model, unchanged. |
| Users encouraged to participate in peer review | ⚠️ Partial | Unchanged — templates invite contribution, but single-user tool limits wider participation. |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit.mjs` — **634 tests across 98 suites, 0 failures** (up from 530/81). |
| Tests run regularly and after every significant change | ✅ Met | `ci.yml`'s `test` job ran on all pushes since last review. |
| Bug fixes add new unit tests | ✅ Met | E.g. the ad-hoc log row fix (#283) and Kanban auto-promote fix (#284) both shipped with dedicated regression tests. |
| Informal tests recorded near the code | ✅ Met | JSDoc `@example` tags on new pure functions this week. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ Met | `QA.md` sign-off tables per release, unchanged (delta table staleness is a separate finding above, not a sign-off gap). |
| Tests run automatically via CI or git hooks | ✅ Met | `ci.yml` full suite; lint-staged pre-commit via Husky, unchanged. |
| Whole process tested end-to-end | ✅ Met | `smoke-tests.cjs` cannot run locally this review (same Playwright browser mismatch as last review); `ci.yml` is authoritative and green on every push. |
| Test code is clean and readable | ✅ Met | `freshPage()` helper and parameterised cases unchanged; new tests follow the same pattern, including 20 new unit tests for the actionlint self-test harness and 36 for the commitlint self-test harness. |
| Formal user-acceptance testing conducted and recorded | ✅ Met | `QA.md` UAT process unchanged. |
| Integration tests confirm multiple units | ✅ Met | `npm run test:scripts` — **248 tests across 50 suites, 0 failures** (up from 171/32). |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | Example config files unchanged. |
| Required libraries documented including versions | ✅ Met | `package-lock.json` verified in sync via `npm ls --depth=0`. |
| Working OS environments documented | ✅ Met | README, `launch.sh`, `.devcontainer/devcontainer.json` unchanged; Node-version docs updated to match `engines` (#303). |
| Example configuration files provided | ✅ Met | Unchanged. |
| Code runs independently of operating system | ✅ Met | `path.join()` usage unchanged. |
| Dependencies managed separately for users/devs/testers | ✅ Met | 0 runtime dependencies; devDependencies only, confirmed via `npm ls`. |
| As few dependencies as possible | ✅ Met | No new dependencies added since last review — only version bumps (eslint, vite, playwright, prettier, stylelint, actions/setup-node), all already justified; 5 more dependabot PRs (sass, vite, playwright, globals, lint-staged) are open and CI-green but not yet merged. |
| Package dependencies managed with environment manager | ⚠️ Partial | Last week's root cause (commitlint/hook breakage) is now fixed and verified — but **first-time-checked `npm audit` found 5 unrelated high-severity advisories** in transitive devDependencies (`brace-expansion`, `fast-uri`, `immutable`, `js-yaml`, `linkify-it`), all fixable via `npm audit fix`, none previously flagged in any QA review. See Priority 4. |
| Docker containers / VM builds version controlled | ✅ Met | `.devcontainer/devcontainer.json` unchanged. |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ✅ Met | `wlLog.error()`/`warn()` unchanged. |
| Code configuration recorded when code runs | ✅ Met | `wlLog.config()` at startup, unchanged. |
| Pipeline route recorded when decisions are made | ✅ Met | `wlLog.info` at branch points unchanged; no new undocumented decision branches this week. |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | `CONTRIBUTING.md` unchanged. |
| Issue tracker records development tasks | ✅ Met | GitHub Issues; this week's PRs reference their originating issues (#297, #310, etc.). |
| New issues guided by user needs and stories | ✅ Met | Issue templates unchanged. |
| Issue templates ensure proper logging | ✅ Met | `bug_report.md`, `feature_request.md` unchanged. |
| Acceptance criteria noted and fulfilment recorded | ✅ Met | PR template checklist unchanged; e.g. PR #316's acceptance criteria are explicitly checked off in its description. |
| QA standards and processes defined | ⚠️ Partial | `CLAUDE.md`/`QA.md` remain well-defined as documents. But `dead-code.yml` and `delivery-metrics.yml` — fixed in code last week — have **zero verified successful scheduled runs** to date (all 10 recorded `dead-code.yml` runs are failures; `delivery-metrics.yml`'s last run predates the fix by 7 minutes). The reports that have landed since came from manual/ad-hoc sessions substituting for the still-unverified automation. See Priority 2. |

---

*Generated by an automated weekly QA review session. Read-only — no source files were modified.*
