# QA Review — 2026-08-31

- Commit: `b580edc` on `main` (audited from `claude/fervent-bardeen-92swo0`)
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated weekly QA review)

> **Environment note:** `npm ci` succeeded cleanly (Node `22.22.2` locally; `.nvmrc`/`engines.node`
> still read `22.22.1`, unchanged since #353). Verified directly: `npm run lint` → 0 errors, 0
> warnings; `npm run build` → `script.js` 13,991 lines/45 files, `styles.css` 8,115 lines, `git
> status` clean after; `npm run docs` (JSDoc) → 0 errors; `npm run test:unit` → **669/104 ✅** (up
> from 659/102); `npm run test:scripts` → **289/59 ✅** (up from 280/57); `npm run test:commitlint`
> and `npm run test:changelog` → pass. `npm run test:actionlint` run against the pinned,
> checksum-verified `actionlint@1.7.12` binary (downloaded and SHA256-verified the same way `ci.yml`
> does) → clean, both regression fixtures still correctly fail. `smoke-tests.cjs` ran and passed
> **locally**, 320/320, via `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH` pointed at this sandbox's
> `chromium_headless_shell-1194`. `npm audit` → 0 vulnerabilities (unchanged). CI on `main`'s HEAD
> (`b580edc`) confirmed green via the GitHub Actions API.
>
> **New this review — a live, ongoing CI outage, found via GitHub Actions job-log inspection, not
> `git log`:** the `dead-code.yml` and `scss-audit.yml` workflows — both of which invoke the
> `claude` CLI — have been failing on **every run since 2026-08-15/17** (2 straight Monday
> schedule failures for `dead-code.yml` on 08-17 and 08-24; 2 straight PR-triggered failures for
> `scss-audit.yml` on 08-21 and 08-28), all reproduced directly by fetching their job logs this
> review. Root cause and a complete, tested, CI-green fix already exist in **open PR #367**
> (reviewed and APPROVEd 2026-08-21, zero blocking findings) — it has simply not been merged in the
> 10 days since. See Priority 1. Also found: a **backlog of 8 open PRs**, several fully reviewed and
> CI-green, some over two weeks old (#315: 26 days: #344: 17 days) — see Priority 2. This review
> also cross-checked an **unmerged draft QA report already sitting on `main`'s history as PR #371**
> ("Weekly QA report 2026-08-24") which independently found and documented the same CI outage on
> 2026-08-24; its findings are credited and folded in below rather than re-presented as newly
> discovered. This review's regression/improvement comparison is still against the last **merged**
> report (2026-08-17), per the skill's own instruction to compare against the most recent existing
> file in `docs/qa-reports/`.

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  73   |
| ⚠️ Partial    |   9   |
| ❌ Not met    |   1   |
| ➖ N/A        |   4   |

**Total items assessed: 87** — net **−3 ✅ / +2 ⚠️ / +1 ❌** vs last review (2026-08-17:
76/7/0/4). One item recovered (no repeat of the PR #355 "merge over a blocking review" pattern),
but four items regressed, all stemming from the same underlying discovery: two of the project's own
CI/QA-automation workflows have been silently broken for two-plus weeks, and the fix for it — fully
reviewed, CI-green — has been waiting to be merged for 10 days.

---

## Regressions since last review (2026-08-17)

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| QA standards and processes defined | ✅ | ❌ | `dead-code.yml` (scheduled, weekly) and `scss-audit.yml` (triggered on `src/css/**` PRs) are two of this project's own defined QA-automation checks. Both have failed on **every run for 2+ weeks**: `dead-code.yml`'s scheduled runs on 2026-08-17 and 2026-08-24 both exited 1 with `Credit balance is too low` (confirmed via job logs, run ids 32018182777 and 32715168561); `scss-audit.yml` failed identically on PR #365 on 2026-08-21 and again on 2026-08-28 (run id 33156577355 — the diagnostic itself was invisible here, swallowed by an unguarded `bash -e` abort, which is the second half of the same root cause). Root cause: both workflows `unset CLAUDE_CODE_OAUTH_TOKEN` whenever `ANTHROPIC_API_KEY` is present, discarding a working subscription token in favour of a pay-as-you-go key that ran out of credit — the opposite of the order documented in each workflow's own header comment. A complete, tested fix has existed as open PR #367 since 2026-08-21 (reviewed, APPROVEd, CI-green) but is still unmerged. A checklist item whose own supporting automation doesn't run isn't met, regardless of how well the gap is understood. |
| Continuous integration applied | ✅ | ⚠️ | The required gate (`ci.yml`: lint/test/scripts-test/CodeQL/lighthouse) stayed green throughout and is green on `main`'s current HEAD (`b580edc`) — verified via the Actions API. But two other CI-invoking workflows sat broken and unnoticed by the process itself for 2+ weeks (see row above); nothing short of a manual audit caught it. "Applied smoothly" doesn't hold when a third of the repo's CI surface silently stopped running. |
| Misuse or failure produces informative error messages | ✅ | ⚠️ | The failing `scss-audit.yml` runs are a textbook counterexample of this principle in the repo's own tooling: the CLI's diagnostic was redirected to a file, and an unguarded `bash -e` abort meant the file was never read before the step died — every failed run showed only a bare `Process completed with exit code 1`, no root cause, for 6+ days before anyone noticed (confirmed directly from job logs: run 33156577355's tail shows no CLI output at all, just the exit and cleanup). `dead-code.yml`'s failure was at least visible (`Credit balance is too low` reached the log) — `auto-chore.yml` has the same "captured but never printed" pattern in a milder form (its own `wc -l` counts a file's lines without ever showing them). All three are fixed in #367 — the gap is that main is still running the version that hides its own failures. |
| Appropriate branching strategy defined and used | ✅ | ⚠️ | The strategy itself (feature branches + PR + review) is sound and still followed for every commit that landed this period. But the queue it's supposed to feed isn't draining: **8 PRs are open**, several fully CI-green and reviewed with zero blocking findings, and two are stale well past a normal review cycle — #315 ("Recreate PR #265's module splits") opened 2026-08-05 (26 days), #344 ("model safeCssColor as an html-injection barrier") opened 2026-08-14 (17 days), both still `draft`. A branching strategy that accumulates reviewed, unmerged work isn't fully serving its purpose even when every individual step in it is correct. See Priority 2. |

---

## Improvements since last review

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| Peer review conducted and recorded near code | ⚠️ | ✅ | No repeat of the 2026-08-17 finding (a `REQUEST CHANGES` review merged over without fixing the named blockers). This period's PRs got real review with real fixes: PR #362 (the JSDoc/impact-check regression fix) received a full `APPROVE` review with 0 blocking findings before merge, and PR #367 (the CI-outage fix, still open) was reviewed line-by-line with only 2 optional nitpicks, both explicitly non-blocking. The process is gating correctly again — the open question this week is purely about merge cadence (see the branching-strategy regression above), not review quality. |

---

## Priorities for this week

1. **Merge PR #367.** It is CI-green, was reviewed 10 days ago with an `APPROVE` verdict and zero
   blocking findings, and it directly fixes the live `scss-audit.yml`/`dead-code.yml` outage
   described above (root cause: OAuth-token/API-key credential ordering, plus an invisible-failure
   `bash -e` pattern repeated across three workflows). Every day it sits unmerged is another day
   those two QA-automation workflows keep failing on `main`. This is the single highest-leverage
   action available this week — one merge closes three of this review's four regressions at once.

2. **Clear the PR backlog, starting with the reviewed and CI-green items.** 8 PRs are open:
   #367 (fix, CI-green, reviewed — see above), #368 ("session guard", CI-green per its own check
   runs), #365 ("archive stale epics", open 10 days, not draft), plus four automation-generated
   drafts (#371 weekly QA report 2026-08-24, #372/#363 delivery-metrics reports, #366/#373 dead-code
   sweeps) that are accurate snapshots of a moment now past and can be closed once superseded by
   this report and this week's sweep. #315 and #344 are the oldest (26 and 17 days) and both
   `draft` — worth an explicit decision (finish, or close) rather than continuing to age silently.

3. **`QA.md`'s test-count table is stale for a sixth consecutive review, and `ARCHITECTURE.md`'s
   own count — refreshed as recently as #361 (2026-08-16) — is already stale again.** `QA.md` still
   reads 647 unit / 99 suites / 1,215 total; actual is **669 / 104 / 1,278**. `ARCHITECTURE.md`
   reads "276 CI-script" (1,265 total); actual CI-script count is **289** (1,278 total) — a 13-test
   gap that opened in just over two weeks. This exact recommendation ("fold the refresh into the
   release-cut or PR-merge checklist") has now been made four reviews running.

4. **Reconcile the Node-version story — third review flagging this.** `.nvmrc`/`engines.node`
   still read `22.22.1` (lowered in #353, 2026-08-16) while `ci.yml` and
   `.devcontainer/devcontainer.json` both still pin and test **Node 24 only**, unchanged. Either add
   a Node-22 job to `ci.yml`, or raise the floor back to match what's actually tested.

5. **`auto-fix-ci.yml`'s "Find PR number" step can still hard-fail on a `gh api` non-2xx instead of
   degrading gracefully** — flagged in the unmerged PR #371's own audit on 2026-08-24, and still
   present on `main` this review (`.github/workflows/auto-fix-ci.yml` lines 30–44 have no fallback
   around the `gh api …/pulls` call). This is the one CI auto-fix automation exists specifically to
   handle — a failure in the failure-handler. Not covered by #367's fix. Worth a small follow-up PR.

---

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | Unchanged — functional style across 54 tracked JS source files. |
| Code grouped into themed files/modules | ✅ Met | No new modules this period; existing themed grouping (leaf modules, numbered feature files) unchanged and confirmed via `ARCHITECTURE.md`'s module map. |
| Main scripts import and run high-level functions | ✅ Met | Unchanged — `07-lifecycle.js`'s `DOMContentLoaded` handler calls only high-level init functions. |
| Low-level functions each carry out one specific task | ⚠️ Partial | `04-render.js` unchanged at **892** non-blank lines — fifth review running flagging it as the largest, least-split module. |
| Repetition minimised | ✅ Met | No new duplication introduced this period; the #362 fix generalised test-file discovery rather than special-casing it a second time. |
| Open for extension, closed for modification | ✅ Met | Unchanged — leaf-module import generation (`LEAF_MODULES` loop, added #361) continues to absorb new leaf modules without hand-copied blocks. |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `date-labels.js`'s `isToday`/`fmtLabel`, `claude-cli-workflow-guards.mjs`'s `discardsOAuthToken`/`toleratesNonZeroExit`/`revealsFile` (in PR #367, not yet merged but reviewed this period) are clear and descriptive. |
| Names are explicit rather than implicit | ⚠️ Partial | `grep -rPo '\([a-z]\) =>' src/js/` still counts exactly **294**, unchanged — seventh consecutive review flagging this, same top files as always (`14-jira.js` 22, `12-misc.js`/`11-timeflow.js` 20 each). |
| Code logic is clear and avoids unnecessary complexity | ⚠️ Partial | Files over 500 non-blank lines unchanged: `04-render.js` (892), `pure-fns-export.js` (745), `12b-changelog-data.js` (631), `03-timer.js`/`09-clock-weather.js`/`11-timeflow.js`/`06a-hero.js`. |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ Met | Verified directly: `npm run lint` → 0 errors, 0 warnings. |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | Unchanged; `npm run build` verified locally, `git status` clean after. |
| Packages follow a standard structure | ✅ Met | `package.json`/`package-lock.json` in sync, 0 `npm audit` vulnerabilities. `.nvmrc` matches `engines.node` but both still diverge from what CI/devcontainer actually test (Node 24) — see Priority 4. |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | This period's CHANGELOG entries (the credential-order root-cause writeup in PR #367, the `date-labels.js` extraction rationale) explain the specific motivating failure mode, not just the mechanical change. |
| Comments are up to date and not misleading | ✅ Met | Spot-checked the three modules touched this period (`02-utils.js` 305/305, `date-labels.js` 31/31 lines) — exact matches. `app-constants.js` remains 6 lines off (63 documented vs. 69 actual) — unchanged from last review, no commit has touched the file since #354, so this is a carried-over minor gap rather than fresh drift. |
| Code is not commented out to control which lines run | ✅ Met | No toggled-off code blocks found. |
| All functions and classes are documented | ✅ Met | Unchanged — last review's `localDate()`/`localMs()` gap remains fixed; no new undocumented exports found this period. |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project. |
| Auto-generated HTML documentation | ✅ Met | `npm run docs` verified locally, 0 errors. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`, unchanged; now also linked from `README.md`'s Project documentation table (#364, merged this period). |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ✅ Met | Unchanged from last review's promotion; #364 added the JSDoc reference-site link on top. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` present, unchanged this period. |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user. |
| QA extent clearly documented | ⚠️ Partial | `QA.md`'s "v1.9.0+ delta" table and `ARCHITECTURE.md`'s own test-count line are both stale — sixth consecutive review flagging this pattern. See Priority 3. |
| Assumptions documented next to code | ✅ Met | Unchanged. |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT), `CITATION.cff` present, unchanged. |
| Citation instructions given | ✅ Met | `CITATION.cff` present; its `version: 1.8.0` field remains stale relative to `v1.9.2` (pre-existing since PR #294, noted for completeness, not scored down). |
| Versioned releases (semantic versioning) | ✅ Met | `v1.9.2` remains the latest tag (`057b439`, 2026-08-14); no new release cut this period. |
| CHANGELOG available to users | ✅ Met | `CHANGELOG.md` current on `main`; `Unreleased` section documents this period's date-labels extraction, test split, and the (still-open) #367 fix write-up is ready to land with it. `test:changelog` passes, 0 near-duplicates. |
| Example usage documented | ✅ Met | JSDoc `@example` tags present on new pure functions and on PR #367's guard predicates. |
| Design certificates confirm compliance | ✅ Met | `ARCHITECTURE.md` design certificate is `1.9.2-r3`, dated 2026-08-16 — still current with the latest tagged release (`v1.9.2`, 2026-08-14); no structural change this period needed a refresh. |
| Software fully specified | ✅ Met | Unchanged — `date-labels.js` and `02-utils.js`'s updated entry both present and accurate in `ARCHITECTURE.md`. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Unchanged. |
| Code committed regularly, per discrete unit of work | ✅ Met | 9 commits landed on `main` since last review (2026-08-17 → 2026-08-28), each single-topic. |
| Appropriate branching strategy defined and used | ⚠️ Partial | **Regression** — see table above. Strategy is sound; the queue behind it isn't draining. |
| Code is open-sourced; sensitive data omitted | ✅ Met | Targeted credential-pattern grep this review found nothing. |
| Committing standards followed | ✅ Met | Unchanged — `test:commitlint` verified passing again this review. |
| Commits tagged at significant stages | ✅ Met | Unchanged — `v1.9.2` remains correctly tagged; no new release needed this period. |
| Continuous integration applied | ⚠️ Partial | **Regression** — see table above. |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | Confirmed via targeted grep this review. |
| Configuration in dedicated config file | ✅ Met | Unchanged. |
| Multiple config files per system/local/user | ✅ Met | Unchanged. |
| Config files versioned separately from analysis code | ✅ Met | Unchanged. |
| Config used to generate outputs is recorded | ✅ Met | Unchanged. |
| Example config templates provided | ✅ Met | `config.local.example.ps1` present with dummy values. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ✅ Met | `.lighthouserc.json` accessibility assertion unchanged; `lighthouse.yml` confirmed green on latest push. |
| Analysis data stored in an open format | ✅ Met | Unchanged. |
| Input data stored safely and treated as read-only | ✅ Met | Unchanged. |
| Input data versioned; changes create new versions | ✅ Met | Unchanged; no new fields required migration this period. |
| All input data documented in a data register | ✅ Met | `DATA.md` unchanged; no new persisted fields this period. |
| Outputs are disposable — regenerated at will | ✅ Met | `npm run build` verified to regenerate cleanly, `git status` clean after. |
| Non-sensitive data available so others can run the code | ✅ Met | Unchanged. |
| Data quality monitored | ✅ Met | Unchanged. |
| Fields in datasets documented | ✅ Met | `DATA.md` current. |
| Large or complex data stored in a database | ➖ N/A | localStorage appropriate for a single-user personal tool. |
| Data documented in information asset register | ✅ Met | `DATA.md` is the register, current. |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ✅ Met | **Improvement** — see table above. |
| Pair programming used | ✅ Met | Unchanged. |
| Users encouraged to participate in peer review | ⚠️ Partial | Unchanged — single-user tool limits wider participation. |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit/*.test.mjs` — **669 tests across 104 suites**, 0 failures (up from 659/102). |
| Tests run regularly and after every significant change | ✅ Met | `ci.yml`'s `test` job ran on all pushes since last review, all green. |
| Bug fixes add new unit tests | ✅ Met | PR #362's fix shipped with regression tests against the real `test/` layout; PR #367 (open) ships 31 new tests including the three real pre-fix workflow shapes as fixtures. |
| Informal tests recorded near the code | ✅ Met | Unchanged. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ Met | Unchanged. |
| Tests run automatically via CI or git hooks | ✅ Met | Unchanged. |
| Whole process tested end-to-end | ✅ Met | `smoke-tests.cjs` ran and passed locally this review (320/320). |
| Test code is clean and readable | ✅ Met | Unchanged; PR #367's guard tests use the actual shipped-broken shapes as fixtures rather than synthetic ones — good regression-test hygiene. |
| Formal user-acceptance testing conducted and recorded | ✅ Met | Same evidence as "whole process tested end-to-end" above. |
| Integration tests confirm multiple units | ✅ Met | `npm run test:scripts` — **289 tests across 59 suites** (up from 280/57), 0 failures. |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | Unchanged. |
| Required libraries documented including versions | ✅ Met | `package-lock.json` verified in sync via `npm ls --depth=0`. |
| Working OS environments documented | ⚠️ Partial | Third review flagging the Node-version gap. See Priority 4. |
| Example configuration files provided | ✅ Met | Unchanged. |
| Code runs independently of operating system | ✅ Met | Unchanged. |
| Dependencies managed separately for users/devs/testers | ✅ Met | 0 runtime dependencies; devDependencies only. |
| As few dependencies as possible | ✅ Met | No new dependencies added this period — only version bumps of existing devDependencies (sass, vite, eslint, globals, commitlint). |
| Package dependencies managed with environment manager | ✅ Met | `npm audit` → 0 vulnerabilities, unchanged. |
| Docker containers / VM builds version controlled | ✅ Met | `.devcontainer/devcontainer.json` present and version controlled; Node 24 pin internally consistent with `ci.yml`. |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ⚠️ Partial | **Regression** — see table above. |
| Code configuration recorded when code runs | ✅ Met | Unchanged. |
| Pipeline route recorded when decisions are made | ✅ Met | Unchanged. |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | Unchanged. |
| Issue tracker records development tasks | ✅ Met | Unchanged — this period's PRs reference numbered issues (#336). |
| New issues guided by user needs and stories | ✅ Met | Unchanged. |
| Issue templates ensure proper logging | ✅ Met | Unchanged. |
| Acceptance criteria noted and fulfilment recorded | ✅ Met | Unchanged. |
| QA standards and processes defined | ❌ Not met | **Regression** — see table above. Two of the project's own defined QA-automation workflows have been non-functional for 2+ weeks with a ready, unmerged fix. |

---

*Generated by an automated QA review session. Read-only — no source files were modified.*
