# QA Review — 2026-09-14

- Commit: `8d7d4e4` on `main` (audited from `claude/fervent-bardeen-2k5h6u`)
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated weekly QA review)

> **Environment note:** `npm ci` succeeded cleanly (Node `22.22.2` locally; `.nvmrc`/`engines.node`
> both read `22.22.1`; `ci.yml`'s `node22-compat` job now actually exercises that floor). Verified
> directly: `npm run build` → `script.js` 13,216 lines/47 files, `styles.css` 8,029 lines, `git
> status` clean after; `npm run lint` → **0 errors, 290 warnings** (all the `no-restricted-syntax`
> single-letter-arrow-param rule, `'warn'` by design); `npm run docs` (JSDoc) → 0 errors; `npm run
> test:unit` → **846/140 ✅** (up from 659/102); `npm run test:scripts` → **373/75 ✅** (up from
> 280/57); `smoke-tests.cjs` ran and passed **locally**, **310/310**, via
> `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH` pointed at this sandbox's pre-installed Chromium; `npm run
> test:commitlint` and `npm run test:changelog` → pass. `npm run test:actionlint` — the binary
> isn't pre-installed in this sandbox, so it was downloaded fresh from the pinned `v1.7.12` GitHub
> release, SHA256-verified against the exact checksum `ci.yml` pins, run via `ACTIONLINT_PATH`, then
> removed → clean, both regression fixtures still correctly fail. **`npm audit` reports 3
> vulnerabilities** (1 moderate — `colord`; 2 high — `fast-uri`, `js-yaml`) — was 0 at the last
> *merged* review (2026-08-17). See Regressions.
>
> **This review also read two weekly QA reviews that ran in the four-week gap since 2026-08-17 but
> never reached `docs/qa-reports/`:** 2026-08-31 (PR #376) and 2026-09-07 (PR #396) both genuinely
> ran, are well-evidenced, and are still sitting open today as draft PRs. Per the skill's own
> instruction, this review's scored comparison is against the last **merged** report (2026-08-17),
> not these drafts; their findings are credited below rather than re-presented as newly discovered.
> The pattern they both flagged — reviewed, CI-green PRs sitting unmerged — has not just persisted,
> it has grown sharply: **19 PRs are open today** (10 on 2026-09-07, none of the growth explained by
> new same-day work), the oldest (**#344**) now **31 days** old. See Priority 1.

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  74   |
| ⚠️ Partial    |   9   |
| ❌ Not met    |   0   |
| ➖ N/A        |   4   |

**Total items assessed: 87** — down from 76 Met / 7 Partial at the 2026-08-17 baseline. Three
long-standing gaps closed for real this period (the `04-render.js` split, the Node-22 CI job, and
the `localDate()`/`localMs()` JSDoc gap), but four new problems opened: a regrown dependency
vulnerability, a `04-render.js`-documentation-style drift now recurring across three separate
modules, and — the one doing the most damage — a PR backlog that has gone from a footnote to the
dominant fact about this repository's process health.

---

## Regressions since last review (2026-08-17)

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| Appropriate branching strategy defined and used | ✅ | ⚠️ | **19 PRs open today**, more than tripled from 6-7 at the last checked-in state. `#344` ("model `safeCssColor` as an html-injection barrier") has been open since 2026-08-14 — **31 days**, still `draft`. Six more (`#376`–`#378`, this project's own 2026-08-31 report/dead-code/delivery-metrics PRs) have sat untouched for 14 days. Most concerning: `#401` (fixes the npm audit vulnerabilities below), `#402` (fixes a live UI bug — see Peer review), `#403` (fixes the stale test-count tables below) and `#404` (tightens the single-letter-arrow lint rule) are all reviewed, CI-passing, `/pr-review`-approved fixes for problems this very report would otherwise flag as fresh — and have sat unmerged for **5 days**. The branch→PR→review pipeline works correctly for everything that lands; the queue behind it has stopped draining. |
| Package dependencies managed with an environment manager (`npm audit`) | ✅ | ⚠️ | `npm audit` reports **3 vulnerabilities** (1 moderate: `colord`; 2 high: `fast-uri`, `js-yaml`), all transitive dev-only dependencies of `stylelint`/`commitlint`'s shared `ajv`. Was 0 at the 2026-08-17 baseline. **A fix already exists and is sitting unmerged**: PR `#401` (`npm audit fix`, no `--force`, lockfile-only) was opened 2026-09-09, is `mergeable_state: clean`, and resolves all three — it just hasn't been merged. |
| Comments are up to date and not misleading | ✅ | ⚠️ | The per-module line-count drift flagged as a single, minor, brand-new-module exception last review (`app-constants.js`, 63 documented vs. 69 actual) is now a **recurring pattern across three modules**: `app-constants.js` is still off by 6, unchanged since 2026-08-17 despite being named explicitly; the newer `signifiers.js` extraction (#408) is off by 1 (46 documented vs. 47 actual — the JSDoc-fix follow-up commit added a line the doc wasn't refreshed for); `12b-changelog-data.js` (#406) is off by 2 (637 vs. 635). None are large individually, but three modules drifting inside days of being documented, one of them unfixed for four straight weeks, is a pattern rather than an outlier. |
| QA standards and processes defined | ✅ | ⚠️ | The scheduled `dead-code.yml`/`delivery-metrics.yml` workflows *do* now run weekly and successfully (confirmed: 2026-08-31 and 2026-09-07 runs both green) — a real improvement over the total silence the 2026-08-17 baseline implicitly accepted. But their output is not reaching `main`: the 2026-08-24 weekly QA report PR (#371) was closed unmerged, and its 2026-08-31 (#376) and 2026-09-07 (#396) successors are both still open, 14 and 7 days respectively. The same is true of the dead-code/delivery-metrics report PRs from the same weeks (#377, #378, #393, #397, #398, #410). A defined process whose own outputs pile up unreviewed for weeks is not functioning as a process, whatever the workflow YAML says. |

---

## Improvements since last review

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| Low-level functions and classes each carry out one specific task | ⚠️ | ✅ | `04-render.js` — flagged as the largest, least-split module for four consecutive reviews (892 non-blank lines) — was split by PR #386 into `04-render.js` (56 lines, orchestration only) plus four themed modules. Confirmed directly: `grep -c . src/js/04-render.js` → 56, matching `ARCHITECTURE.md`'s documented count exactly. |
| Working operating-system environments documented | ⚠️ | ✅ | PR #382 added a `node22-compat` job to `ci.yml` that reads `.nvmrc` itself (`node-version-file`) and runs build/lint/unit/CI-script tests against it. Confirmed green on the current HEAD's CI run (`8d7d4e4`, job "Node 22 compatibility (matches .nvmrc floor)", conclusion `success`). Closes a gap flagged for three consecutive prior reviews. |
| All functions and classes are documented | ⚠️ | ✅ | The 2026-08-17 regression (`test/unit/_helpers.mjs`'s `localDate()`/`localMs()` shipped with zero JSDoc) was fixed the same week by PR #360. No new undocumented exported functions found this review. |

---

## Priorities for this week

1. **Clear the PR backlog — it is now the single largest risk to this repository's process
   integrity.** 19 PRs are open. In order of what's cheapest to unblock: merge `#401` (one-line
   `npm audit fix`, already green, fixes Regression #2 above), `#403` (docs-only test-count refresh,
   fixes Priority 3 below), `#402` (one-line UI-text fix for a live bug — see Priority 2), and `#404`
   (lint-rule tightening). Then triage the automation backlog: `#376`/`#377`/`#378` (2026-08-31) and
   `#393` (2026-09-04) and `#396`/`#397`/`#398` (2026-09-07) and `#410` (2026-09-11) are all
   superseded weekly-report snapshots that can be closed once this report lands, rather than merged.
   `#344` (31 days old, still draft/unstable) needs an explicit decision — finish it or close it —
   rather than continued silence. Left unaddressed, this is the exact condition that let
   `dead-code.yml`/`scss-audit.yml` break silently for two-plus weeks in August (per the 2026-08-31
   and 2026-09-07 interim reviews): a backlog nobody is watching hides the next real outage inside
   it.

2. **A second (now third, counting the still-open fix) confirmed instance of "own review says
   `REQUEST CHANGES` or flags a 🔴 blocking issue, merges anyway, finding ships to `main` unfixed."**
   PR #385 ("Make epic archiving reversible") shipped a confirm-dialog string —
   `src/js/02-utils.js:62`, `'...you can restore any of them from ⚙ → archived.'` — that references
   a control the same PR deleted (replaced by the `🗂 epics` toolbar button). Confirmed still present
   on `main` today, verbatim. A fix exists (`#402`, one line, open since 2026-09-09) but hasn't
   merged. This is the same failure mode flagged for PR #355 in the 2026-08-17 report: the review
   dialogue works, but nothing gates the merge on its own findings. Merge `#402`, and revisit whether
   `merge-gate` should also block on Claude's own `REQUEST CHANGES`/🔴 verdicts, not only ChatGPT's
   unresolved threads.

3. **`QA.md`/`ARCHITECTURE.md`'s test-count tables are stale by the widest margin recorded in this
   series, and for at least a ninth consecutive review.** Both still read 698 unit / 320 smoke / 324
   CI-script = 1,342 total (dated "as of 2026-09-01"); actual today is **846 unit / 310 smoke / 373
   CI-script = 1,529 total** — a 187-test, 14% gap. (Smoke count *dropped* 320→310 because PR #399
   correctly removed the "build bridge" banner's smoke-test section along with the banner — a real
   decrease, not a coverage loss.) A fix is already open and unmerged: `#403`. The recommendation to
   fold this refresh into the release-cut or PR-merge checklist has now been made in every review
   since 2026-08-03 and still hasn't been adopted.

4. **`(a) =>` single-letter-arrow-param density is essentially unchanged**: `grep -rPo
   '\([a-z]\) =>' src/js/` counts **289**, down only 5 from 294 at the 2026-08-17 baseline despite a
   `'warn'`-severity lint rule (PR #384) built specifically to stop growth. A follow-up PR (`#404`,
   open 5 days) proposes ratcheting the rule so it can't regrow even if the existing pile isn't
   renamed — worth merging.

5. **Reconfirm whether `main`'s design certificate needs a refresh.** `ARCHITECTURE.md`'s
   certificate reads "Covers app version v1.9.2 (main, 2026-09-01)" — 13 days and roughly a dozen
   merged PRs behind current `main` (2026-09-10, `#408`). No release has been cut in that window
   (still `v1.9.2`), so this isn't scored down, but the gap is wide enough to flag before it becomes
   one.

---

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | Unchanged — functional style across 59 tracked JS source files. |
| Code grouped into themed files/modules | ✅ Met | Two more leaf-module extractions landed this period: `signifiers.js` (#408) and `12b-changelog-data.js` (#406), both themed, single-purpose, documented in `ARCHITECTURE.md`. |
| Main scripts import and run high-level functions | ✅ Met | Unchanged — `07-lifecycle.js`'s `DOMContentLoaded` handler calls only high-level init functions. |
| Low-level functions each carry out one specific task | ✅ Met | **Improvement** — see table above. `04-render.js` reduced from 892 to 56 lines across a 5-file split (PR #386), confirmed by direct line count. |
| Repetition minimised | ✅ Met | The `12b-changelog-data.js`/`signifiers.js` extractions reuse the `LEAF_MODULES`-driven import generation added for earlier extractions rather than hand-copying build logic. |
| Open for extension, closed for modification | ✅ Met | Same evidence as above — a 4th leaf module needed no changes to `build.js`/`vite.config.js` beyond a registry entry. |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `signifiers.js`'s `sigSymbol()`/`sigTitle()` and `12b-changelog-data.js`'s `STORE_DEV_LOG`/`TEST_AREA_NAMES` remain clear and descriptive. |
| Names are explicit rather than implicit | ⚠️ Partial | `grep -rPo '\([a-z]\) =>' src/js/` counts **289**, down only 5 from 294 at the last review despite a dedicated `'warn'`-severity lint rule. See Priority 4. |
| Code logic is clear and avoids unnecessary complexity | ⚠️ Partial | Files over 500 non-blank lines, largely unchanged: `pure-fns-export.js` (745, largest single file — a split is proposed in open PR #409), `12b-changelog-data.js` (635), `03-timer.js`, `09-clock-weather.js`, `11-timeflow.js`, `06a-hero.js`, `02-utils.js` all still over the threshold. `04-render.js` is resolved (see Improvements) but the underlying pattern persists elsewhere. |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ Met | Verified directly: `npm run lint` → 0 errors, 290 warnings (all the known single-letter-arrow-param rule). |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | Unchanged; `npm run build` verified locally, `git status` clean after. |
| Packages follow a standard structure | ✅ Met | `package.json`/`package-lock.json` in sync (`npm ls --depth=0`, no drift beyond the flagged audit findings). `.nvmrc` matches `engines.node`, now with a dedicated CI job exercising that floor. |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | This period's commit messages (e.g. #392's `GetDefaultFolder(9)` magic-number rationale, #386's per-file split rationale) explain the specific motivating constraint, not just the mechanical change. |
| Comments are up to date and not misleading | ⚠️ Partial | **Regression** — see table above. Per-module line-count drift now spans three modules (`app-constants.js`, `signifiers.js`, `12b-changelog-data.js`), one unfixed for four consecutive reviews. |
| Code is not commented out to control which lines run | ✅ Met | No toggled-off code blocks found. |
| All functions and classes are documented | ✅ Met | **Improvement** — see table above. `localDate()`/`localMs()` JSDoc gap fixed by PR #360; no new undocumented exports found. |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project. |
| Auto-generated HTML documentation | ✅ Met | `npm run docs` verified locally, 0 errors. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`, linked from README since PR #364. |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ✅ Met | Unchanged; new config from PR #392 (`$CalendarExcludeNames`) documented. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` present; `CONTRIBUTING.md` updated with the `session:claim`/`session:check` workflow (PR #368). |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user. |
| QA extent clearly documented | ⚠️ Partial | Widest gap yet in this series — see Priority 3. `QA.md`/`ARCHITECTURE.md` read 1,342 total tests; actual is 1,529. A fix (`#403`) is open and unmerged. |
| Assumptions documented next to code | ✅ Met | `session-guard.mjs`'s JSDoc documents its age-based-liveness assumption explicitly. |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT), `CITATION.cff` present, unchanged. |
| Citation instructions given | ✅ Met | `CITATION.cff` present; `version: 1.8.0`/`date-released: 2026-05-25` remain stale relative to `v1.9.2` — pre-existing since PR #294, noted for completeness, not scored down. |
| Versioned releases (semantic versioning) | ✅ Met | `v1.9.2` remains the latest tag; no new release cut this period. |
| CHANGELOG available to users | ✅ Met | `CHANGELOG.md` current on `main`; `test:changelog` passes, 0 near-duplicates across 188 entries. |
| Example usage documented | ✅ Met | JSDoc `@example` tags present on new pure functions; PowerShell comment-based help on PR #392's new functions. |
| Design certificates confirm compliance | ✅ Met | See Priority 5 — current (`v1.9.2`, no release since), but 13 days/a dozen PRs behind `main`'s actual head. |
| Software fully specified | ✅ Met | `ARCHITECTURE.md` module map includes both new leaf-module extractions and the `04-render.js` split's four new files. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Unchanged. |
| Code committed regularly, per discrete unit of work | ✅ Met | 30 commits landed on `main` since 2026-08-17, each single-topic (per-PR squash merges). |
| Appropriate branching strategy defined and used | ⚠️ Partial | **Regression** — see table above. The strategy itself is followed correctly for everything that merges; the queue behind it has stopped draining. |
| Code is open-sourced; sensitive data omitted | ✅ Met | Targeted credential-pattern grep across `src/`, `.github/scripts/`, and root scripts this review found nothing. |
| Committing standards followed | ✅ Met | `test:commitlint` self-test passes. |
| Commits tagged at significant stages | ✅ Met | `v1.9.2` remains correctly tagged; no new release needed this period. |
| Continuous integration applied | ✅ Met | `ci.yml` green on current HEAD (`8d7d4e4`, all 4 jobs). Two transient "Install Playwright browsers" step failures on 2026-09-09 (runs for `64c4e7a`/`0036b28`) self-resolved on the next push with no code change — an infrastructure flake, not scored down, but worth a retry step if it recurs. |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | Confirmed via targeted grep this review. |
| Configuration in dedicated config file | ✅ Met | Unchanged. |
| Multiple config files per system/local/user | ✅ Met | Unchanged. |
| Config files versioned separately from analysis code | ✅ Met | Unchanged. |
| Config used to generate outputs is recorded | ✅ Met | PR #392's `$CalendarExcludeNames` default is logged at startup. |
| Example config templates provided | ✅ Met | `config.local.example.ps1` present with dummy values, updated for the calendar-exclusion setting. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ✅ Met | `.lighthouserc.json` accessibility assertion unchanged; `lighthouse.yml` green on current HEAD. |
| Analysis data stored in an open format | ✅ Met | Unchanged. |
| Input data stored safely and treated as read-only | ✅ Met | Unchanged. |
| Input data versioned; changes create new versions | ✅ Met | Unchanged; no new persisted fields required migration this period. |
| All input data documented in a data register | ✅ Met | `DATA.md` unchanged; `wl_reflection_v1` correctly marked "(retired)" after PR #400 removed the feature but kept the stored data. |
| Outputs are disposable — regenerated at will | ✅ Met | `npm run build` verified to regenerate cleanly, `git status` clean after. |
| Non-sensitive data available so others can run the code | ✅ Met | Unchanged. |
| Data quality monitored | ✅ Met | Unchanged. |
| Fields in datasets documented | ✅ Met | `DATA.md` current. |
| Large or complex data stored in a database | ➖ N/A | localStorage appropriate for a single-user personal tool. |
| Data documented in information asset register | ✅ Met | `DATA.md` is the register, current. |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ⚠️ Partial | The 2026-08-17 instance (PR #355) is resolved. A **new instance replaced it**: PR #385's own recorded review flagged a 🔴 blocking finding (a stale confirm-dialog string pointing users at a deleted control); it merged unfixed, and the string is still on `main` today — see Priority 2. A fix is open (`#402`) but unmerged. |
| Pair programming used | ✅ Met | Unchanged. |
| Users encouraged to participate in peer review | ⚠️ Partial | Unchanged — single-user tool limits wider participation. |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit/*.test.mjs` — **846 tests across 140 suites**, 0 failures (up from 659/102). |
| Tests run regularly and after every significant change | ✅ Met | `ci.yml`'s `test` job ran on all pushes since last review; green on current HEAD. |
| Bug fixes add new unit tests | ✅ Met | PR #392 (calendar store-type fix) shipped 15 new regression tests; PR #390/#385 (epic archiving/hard-delete) each shipped dedicated coverage. |
| Informal tests recorded near the code | ✅ Met | Unchanged. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ Met | Unchanged. |
| Tests run automatically via CI or git hooks | ✅ Met | Unchanged. |
| Whole process tested end-to-end | ✅ Met | `smoke-tests.cjs` ran and passed locally, **310/310**, re-verified via the Playwright escape hatch. |
| Test code is clean and readable | ✅ Met | New test files (`signifiers.test.mjs`, `changelog-data.test.mjs`) mirror the one-file-per-feature-area convention established by #355's split. |
| Formal user-acceptance testing conducted and recorded | ✅ Met | Same evidence as "whole process tested end-to-end" above. |
| Integration tests confirm multiple units | ✅ Met | `npm run test:scripts` — **373 tests across 75 suites** (up from 280/57), 0 failures. |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | Unchanged. |
| Required libraries documented including versions | ✅ Met | `package-lock.json` verified in sync via `npm ls --depth=0`. |
| Working OS environments documented | ✅ Met | **Improvement** — see table above. |
| Example configuration files provided | ✅ Met | Unchanged. |
| Code runs independently of operating system | ✅ Met | Unchanged. |
| Dependencies managed separately for users/devs/testers | ✅ Met | 0 runtime dependencies; devDependencies only. |
| As few dependencies as possible | ✅ Met | No new direct dependencies added this period — only version bumps and reuse of already-present transitives. |
| Package dependencies managed with environment manager | ⚠️ Partial | **Regression** — see table above. `npm audit` → 3 vulnerabilities (was 0); fix open and unmerged (`#401`). |
| Docker containers / VM builds version controlled | ✅ Met | `.devcontainer/devcontainer.json` present and version controlled; still pins Node 24, internally consistent with `ci.yml`'s primary jobs. |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ✅ Met | Unchanged — the #350 `save()`-failure banner and #367's OAuth-ordering fix both remain in place and verified working. |
| Code configuration recorded when code runs | ✅ Met | PR #392's calendar-exclusion config is logged at startup. |
| Pipeline route recorded when decisions are made | ✅ Met | `tidyStaleEpics()`/`renderEpicsManager()` log archive/restore decisions via `wlLog.info`. |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | Unchanged. |
| Issue tracker records development tasks | ✅ Met | Unchanged — this period's PRs reference numbered issues (#268, #328, #336, #388). |
| New issues guided by user needs and stories | ✅ Met | Unchanged. |
| Issue templates ensure proper logging | ✅ Met | Unchanged. |
| Acceptance criteria noted and fulfilment recorded | ✅ Met | Unchanged. |
| QA standards and processes defined | ⚠️ Partial | **Regression** — see table above. The scheduled automation itself runs correctly and weekly; its outputs are not reaching `main`. |

---

*Generated by an automated QA review session. Read-only — no source files were modified.*
