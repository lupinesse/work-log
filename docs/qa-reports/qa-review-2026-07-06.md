# QA Review — 2026-07-06

- Commit: `c2983b2` on `claude/fervent-bardeen-2admbf`
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated weekly QA review)

> **Environment note:** `npm run lint` (ESLint) and `npm run build` fail in this environment —
> `node_modules/` contains only a single stub entry (`Cannot find package '@eslint/js'`,
> `Cannot find package 'sass'`), so no devDependency other than the package manager itself is
> installed. `smoke-tests.cjs` cannot run (Playwright not installed). Unit tests
> (`npm run test:unit` → **474/74** ✅) and CI-script tests (`npm run test:scripts` → **171/32** ✅)
> execute cleanly since they use only Node's built-in test runner. Items that depend on lint,
> build, or Playwright are marked where CI is the authoritative source, consistent with previous
> reviews. Runtime is Node `22.22.2`; `.nvmrc` and `engines.node` both now pin `24.15.0` — the
> mismatch itself is no longer a repo defect (see Improvements), only a residual local-environment
> constraint.

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  77   |
| ⚠️ Partial    |   6   |
| ❌ Not met    |   0   |
| ➖ N/A        |   4   |

**Total items assessed: 87** — net +2 ✅ vs last review (2026-06-29 / `1c9fa58`), driven by PR
#254 (QA-fix commit closing three 2026-06-29 priorities) landing since the last review.

---

## Regressions since last review (2026-06-29 / `1c9fa58`)

None found. Every item that was ✅ last week is still ✅, and one further item improved (below).

---

## Improvements since last review

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| Comments are up to date and not misleading | ⚠️ | ✅ | #254 fixed the `ARCHITECTURE.md` overview file count: now reads "49 source files", matching `ls src/js/ \| wc -l` = **49**. All non-blank line-count annotations spot-checked (`04-render.js` 646, `12b-changelog-data.js` 631, `09-clock-weather.js` 565, `11-timeflow.js` 536, `06a-hero.js` 512) remain accurate. |
| Names are explicit rather than implicit | ⚠️ | ⚠️ (improved, not resolved) | #254 renamed a batch of single-letter lambda arguments. `grep -rPo '\([a-z]\) =>' src/js/` now counts **280** occurrences, down from 426 last week (-34%). Highest-density files today: `14-jira.js` (22), `12-misc.js` (20), `11-timeflow.js` (20), `06a-hero.js` (19). Still Partial — remains on the priority list. |
| Package/environment version alignment | ⚠️ | ✅ | #254 bumped `engines.node` in `package.json` to `>=24.15.0`, now matching `.nvmrc` (`24.15.0`) exactly. Previously flagged mismatch (`>=22.12.0` vs `24.15.0`) is resolved. |

---

## Priorities for this week

1. **Tag v1.9.1** — `v1.9.0` was tagged 2026-06-12; `git tag -l` still shows no later tag. At
   least 5 more PRs (#247–#256) have merged since, including two user-facing fixes now sitting in
   `CHANGELOG.md`'s `## Unreleased` section (backup retention for all time-series arrays; tabbed
   board full-width layout). This has been flagged for two consecutive reviews — a patch tag is
   overdue and cheap to cut.

2. **Continue reducing single-letter lambda arguments** — 280 occurrences remain
   (`14-jira.js` 22, `12-misc.js` 20, `11-timeflow.js` 20, `06a-hero.js` 19, `16-rapid.js` 15,
   `12a-changelog.js` 15, `02-utils.js` 15, `13-calendar.js` 14). #254 made real progress
   (-146 from 426); continuing the sweep into `14-jira.js` and `12-misc.js` next would be the
   highest-density remaining targets.

3. **Refresh `QA.md`'s v1.9.0+ delta table** — it still reads "466 tests across 73 suites" /
   "957 tests total (466 unit + 320 smoke + 171 CI-scripts)" dated "as of 2026-06-26". Actual
   unit count is now **474 tests / 74 suites** (the `+8`/`+1` come from the 7 new
   `buildBackupPayload()` tests in #255 plus one more). The table is explicitly dated so this
   is not misleading, but it is one release behind — refresh it alongside the v1.9.1 tag.

4. **Split large modules** — unchanged since 2026-06-29: `04-render.js` (646 non-blank lines),
   `12b-changelog-data.js` (631, data file — acceptable), `09-clock-weather.js` (565),
   `11-timeflow.js` (536), `06a-hero.js` (512) all exceed comfortable single-responsibility
   thresholds. No split has been attempted in the last two review periods.

5. **Resolve local-environment dependency gap** — `node_modules/` in this review environment
   contains no installed devDependencies beyond a single stub (`Cannot find package '@eslint/js'`,
   `Cannot find package 'sass'`), so `npm run lint`, `npm run build`, and Playwright-based smoke
   tests cannot be verified locally, only through CI. This is an environment-provisioning issue,
   not a code defect — but it has now blocked local lint/build verification for at least three
   consecutive reviews. Worth confirming the review environment runs `npm ci` before future audits.

---

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | 49 JS source files in named-function functional style; no class inheritance — appropriate for app size. |
| Code grouped into themed files/modules | ✅ Met | Numbered-prefix convention maintained; no new module-boundary regressions since #248's dead-code sweep. |
| Main scripts import and run high-level functions | ✅ Met | `07-lifecycle.js` `DOMContentLoaded` handler calls only high-level init functions; no inline business logic. |
| Low-level functions each carry out one specific task | ⚠️ Partial | Largest modules by non-blank line count unchanged from last review: `04-render.js` (646), `12b-changelog-data.js` (data file, acceptable), `09-clock-weather.js` (565), `11-timeflow.js` (536), `06a-hero.js` (512). No split attempted this period. |
| Repetition minimised | ✅ Met | Shared utilities in `pure-fns-{format,export,tasks,validate}.js` and `02-utils.js`; #255's `buildBackupPayload()` centralises retention logic that was previously duplicated per-array. |
| Open for extension, closed for modification | ✅ Met | `01b-migrate.js` migrations append-only; `wl_*_v1` key pattern extends storage without touching existing keys. |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `buildBackupPayload`, `buildRollingSummary`, `mergeAdjacentEntries`, `locationFor` — self-documenting names throughout, including #255's new function. |
| Names are explicit rather than implicit | ⚠️ Partial | `grep -rPo '\([a-z]\) =>' src/js/` now counts **280** (down from 426 in #254's sweep). Remaining density: `14-jira.js` (22), `12-misc.js` (20), `11-timeflow.js` (20), `06a-hero.js` (19), `16-rapid.js` (15). Improving but not resolved. |
| Code logic is clear and avoids unnecessary complexity | ⚠️ Partial | Four modules above 500 non-blank lines with mixed concerns remain (see modular-code finding). Unchanged. |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ Met | Cannot run `npm run lint` locally this review (devDependencies not installed in this environment — see priority 5). Last verifiable state: 0 errors, 0 warnings (confirmed `eslint-disable-next-line prefer-const` comment still present and accurate at `10a-tasks-render.js:306`). CI `ci.yml` is authoritative and has passed on all recent pushes including #256's own CI-workflow fix. |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | `src/`, `dist/` (untracked), `test/`, `docs/`, `.github/`, `scripts/`. Build artefacts not committed; regenerable via `npm run build`. |
| Packages follow a standard structure | ✅ Met | `package.json` + `package-lock.json` committed. `.nvmrc` (`24.15.0`) now matches `engines.node` (`>=24.15.0`) exactly — mismatch from last review resolved by #254. |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | `// Assumption:` blocks in `pure-fns.js`, `05-entries.js`, `07-lifecycle.js`; #256's PR description documents the non-obvious `secrets` context restriction in workflow `if:` conditions, mirrored as a comment in the workflow YAML. |
| Comments are up to date and not misleading | ✅ Met | `ARCHITECTURE.md` overview now reads "49 source files", matching `ls src/js/ \| wc -l` = 49 (fixed in #254; previously off-by-one). Spot-checked line-count annotations for the five largest modules all still match `grep -c .` exactly. Previous ⚠️ resolved. |
| Code is not commented out to control which lines run | ✅ Met | No toggled-off code blocks in `src/js/`. |
| All functions and classes are documented | ⚠️ Partial | Cannot run `npm run docs` locally (devDependencies not installed). CI `jsdoc-check.yml` is the authoritative source; all recent PRs (#247–#256) have passed. Unchanged from previous reviews. |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project. |
| Auto-generated HTML documentation | ⚠️ Partial | `docs.yml` deploys on push to `main` via GitHub Pages. Cannot verify locally. All recent PRs passed `docs.yml`. Unchanged. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`; README documentation index links to hosted docs. |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ✅ Met | README current — kanban board, Today's Flow, Rolling Summary, all main features; setup/build/test sections accurate. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` present. |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user. |
| QA extent clearly documented | ⚠️ Partial | `QA.md` v1.9.0+ delta table (added in #247) is now one release stale: it states "466 tests across 73 suites" / "957 tests total", dated "as of 2026-06-26", while the actual count is **474 unit tests / 74 suites** (957 → 965 total) after #255 added 7 tests. The table is explicitly dated, so it is not misleading, but it has not been refreshed alongside the two PRs merged since. See priority 3. |
| Assumptions documented next to code | ✅ Met | `// Assumption:` comments in `pure-fns.js`, `05-entries.js`, `07-lifecycle.js`; `DATA.md` documents schema assumptions for all documented keys. |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT); `CITATION.cff` carries `license-expression`. |
| Citation instructions given | ✅ Met | `CITATION.cff` with `preferred-citation`; README cite section. |
| Versioned releases (semantic versioning) | ⚠️ Partial | `v1.9.0` tagged 2026-06-12; no later tag exists (`git tag -l` confirms). 5+ PRs merged since (#247–#256) including two user-facing fixes in `CHANGELOG.md`'s Unreleased section. Flagged for a second consecutive review — see priority 1. |
| CHANGELOG available to users | ✅ Met | `CHANGELOG.md` current; Unreleased section documents both the backup-retention fix (#255) and the tabbed-board width fix, each with clear rationale. |
| Example usage documented | ✅ Met | README usage examples; JSDoc `@example` on core pure functions. |
| Design certificates confirm compliance | ✅ Met | `ARCHITECTURE.md` certificate: `v1.9.0-r2`, last reviewed 2026-06-26, status "Approved". No architecturally significant changes since (#248, #254–#256 are dead-code/QA/CI fixes) that would require a cert bump. |
| Software fully specified | ✅ Met | `ARCHITECTURE.md` module map present for all 49 modules; overview file count now accurate (see Code documentation). `DATA.md` field dictionary present. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Git repo; 250+ commits; remote `lupinesse/work-log`. |
| Code committed regularly, per discrete unit of work | ✅ Met | 4 commits since last review (#248, #255, #254, #256), each a single-topic squash commit with a clear conventional message. |
| Appropriate branching strategy defined and used | ✅ Met | All recent commits went through PRs (`claude/`-prefixed branches). No direct-to-main commits found. |
| Code is open-sourced; sensitive data omitted | ✅ Met | MIT licence; no credentials in tracked files; `config.local.ps1` and `00-config.local.js` gitignored. |
| Committing standards followed | ⚠️ Partial | All recent commits follow conventional-commit convention (commitlint + Husky). Pre-commitlint history remains. Unchanged. |
| Commits tagged at significant stages | ⚠️ Partial | `v1.9.0` tagged 2026-06-12; no patch tag since despite 5+ merged PRs. Same underlying gap as "Versioned releases" above — see priority 1. |
| Continuous integration applied | ✅ Met | 16 GitHub Actions workflow files active (`.github/workflows/`); `ci.yml` runs `npm test` and `npm run test:scripts` on every push. #256 fixed a latent bug where `review` and `a11y-audit` jobs hard-failed on every PR when the Claude Reviewer App wasn't configured — CI signal is now trustworthy again. |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | Anthropic key server-side only; local config gitignored. |
| Configuration in dedicated config file | ✅ Met | `src/js/00-config.js`; personal overrides in gitignored `00-config.local.js`. |
| Multiple config files per system/local/user | ✅ Met | Base + user override + server (`config.local.ps1`). |
| Config files versioned separately from analysis code | ✅ Met | `00-config.js` committed independently; overrides untracked. |
| Config used to generate outputs is recorded | ✅ Met | `scripts/config-snapshot.cjs`; `wlLog.config()` at startup. |
| Example config templates provided | ✅ Met | `src/js/00-config.local.example.js` and `config.local.example.ps1`, dummy values only. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ✅ Met | `.lighthouserc.json` accessibility assertion at `"error"` ≥ 0.85. CI fails on any drop. |
| Analysis data stored in an open format | ✅ Met | All data JSON (localStorage + backup export). |
| Input data stored safely and treated as read-only | ✅ Met | Backup merge only adds entries with unseen IDs — existing records never mutated. |
| Input data versioned; changes create new versions | ✅ Met | `01b-migrate.js` append-only migration log; versioned `wl_*_v1` keys. |
| All input data documented in a data register | ✅ Met | `DATA.md` documents input keys and schema. |
| Outputs are disposable — regenerated at will | ✅ Met | `npm run build` regenerates `script.js`, `styles.css`, `dist/`; none committed. |
| Non-sensitive data available so others can run the code | ✅ Met | Self-contained fixtures in `test/unit.mjs`; no personal data in repo. |
| Data quality monitored | ✅ Met | `wlLog.warn()` on `JSON.parse` failures in all `load*()` functions; #255 adds a per-array excluded-record count logged at export time. |
| Fields in datasets documented | ✅ Met | `DATA.md` field dictionary present and current. |
| Large or complex data stored in a database | ➖ N/A | localStorage appropriate for a single-user personal tool. |
| Data documented in information asset register | ✅ Met | `DATA.md` is the register. |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ✅ Met | #254, #255, #256 all merged as PRs (not direct-to-main commits), consistent with the four-phase ChatGPT/Claude review pipeline used on prior PRs. |
| Pair programming used | ✅ Met | `CONTRIBUTING.md` documents AI-pair model; `CITATION.cff` credits Claude Code. |
| Users encouraged to participate in peer review | ⚠️ Partial | Templates invite contribution; single-user tool limits wider participation. Unchanged. |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit.mjs` — **474 tests across 74 suites, 0 failures** (up from 466/73 last review; #255 added 7 tests for `buildBackupPayload()`). |
| Tests run regularly and after every significant change | ✅ Met | `ci.yml` `test` job runs `npm test` on every push. |
| Bug fixes add new unit tests | ✅ Met | #255 (backup-retention fix) ships with 7 new regression tests for `buildBackupPayload()`; CHANGELOG documents the added coverage explicitly. |
| Informal tests recorded near the code | ✅ Met | JSDoc `@example` tags on core pure functions in `pure-fns.js`. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ Met | `QA.md` sign-off tables per release (delta table stale by one release — see "QA extent" finding above, not a sign-off gap). |
| Tests run automatically via CI or git hooks | ✅ Met | `ci.yml` full suite; lint-staged pre-commit via Husky. |
| Whole process tested end-to-end | ✅ Met | `smoke-tests.cjs` Playwright suite (cannot run locally this review — Playwright not installed). CI `ci.yml` is authoritative. |
| Test code is clean and readable | ✅ Met | `freshPage()` helper, fixtures, and parameterised cases in both test files. |
| Formal user-acceptance testing conducted and recorded | ✅ Met | `QA.md` UAT process and outcomes per release. |
| Integration tests confirm multiple units | ✅ Met | `npm run test:scripts` — **171 tests across 32 suites, 0 failures**, unchanged from last review. |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | Example config files document all secrets with placeholders. |
| Required libraries documented including versions | ✅ Met | `package-lock.json` committed; exact version pins. No dependency bumps since last review. |
| Working OS environments documented | ✅ Met | README (Windows primary), `launch.sh`, `.devcontainer/devcontainer.json`. |
| Example configuration files provided | ✅ Met | `src/js/00-config.local.example.js`, `config.local.example.ps1`. |
| Code runs independently of operating system | ✅ Met | `path.join()` throughout `build.js` and related scripts. |
| Dependencies managed separately for users/devs/testers | ✅ Met | 0 runtime dependencies; devDependencies only. |
| As few dependencies as possible | ✅ Met | Each devDependency justified by a named npm script or CI workflow; no additions since last review. |
| Package dependencies managed with environment manager | ✅ Met | npm + lockfile + `.nvmrc` (`24.15.0`), now matching `engines.node` exactly (resolved this review — see Improvements). |
| Docker containers / VM builds version controlled | ✅ Met | `.devcontainer/devcontainer.json` (javascript-node:1-24-bookworm). |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ✅ Met | `wlLog.error()`/`warn()` on parse failures, missing DOM, and API errors throughout `src/js/`. |
| Code configuration recorded when code runs | ✅ Met | `wlLog.config()` at startup (`07-lifecycle.js`). |
| Pipeline route recorded when decisions are made | ✅ Met | `wlLog.info` at branch points in `07-lifecycle.js` (e.g. auto-pause decision); #255 adds per-array excluded-record logging at export time. |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | `CONTRIBUTING.md` documents solo-developer + AI-pair model. |
| Issue tracker records development tasks | ✅ Met | GitHub Issues; recent PRs reference issue/finding closures (e.g. #254 title cites "QA-2026-06-29 findings"). |
| New issues guided by user needs and stories | ✅ Met | Issue template "As a user…" sections in `.github/ISSUE_TEMPLATE/`. |
| Issue templates ensure proper logging | ✅ Met | `bug_report.md`, `feature_request.md` in `.github/ISSUE_TEMPLATE/`. |
| Acceptance criteria noted and fulfilment recorded | ✅ Met | PR template acceptance checklist; issues carry Definition of Done. |
| QA standards and processes defined | ✅ Met | `CLAUDE.md` operative standard; `QA.md` process; this automated weekly QA review is the enforcement mechanism — and is itself demonstrably closing its own findings week over week (#254 resolved 3 of last week's 5 priorities). |
