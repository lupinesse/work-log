# Fixture — NOT a real workflow. Deliberately broken.
#
# Reproduces the PR #256 regression: the `secrets` context is not available in
# `jobs.<job_id>.steps.if`. a11y-audit.yml carried this bug for weeks — every
# run failed with zero jobs and never posted a check.
#
# The correct pattern (used repo-wide) is to map the secret into `env` in a
# check step, emit a $GITHUB_OUTPUT flag, then guard on `steps.<id>.outputs.*`.
#
# Lives outside .github/workflows/ so GitHub never tries to run it.
# check-actionlint.mjs asserts actionlint still reports that "secrets" is not
# available here. Do not "fix" this file.
name: Fixture - secrets in step if
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Guarded on a context that is unavailable here
        if: secrets.SOME_TOKEN != '' # ← the bug
        run: echo "unreachable"
