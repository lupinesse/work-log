# QA Review — 2026-06-15

- Commit: `6f0cbcf` on `claude/fervent-bardeen-4k8i68`
- Checklist: Higher quality assurance (Duck Book)
- Reviewer: Claude Code (automated weekly QA review)

> **Audit environment note:** This audit runs in a fresh ephemeral container where `npm ci`
> has not been executed, so `npm run lint`, `npm run docs`, and `npm test` (which requires
> Playwright) cannot be invoked locally. ESLint and JSDoc totals are therefore inherited from
> the 2026-06-12 audit and updated where source diffs make the change unambiguous (e.g. the
> TypeScript optional-syntax grep). CI is the enforcement backstop: `ci.yml` runs lint +
> JSDoc + full test suite on every PR.

---

## Scoreboard

| Status        | Count |
|---------------|-------|
| ✅ Met        |  75   |
| ⚠️ Partial    |   8   |
| ❌ Not met    |   0   |
| ➖ N/A        |   4   |

**Total items assessed: 87**

---

## Regressions since last review (2026-06-12 / `5381010`)

Four items dropped from ✅ to ⚠️, all with the same root cause: `QA.md` was not updated when v1.9.0 shipped.

| Item | Was | Now | Root cause |
|------|-----|-----|------------|
| QA extent clearly documented | ✅ | ⚠️ | `QA.md` last entry is `v1.8.5–v1.8.9`. No v1.9.0 acceptance section; test counts cited (429 unit + 282 smoke) are now stale vs. 461 unit / 72 suites. |
| CHANGELOG available to users | ✅ | ⚠️ | 19 commits landed since the `v1.9.0` tag (`a7e1ad8`), including three user-facing bug fixes (#218 `initBoardTabs` error swallowing, #224 `readOptionalLogForBackup` ReferenceError, #226 a11y fixes). The `## Unreleased` section is empty; those fixes are undocumented in the user-visible log. |
| Stakeholder / user-acceptance sign-offs recorded | ✅ | ⚠️ | Same root cause as QA extent — `QA.md` has no v1.9.0 sign-off row. |
| Formal user-acceptance testing conducted and recorded | ✅ | ⚠️ | Same root cause — `QA.md` UAT table ends at v1.8.5–v1.8.9. |

---

## Improvements since last review

Twelve items rose in status (⚠️ → ✅), driven by a concentrated fix-and-refactor sprint between 2026-06-12 and 2026-06-15:

| Item | Was | Now | Evidence |
|------|-----|-----|----------|
| Low-level functions each carry out one specific task | ⚠️ | ✅ | Six monster modules split across 5 PRs (#208–#212): `pure-fns.js` 908 → 55-line barrel + 4 sub-modules (193/301/231/275 lines); `11-timeblock.js` 988 → 327 + `11a-timeblock-render.js` 350 + `11b-timeblock-carry.js` 390; `10a-tasks-render.js` 666 → 407 + `10a-tasks-row.js` 327 + `10b-signifiers.js` 89; `10b-tasks-events.js` 883 → 362 + `10c-tasks-board.js` + `10d-tasks-editors.js`; `12a-changelog.js` 899 → 268 + `12b-changelog-data.js` 633 + `12c-startup.js` 40. Largest module now `04-render.js` at 686 lines. |
| Comments are up to date and not misleading | ⚠️ | ✅ | PR #223 `docs(architecture): update stale line counts after module splits` — all five stale counts corrected; `ARCHITECTURE.md` now has 40 `#### **` module sections covering all 49 source files (sub-modules listed as nested bullets). |
| All functions and classes are documented | ⚠️ | ✅ | PR #213 `fix(docs): replace TypeScript optional syntax in JSDoc @param types` — 8 ERRORs eliminated. Grep confirms zero `@param.*?` TS-optional patterns in `10a-tasks-render.js`, `11-timeflow.js`, `pure-fns.js` or sub-modules. `npm run docs` added to `ci.yml` lint job, so this cannot silently regress. |
| Auto-generated HTML documentation | ⚠️ | ✅ | Same fix as above; `ci.yml` lint job now runs `npm run docs` and fails the check on non-zero exit. CI is the enforcement gate. |
| Published outputs meet accessibility regulations | ⚠️ | ✅ | PR #214 `fix(ci): enforce accessibility gate in Lighthouse CI` changed `.lighthouserc.json` `categories:accessibility` from `"warn"` to `"error"`. Build now fails if score drops below 0.85. |
| All input data documented in a data register | ⚠️ | ✅ | PR #215 `docs(data): add wl_location_v1 to DATA.md` — `wl_location_v1` (origin, managed-by, field shape `{"YYYY-MM-DD": "remote"\|"office"}`, retention) fully documented in `DATA.md §Work location`. |
| Fields in input and output datasets documented | ⚠️ | ✅ | Same PR — field dictionary entry for `wl_location_v1` complete. |
| Data documented in information asset register | ⚠️ | ✅ | Same PR — `DATA.md` is the asset register; location store entry closes the sole gap from the last review. |
| Design certificates confirm compliance | ⚠️ | ✅ | PR #220 `docs(architecture): refresh for v1.9.0` updated the certificate to `v1.9.0 (main, 2026-06-13)`, Status **Approved**. |
| Software fully specified | ⚠️ | ✅ | Same PR — `ARCHITECTURE.md` now has dedicated sections for `00-config.js`, `01b-migrate.js`, `logger.js`, `24-location.js`, `25-rollingsummary.js`, and all new sub-modules. All 49 source files accounted for. |
| Appropriate branching strategy defined and used | ⚠️ | ✅ | No direct commits to `main` in the audit window; all 19 post-v1.9.0 commits arrived through squash-merge PRs (#207–#226). |
| Commits tagged at significant stages | ⚠️ | ✅ | `v1.9.0` tag exists on GitHub at `a7e1ad8` (confirmed via API). All releases v1.8.1–v1.9.0 now tagged. |

Also improved within unchanged ⚠️ status: catch-block single-letter error variable names renamed to `err` via PR #217, reducing the ESLint `no-unused-vars` and implicit-name warnings.

---

## Priorities for this week

1. **Update `QA.md` for v1.9.0** — Add a v1.9.0 acceptance record with current test counts (461 unit / 72 suites confirmed today; smoke count from latest CI run), sign-off row, and UAT outcomes. This single action resolves all four regressions listed above. File: `QA.md`; pattern from existing `## v1.8.5–v1.8.9` section.

2. **Populate `## Unreleased` in CHANGELOG** — At least three user-facing fixes landed after v1.9.0 and are invisible to users: `initBoardTabs` localStorage error swallowing (#218), `readOptionalLogForBackup` ReferenceError (#224), and five a11y+changelog leftovers (#226). Add `### Fixed` entries to `## Unreleased` so the changelog is a reliable source of truth.

3. **Cut a v1.9.1 patch release** — 19 commits since v1.9.0 include bug fixes and CI enforcement changes. Tagging a patch release would mark the post-v1.9.0 stabilisation point and align `QA.md` with a concrete version anchor.

4. **Align `engines.node` with `.nvmrc`** — `package.json` `engines.node` remains `>=22.12.0` while `.nvmrc` pins `24.15.0`. This is the fourth consecutive review to flag it. Fix: set `"node": ">=24.0.0"` (or `24.x`) in `package.json`. One-line change in `package.json`.

5. **Reduce remaining ESLint warnings** — PR #217 fixed catch-block single-letter vars; single-letter chain vars (`e`, `t`, `d` in `.filter`/`.map` callbacks) remain. Target the five highest-frequency modules (`13-calendar.js`, `10a-tasks-render.js`, `11-timeblock.js`, etc.). The last-known count is 135 warnings; aim to bring it below 100.

---

## Full results

### Modular code

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Logic written as functions; classes where appropriate | ✅ Met | 49 JS source files in named-function functional style; no classes — appropriate for app size. |
| Code grouped into themed files/modules | ✅ Met | Numbered prefix convention extended to new sub-modules (`10a-tasks-row.js`, `10b-signifiers.js`, `10c-tasks-board.js`, `10d-tasks-editors.js`, `11a-timeblock-render.js`, `11b-timeblock-carry.js`, `12b-changelog-data.js`, `12c-startup.js`, `pure-fns-{format,export,tasks,validate}.js`). |
| Main scripts import and run high-level functions | ✅ Met | `07-lifecycle.js` `DOMContentLoaded` calls only high-level init functions; `build-config.js` is the single `LEAF_MODULES`/`readPureFnsExports` source of truth. |
| Low-level functions each carry out one specific task | ✅ Met | **Improvement.** Six previously flagged monster modules split (#208–#212). Largest is now `04-render.js` at 686 lines; `12b-changelog-data.js` at 633 is a data constant file (appropriate). No module over 700 lines. |
| Repetition minimised | ✅ Met | `fmtDur`, `escHtml`, `qa()`, `wlLog.*`, `safeCssColor` shared without duplication; `build-config.js` eliminates build-script repetition. |
| Open for extension, closed for modification | ✅ Met | `01b-migrate.js` migrations remain append-only; `pure-fns-*.js` sub-modules extend without touching the barrel. |
| Subclasses retain parent-class behaviour (Liskov) | ➖ N/A | Functional style — no class inheritance. |

### Good coding practices

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Names are informative and concise | ✅ Met | `buildRollingSummary`, `mergeBackupEntries`, `locationFor`, `parseJiraLabel`, `readOptionalLogForBackup` — self-documenting. |
| Names are explicit rather than implicit | ⚠️ Partial | PR #217 renamed catch-block `e`/`err` vars; single-letter vars (`e`, `t`, `d`) persist in `.filter`/`.map`/`.reduce` chains throughout the codebase (visible in ESLint warning list). Cannot re-run ESLint locally; CI enforcement is the backstop. |
| Code logic is clear and avoids unnecessary complexity | ✅ Met | Module splits reduced average module size significantly; no single module now exceeds 700 lines of logic code. |
| Code follows a standard style (ESLint + Prettier + Stylelint) | ✅ Met | CI `lint` job (`ci.yml`) runs `eslint` + `stylelint` + `npm run docs` on every PR. Last known: 0 errors, 135 warnings. Cannot re-run locally (packages not installed). |

### Project structure

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Clear, standard directory structure | ✅ Met | `src/`, `test/`, `docs/`, `.github/`, `scripts/`; build artefacts (`script.js`, `styles.css`, `dist/`) untracked. |
| Packages follow a standard structure | ✅ Met | `package.json` + `package-lock.json` committed; `.nvmrc` pins `24.15.0`. Known minor nit: `engines` says `>=22.12.0` while `.nvmrc` pins 24 — four reviews have flagged this; fix is a one-liner. |

### Code documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Comments explain *why*, not *what* | ✅ Met | `// Assumption:` blocks, module-split rationale comments, `// WHY:` notes persist throughout. |
| Comments are up to date and not misleading | ✅ Met | **Improvement.** PR #223 corrected all five stale line-count annotations in `ARCHITECTURE.md`. Certificate updated to v1.9.0 in #220. |
| Code is not commented out to control which lines run | ✅ Met | No toggled-off code blocks found. |
| All functions and classes are documented | ✅ Met | **Improvement.** PR #213 eliminated 8 JSDoc ERRORs (TypeScript `?` optional syntax in `@param` tags). Grep over `10a-tasks-render.js`, `11-timeflow.js`, `pure-fns*.js` confirms zero remaining `@param.*?` TS-optional patterns. CI now fails on JSDoc parse errors (`npm run docs` in lint job). |
| Python docstrings / R roxygen2 | ➖ N/A | JavaScript project; JSDoc is the equivalent. |
| Auto-generated HTML documentation | ✅ Met | **Improvement.** `npm run docs` added to `ci.yml` lint job; exit-1 fails CI. JSDoc errors fixed, so docs now generate clean. |
| Documentation hosted for easy access | ✅ Met | GitHub Pages via `docs.yml`; README links to hosted docs. |

### Project documentation

| Item | Status | Evidence / notes |
|------|--------|------------------|
| README covers purpose, installation, usage | ✅ Met | README current; documentation index links ARCHITECTURE.md, DATA.md, CONTRIBUTING.md, ROADMAP.md, QA.md. |
| Contributor guidance and code of conduct | ✅ Met | `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` present. |
| Desk instructions for unfamiliar users | ➖ N/A | Personal productivity tool, single user. |
| QA extent clearly documented | ⚠️ Partial | **Regression.** `QA.md` last entry is `v1.8.5–v1.8.9`. No v1.9.0 section; cited test counts (429 unit + 282 smoke) are stale vs. 461 / 72 suites confirmed today. |
| Assumptions documented next to code | ✅ Met | `// Assumption:` comments in `pure-fns*.js`, `05-entries.js`, `07-lifecycle.js`; `DATA.md` documents schema assumptions. |
| Copyright and licences specified | ✅ Met | `LICENSE` (MIT); `CITATION.cff` with `license-expression`. |
| Citation instructions given | ✅ Met | `CITATION.cff` with `preferred-citation`; README cite section. |
| Versioned releases (semantic versioning) | ✅ Met | Tags `v1.0.0`–`v1.9.0` on GitHub. `v1.9.0` confirmed at `a7e1ad8`. |
| CHANGELOG available to users | ⚠️ Partial | **Regression.** `v1.9.0` section is clean (`### Added / Changed / Fixed`, no duplicates — #222 fixed them). But `## Unreleased` is empty despite 19 post-release commits including three user-facing bug fixes (#218, #224, #226). |
| Example usage documented | ✅ Met | README usage examples; JSDoc `@example` on core pure functions. |
| Design certificates confirm compliance | ✅ Met | **Improvement.** `ARCHITECTURE.md` certificate: `v1.9.0 (main, 2026-06-13)`, Status **Approved** (PR #220). |
| Software fully specified | ✅ Met | **Improvement.** `ARCHITECTURE.md` now has 40 `#### **` sections covering all 49 source files (sub-modules as nested bullets). All previously missing sections (`00-config.js`, `01b-migrate.js`, `logger.js`, `24-location.js`, `25-rollingsummary.js`, and all new sub-modules) added in #220 and #223. |

### Version control

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Code is version controlled using Git | ✅ Met | Git repo; remote `lupinesse/work-log`. |
| Code committed regularly, per discrete unit of work | ✅ Met | PRs #207–#226 are single-topic squash commits; 19 in the audit window. |
| Appropriate branching strategy defined and used | ✅ Met | **Improvement.** No direct commits to `main` in audit window; all 19 post-v1.9.0 commits arrived through squash-merge PRs. (Local `5381010` issue from 2026-06-12 is resolved.) |
| Code is open-sourced; sensitive data omitted | ✅ Met | MIT licence; no credentials in tracked files; `config.local.ps1` and `00-config.local.js` gitignored. |
| Committing standards followed | ⚠️ Partial | 4 pre-commitlint commits remain in old history; all recent commits (#207–#226) are conventional-commit conformant (`fix(...)`, `refactor(...)`, `docs(...)`, `test(...)`). |
| Commits tagged at significant stages | ✅ Met | **Improvement.** `v1.9.0` confirmed on GitHub at `a7e1ad8`. All releases v1.8.1–v1.9.0 tagged. 19 post-release commits are untagged, warranting a v1.9.1 patch. |
| Continuous integration applied | ✅ Met | 16 GitHub Actions workflows active: `ci.yml` (lint + docs + test), `codeql.yml`, `lighthouse.yml`, `docs.yml`, `a11y-audit.yml`, `scss-audit.yml`, `dead-code.yml`, `jsdoc-check.yml`, `impact-check.yml`, `pr-review.yml`, `chatgpt-pr-review.yml`, `auto-fix-ci.yml`, `auto-label-pr.yml`, `delivery-metrics.yml`, `release.yml`, `weekly-qa-review.yml`. |

### Configuration

| Item | Status | Evidence / notes |
|------|--------|------------------|
| No credentials or secrets in code | ✅ Met | API keys server-side; local config files gitignored. |
| Configuration in dedicated config file | ✅ Met | `src/js/00-config.js`; personal overrides in gitignored `00-config.local.js`. |
| Multiple config files per system/local/user | ✅ Met | Base + user (`00-config.local.js`) + server (`config.local.ps1`). |
| Config files versioned separately from analysis code | ✅ Met | `00-config.js` committed independently; overrides untracked. |
| Config used to generate outputs is recorded | ✅ Met | `scripts/config-snapshot.cjs`; `wlLog.config()` at startup. |
| Example config templates provided | ✅ Met | `src/js/00-config.local.example.js` and `config.local.example.ps1`, dummy values only. |

### Data management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Published outputs meet accessibility regulations | ✅ Met | **Improvement.** `.lighthouserc.json` `categories:accessibility` now `"error"` (min 0.85) — CI fails on a drop. PR #214. |
| Analysis data stored in an open format | ✅ Met | All data JSON (localStorage + backup export). |
| Input data stored safely and treated as read-only | ✅ Met | Backup merge adds only entries with unseen IDs — never mutates existing records. |
| Input data versioned; changes create new versions | ✅ Met | `01b-migrate.js` append-only migration log; versioned `wl_*_v1` keys. |
| All input data documented in a data register | ✅ Met | **Improvement.** `DATA.md` now documents all 32 localStorage keys including `wl_location_v1` (PR #215). |
| Outputs are disposable — regenerated at will | ✅ Met | `npm run build` regenerates `script.js`, `styles.css`, `dist/`; none committed. |
| Non-sensitive data available so others can run the code | ✅ Met | Self-contained fixtures in `smoke-tests.cjs`; no personal data in repo. |
| Data quality monitored | ✅ Met | `wlLog.warn()` on `JSON.parse` failures; `initBoardTabs` localStorage errors now logged instead of swallowed (#218). |
| Fields in input and output datasets documented | ✅ Met | **Improvement.** `DATA.md` field dictionary now complete including `wl_location_v1` map shape. |
| Large or complex data stored in a database | ➖ N/A | localStorage appropriate for a single-user tool. |
| Data documented in information asset register | ✅ Met | **Improvement.** `DATA.md` serves as the register; location store gap closed (#215). |

### Peer review

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Peer review conducted and recorded near code | ✅ Met | PRs #207–#226 merged through ChatGPT + Claude four-phase dialogue with merge-gate. |
| Pair programming used | ✅ Met | `CONTRIBUTING.md` documents AI pair model; `CITATION.cff` credits Claude Code. |
| Users encouraged to participate in peer review | ⚠️ Partial | Templates invite contribution; inherent single-user tool limitation. |

### Testing

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Core functionality unit tested | ✅ Met | `test/unit.mjs` — **461 tests across 72 suites, 0 failures** (up from 437/63). New suites: regression #218, regression #224/#225. |
| Code-based tests run regularly | ✅ Met | `ci.yml` runs `npm test` on every PR and push to `main`; includes Playwright install step. |
| Bug fixes add new unit tests | ✅ Met | PRs #218, #224, #225 each shipped with regression tests; CHANGELOG cites evidence. |
| Informal tests recorded near the code | ✅ Met | JSDoc `@example` tags on core pure functions. |
| Stakeholder / user-acceptance sign-offs recorded | ⚠️ Partial | **Regression.** `QA.md` sign-off table ends at v1.8.5–v1.8.9; no v1.9.0 entry. |
| Tests run and recorded automatically via CI or git hooks | ✅ Met | `ci.yml` full suite; lint-staged pre-commit via Husky; unit test suites isolated with `node --test`. |
| Whole process tested end-to-end | ✅ Met | Smoke tests (`smoke-tests.cjs`) run in CI with Playwright installed. Last known: 317 passed, 0 failed (cannot re-run locally without `npm ci` + Playwright). |
| Test code is clean and readable | ✅ Met | `freshPage()` helper, fixtures, parameterised cases; new regression suites follow same pattern. |
| Formal user-acceptance testing conducted and recorded | ⚠️ Partial | **Regression.** `QA.md` UAT process and outcomes end at v1.8.5–v1.8.9. |
| Integration tests confirm multiple units work together | ✅ Met | Smoke suite exercises full app flows; `ci-scripts.test.mjs` (30 tests) passes. CI scripts tests verified locally: 30 pass, 0 fail. |

### Dependency management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Required secrets documented outside version control | ✅ Met | Example config files document all secrets with placeholders. |
| Required libraries documented including versions | ✅ Met | `package-lock.json` committed; exact pins. 15 devDependencies. |
| Working OS environments documented | ✅ Met | README (Windows primary), `launch.sh`, `.devcontainer/devcontainer.json`. |
| Example configuration files provided | ✅ Met | `src/js/00-config.local.example.js`, `config.local.example.ps1`. |
| Code runs independently of operating system | ✅ Met | `path.join()` throughout build scripts. |
| Dependencies managed separately for users/devs/testers | ✅ Met | 0 runtime dependencies; 15 devDependencies. |
| As few dependencies as possible | ✅ Met | 15 devDependencies (same as last review); no new additions. |
| Package dependencies managed with environment manager | ✅ Met | npm + lockfile + `.nvmrc` (24.15.0). |
| Docker containers / VM builds version controlled | ✅ Met | `.devcontainer/devcontainer.json` (javascript-node:1-24-bookworm). |

### Logging

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Misuse or failure produces informative error messages | ✅ Met | `wlLog.error()`/`warn()` on parse failures, missing DOM, API errors; `initBoardTabs` localStorage errors now logged (#218). |
| Code configuration recorded when code runs | ✅ Met | `wlLog.config()` at startup (`07-lifecycle.js`). |
| Pipeline route recorded when decisions are made | ✅ Met | `wlLog.info` at branch points throughout BuJo modules (closed #12 in v1.9.0). |

### Project management

| Item | Status | Evidence / notes |
|------|--------|------------------|
| Roles and responsibilities clearly defined | ✅ Met | `CONTRIBUTING.md` solo-developer + AI-pair model. |
| Issue tracker records development tasks | ✅ Met | GitHub Issues; PRs #207–#226 reference issue closure. |
| New issues guided by user needs and stories | ✅ Met | Issue template "As a user…" sections. |
| Issue templates ensure proper logging | ✅ Met | `bug_report.md`, `feature_request.md` in `.github/ISSUE_TEMPLATE/`. |
| Acceptance criteria noted and fulfilment recorded | ✅ Met | PR template acceptance checklist; issues carry Definition of Done. |
| QA standards and processes defined | ✅ Met | `CLAUDE.md` operative standard; this automated review is the enforcement mechanism. |
