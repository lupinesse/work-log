node_modules/
dist/
portable/

# Root-level build artefacts — generated by `npm run build`, never commit directly
script.js
styles.css
# Ignore generated JSDoc HTML output but keep the qa-reports and dead-code-reports directories
docs/*
!docs/qa-reports/
!docs/dead-code-reports/
!docs/ab-tests/
.portable-dest
.DS_Store
*.local
lint-results.txt

# Build artefacts — regenerated by `npm run build`, never committed
script.js
styles.css

# Generated / local-only files
test-results.txt
lint-out.json
verify-*.png
*.png
!screenshots/

# Personal JS config overrides — never commit
src/js/00-config.local.js

# Personal PowerShell config overrides — never commit (*.local only matches no-extension files)
config.local.ps1

# Personal data — never commit
timesheets/
JSON backups/

# Stale local files — not part of the project
claude-code-top-zone.md
assistant/
*.exe

# Claude Code session data — ignore local state but track shared skills and docs
.claude/*
!.claude/skills/
!.claude/token-optimization.md
